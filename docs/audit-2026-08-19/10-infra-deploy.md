# Aria: infra / deploy / productie

Audit read-only 2026-08-19. Surse: `Dockerfile.app`, `start-combined.sh`, `apps/*/Dockerfile`, `dev-infra/*`, `docs/08-deployment.md`, `README-SETUP.md`, `apps/backend/src/config/config.schema.ts`, `main.ts`, `app.module.ts`, `infra/**`, `modules/health/**`, `prisma/schema.prisma` + 29 migrari + seed-uri, `package.json`-uri, `jest.config.js`, `.gitignore`/`.dockerignore`, `next.config.mjs` (diff necomis), `git log -15` vs `CHANGELOG.md`.

Stare deploy (din docs/08 + memorie proiect): LIVE pe Railway, UN container (`Dockerfile.app` + `start-combined.sh`), Postgres + Redis plugin, MinIO (`bitnamilegacy/minio`), Mailpit ca sink SMTP. Ultimul deploy cunoscut: 2026-08-03 (Studio 3D). Working tree are ~4.800 linii NECOMISE (copy RO/EN rescris, review-step, fisa cererii, fix socket in `next.config.mjs`).

## BINE FACUT

- **Validare env fail-fast cu Zod la boot** — `apps/backend/src/config/config.schema.ts:4-73` (`ConfigModule.forRoot({ validate })` in `app.module.ts:37`): secretele au `min(16)`, URL-urile `url()`, `MESSAGE_ENCRYPTION_KEY` regex 64 hex. Lipsa unei variabile obligatorii = container care nu porneste, nu eroare tacuta.
- **Health endpoint real** — `modules/health/health.controller.ts:22-36`: verifica DB (`SELECT 1`), Redis (`ping`), S3 (`HeadBucket`), intoarce 503 cu detalii daca ceva e jos. Public (`@Public()`), la `/api/v1/health`.
- **Logging structurat JSON** cu traceId per request (`app.module.ts:39-47`: `nestjs-pino`, `genReqId` din `x-trace-id` sau uuid, `redact` pe `authorization` + `cookie`; `pino-pretty` DOAR in development). Filtrul global de erori (`common/filters/all-exceptions.filter.ts`) nu scurge stack-uri catre client si logheaza exceptiile non-HTTP.
- **Storage corect (invarianta 3.4)** — `infra/storage/storage.service.ts:13-16`: doar presigned PUT 15 min / GET 5 min, `forcePathStyle` pt MinIO, `ContentLength`+`ContentType` legate in semnatura PUT (clientul nu poate urca alt tip/marime decat cele declarate). Nicio rutare de fisiere prin NestJS.
- **Migrari disciplinate**: 29 foldere cu timestampuri monoton crescatoare (2026-06-12 → 2026-08-02), `migration_lock.toml` = postgresql, SQL-ul manual (2 migrari cu `UPDATE` de backfill + `DROP COLUMN` la `20260705100200_desired_deadline_bucket` / `20260705100300_contact_channel_enum`) este comentat si are backfill INAINTE de drop; migrarile noi (`20260802*`) au comentariu `-- Rollback:`. Trigger append-only pe `audit_logs` (`20260615201326`, linia 125-133).
- **Indexuri pe coloanele fierbinti**: `requests(status)`, `requests(client_user_id)`, `claim_slots(request_id,status)`, `claim_slots(company_id)`, `notifications(user_id,read_at)`, `penalty_events(company_id,scope,expires_at)`, `idempotency_keys(expires_at)`, `quote_versions(valid_until)`, `audit_logs(created_at)`, `refresh_tokens(token_hash)` unique. Nu am gasit lipsuri grave pentru volumul MVP.
- **Cookie-uri httpOnly + `secure` in prod + SameSite=Lax** (`auth.controller.ts:48-56`), API-ul e proxied same-origin prin rewrites Next (`next.config.mjs:25-37`) → fara CORS cross-site, fara cookies third-party.
- **Docker**: build multi-stage, `openssl` instalat inainte de `prisma generate` (gotcha documentat), Chromium de sistem pt Puppeteer (`PUPPETEER_SKIP_DOWNLOAD=1`), pnpm fixat (`corepack prepare pnpm@9.12.3`), `--frozen-lockfile`. `.dockerignore` exclude `**/.env*`, `.git`, `node_modules`, `Design`, `dev-infra`. Niciun `.env` real in git (`git ls-files | grep .env` → doar `.env.example`).
- **Seed-uri idempotente**: `seed.ts` = upsert-uri; `seed-demo.ts` se opreste daca `admin@demo.ro` exista (linia 28-31); parolele demo vin din env (`DEMO_PASSWORD`/`DEMO_ADMIN_PASSWORD`, linia 32-33) — adminul public nu are parola din repo.
- **docs/08 are sectiunea "gotcha-uri Railway"** (limita 5 servicii, PORT injectat, bitnami legacy, MSYS path) si "Verificare post-deploy" — util operational.
- **Teste fara infrastructura**: `jest.config.js` mapeaza `@marketplace/shared` pe surse, toate cele 6 spec-uri backend (39 teste, log `04-backend-test.log` EXIT=0) mock-uiesc Prisma/Redis/S3. Shared: 3 fisiere vitest (113 teste: engine 51, piece3d 52, guided-pieces v3 10).
- `trust proxy` + `helmet` + `ValidationPipe(whitelist, forbidNonWhitelisted)` + throttler global 100/min montate in `main.ts:21-31` / `app.module.ts:49`.

## DE MODIFICAT — `[P0|P1|P2] cale:linie — problema → recomandare`

### Email (BLOCANT lansare)
- `[P0] docs/08-deployment.md:60-62 + Railway vars SMTP_HOST=${{mailpit...}}` — in prod SMTP-ul este **Mailpit** (sink): NICIUN email nu ajunge la utilizatori reali. Iar `auth.service.ts:128-131` refuza login-ul fara `emailVerifiedAt` (`EMAIL_NOT_VERIFIED`) → un utilizator real NU poate intra niciodata in cont. La fel: notificari, unsubscribe, reset. → Provider SMTP real (Brevo/Resend/Postmark/SES) pe un domeniu detinut: `SMTP_HOST/PORT/USER/PASS`, `SMTP_SECURE=true` (465) sau STARTTLS pe 587 (nodemailer face upgrade automat cu `secure:false`), `MAIL_FROM` pe domeniul propriu + SPF, DKIM, DMARC in DNS. Codul suporta deja (`mail.service.ts:12-21`), e doar config.
- `[P1] apps/backend/src/infra/mail/mail.service.ts:24` — `sendMail` fara `replyTo`, fara varianta `text` (doar `html`), fara `List-Unsubscribe` header → scor spam mai slab. → adauga `REPLY_TO`/`SUPPORT_EMAIL` in config + text fallback + `List-Unsubscribe` (exista deja link HMAC de unsubscribe).
- `[P1] docs/08-deployment.md:62` — `MAIL_FROM=no-reply@cozyfable.app`: domeniul trebuie sa fie cel real al platformei (brandul e "CozyHome") si DNS-ul configurat; altfel SPF fail.
- `[P2] apps/backend/src/config/config.schema.ts:53-57` — `NOMINATIM_USER_AGENT` default `contact: dev@marketplace.local`. Politica Nominatim cere contact real; pe prod seteaza explicit (altfel risc de blocare IP → geocodarea cererilor pica).

### Date demo pe prod (BLOCANT lansare)
- `[P0] apps/backend/prisma/seed-demo.ts` (rulat pe prod conform docs/08 + memorie) — prod contine acum date fictive care trebuie STERSE inainte de lansare. Ce creeaza (inventar pentru curatare):
  - **Useri (24, parola comuna `DEMO_PASSWORD`)**: `admin@demo.ro` (ADMIN), clienti `ana.popescu@`, `mihai.ionescu@`, `elena.dumitru@`, `radu.stanescu@`, `ioana.marinescu@demo.ro`; owneri `owner.a..h@demo.ro` (8); echipa A: `mgr.a@`, `trusted1.a@`, `trusted2.a@`, `managed.a@demo.ro`; echipa B: `mgr.b@`, `trusted1.b@`, `trusted2.b@`, `trusted3.b@demo.ro` (linii 46-62, 132-147).
  - **Firme (8)**: A Mobila Premium, B DesignWood, C CasaMea, D Atelier Bucov, E Lemn & Stil, F FastFurniture, G MobMaster, H VintageHaus + `company_members`, `company_locations`, `company_verification_profiles`, `company_credit_wallets` (200 credite), `subscriptions`, `company_offer_field_permissions`, `penalty_events` (linii 66-130).
  - **Cereri (15)** R1..R15 cu `request_rooms`, `request_contact_preferences`, `claim_slots` (+ `chat_threads`), `quotes` + `quote_versions` + `quote_change_requests`, `reviews` (2) + `review_disputes` (1), `mock_billing_orders` (2 facturi seria `MM` nr. 400 si 700, seller "Marketplace Mobilier SRL"/RO12345678), `audit_logs` (52 randuri `entity_type='demo'`), `notifications` (30, payload `{demo:true}`) (linii 152-270).
  - **Plus**: `update-demo-credits.ts` a rescris `credit_cost` pe toate cererile si wallet=200 pe firmele `@demo.ro`; `seed-inspiration.ts` a creat firma REALA "Mobila Unicat" cu `cui='N/A'`, `regComNumber='N/A'` (linii 12-21) si a DEPUBLICAT pozele Unsplash (raman in tabel).
  → Scrie un script `prisma/cleanup-demo.ts` (read in audit, nu exista) care sterge in ordine FK: notificari/audit demo → quotes/claims/chat → requests demo → wallets/subscriptions/members → companies demo → users `@demo.ro`; pastreaza Mobila Unicat (completeaza CUI/RegCom real) si config seed.ts. Alternativ: `railway` DB reset + DOAR `seed.ts` + `seed-inspiration.ts`.
- `[P0] apps/backend/prisma/seed.ts:149-156` — `system_settings` seed-uite pe prod cu date fiscale FICTIVE: `seller_name='Marketplace Mobilier SRL'`, `seller_cui='RO12345678'`, `seller_reg_com='J40/1234/2020'`, `seller_address='Str. Exemplu...'`, `seller_iban='RO49AAAA1B31007593840000'`, `invoice_series='MM'`. `payments.service.ts:112-119` le pune in snapshotul facturii → prima factura reala ar iesi cu CUI/IBAN false. → completeaza din Admin → Settings inainte de prima vanzare; sterge cele 2 `mock_billing_orders` demo (numerotarea `MM` ar continua de la 701, cu gol 1-399/401-699 — inadmisibil fiscal).
- `[P1] apps/backend/prisma/seed-demo.ts:32` + `seed.ts` — niciun guard `NODE_ENV==='production'`/`ALLOW_DEMO_SEED` → o rulare gresita pe prod (ex. `pnpm prisma:seed` dupa un reset) re-populeaza date demo. → guard explicit in `seed-demo.ts` (abort daca `NODE_ENV=production` fara `ALLOW_DEMO_SEED=1`) si un `console.log` cu DB host-ul tinta (mascat) inainte de scriere.
- `[P1] apps/frontend/src/messages/ro.json:4097` (+ en.json) — pagina Termeni/Confidentialitate are placeholdere `[DENUMIRE LEGALĂ FIRMĂ]`, `[CUI]`, `[ADRESĂ]`, `[EMAIL CONTACT]` → trebuie completate cu entitatea juridica reala inainte de lansare (GDPR art. 13 cere operatorul identificat).

### Container / proces / restart
- `[P1] start-combined.sh:1-20` — bash ruleaza ca PID 1 fara `trap` si fara `exec`: SIGTERM trimis de Railway la redeploy NU ajunge la `node dist/main.js`/`server.js` (PID 1 ignora semnalele fara handler) → containerul e SIGKILL-uit dupa grace period; `enableShutdownHooks()` din `main.ts:46` nu se executa niciodata in prod: Prisma nu se deconecteaza curat, workerii BullMQ nu inchid job-urile in zbor (devin "stalled", re-procesate dupa ~30s — ok doar pentru ca sunt idempotente), browserul Puppeteer ramane orfan. → `trap 'kill -TERM $BACK_PID $FRONT_PID; wait' TERM INT` in script SAU `tini` ca init (`ENTRYPOINT ["tini","--"]`) + `exec`.
- `[P1] start-combined.sh:9` — `prisma migrate deploy` la FIECARE boot, inainte de a porni aplicatiile: (a) daca migrarea esueaza, containerul moare in loop si frontendul (acelasi container) dispare si el → site down total; (b) la deploy Railway exista fereastra in care containerul VECHI ruleaza pe schema NOUA (fara migrari compatibile-inapoi = erori); (c) nu exista rollback (Prisma nu genereaza `down`). → pastreaza migrate la boot (acceptabil la 1 replica) dar: migrari mereu aditive (expand/contract), backup DB inainte de fiecare deploy cu migrare, si un `healthcheckPath=/api/v1/health` configurat in Railway ca sa nu comute traficul pe un container care nu a pornit.
- `[P1] Dockerfile.app:43` — `COPY --from=build /app ./` copiaza TOT workspace-ul: devDependencies (nest cli, jest, ts-jest, tsx, pino-pretty, prisma CLI, eslint FE), sursele `.ts`, `.next/` complet (inclusiv `cache/`), `apps/frontend/node_modules` (three/drei/framer pentru build) → imagine de cativa GB, suprafata de atac mai mare, cold-start mai lent. → `pnpm deploy --prod` / `pnpm prune --prod` pentru backend in stagiul final, pastreaza doar `dist`, `prisma`, `node_modules` prod si `/app/web` (standalone).
- `[P1] Dockerfile.app:34-55 + apps/*/Dockerfile` — niciun `USER node`; procesele ruleaza ca **root** (Chromium pornit cu `--no-sandbox` ca root: `quote-pdf.service.ts:44-46`, `invoice-pdf.service.ts:56`). → `USER node` in stagiul final (Chromium merge ca user neprivilegiat cu `--no-sandbox`, si e si mai sigur).
- `[P2] Dockerfile.app:6,34` — `node:20-bookworm-slim` nepinuit pe digest/minor (build nereproductibil). Node 20 iese din suport activ in 2026-04 (maintenance pana 2026-04-30 — la data auditului e deja in maintenance/EOL). → `node:22-bookworm-slim@sha256:...`.
- `[P2] start-combined.sh:11-15` — cele doua procese impart CPU/RAM-ul unui singur serviciu Railway; PDF-urile Puppeteer (Chromium) concurente cu Next SSR pot da OOM. → limita de pagini concurente in PdfService / cache PDF pe S3 (iese din aria infra, semnalez).
- `[P2] apps/backend/src/app.module.ts:49` — ThrottlerModule cu storage in memorie: la mai multe replici limitele devin per-instanta. Ok la 1 replica; daca se scaleaza → `@nest-lab/throttler-storage-redis`.
- `[P2] apps/backend/src/main.ts:21` — `app.set('trust proxy', true)` = incredere in TOATE hop-urile din `X-Forwarded-For` → un client poate trimite propriul `X-Forwarded-For` si sa ocoleasca throttlingul per-IP (login 5/min) si `IP_HASH`-ul din audit. → `trust proxy` = numar de hop-uri cunoscut (edge Railway + rewrite Next = 2) sau lista CIDR.
- `[P2] apps/backend/src/infra/event-bus/events.gateway.ts:16` — Socket.IO `cors: { origin: true, credentials: true }` reflecta orice origine; REST-ul restrange la `FRONTEND_ORIGIN` (`main.ts:25`). → foloseste aceeasi valoare.

### Observabilitate
- `[P1] (lipsa)` — fara Sentry/alerting/uptime monitor; fara log level configurabil (`LOG_LEVEL` nu exista in `config.schema.ts`; pino pe default `info` in prod, inclusiv `autoLogging` pe TOATE requesturile = zgomot + cost). Nicio metrica (`/metrics`). → Sentry (`@sentry/nestjs` + `@sentry/nextjs`) sau macar alert Railway pe restart + un uptime check extern pe `/api/v1/health` (BetterStack/UptimeRobot); `LOG_LEVEL` env; `autoLogging.ignore` pe `/health`.
- `[P2] apps/backend/src/app.module.ts:43` — `redact` acopera doar `authorization` si `cookie`; `req.url` e logat in clar — azi nu exista tokenuri in query pe backend (verify-email e POST cu body, `auth.controller.ts:75`), dar linkul `/verify-email?token=` (`auth.service.ts:88`) ajunge pe Next/edge Railway logs. → pastreaza tokenurile in body/hash (ok) si nu activa access log pe Next.
- `[P2] (lipsa)` — niciun endpoint de readiness separat de liveness; health face 3 apeluri externe la fiecare request si e public fara throttle dedicat (cade sub 100/min global). Ok pentru MVP; daca se pune un monitor agresiv, exclude din throttle si cache 5s.

### Storage / MinIO
- `[P1] docs/08-deployment.md:26 (`bitnamilegacy/minio`)` — imagine "legacy" nementinuta (Bitnami a oprit publicarea in 2025; MinIO insusi in mod mentenanta) → fara patch-uri de securitate; volum Railway fara backup/replicare mentionat nicaieri (`grep backup docs/` → nimic). → pentru lansare: Railway Object Storage / Cloudflare R2 / Backblaze B2 (S3-compatible, presigned merge identic — codul schimba doar `S3_ENDPOINT/KEY/SECRET`, eventual `forcePathStyle`); daca ramane MinIO: snapshot periodic al volumului + `mc mirror` catre un bucket extern.
- `[P1] (config Railway, de verificat)` — credentialele MinIO: sunt `minioadmin/minioadmin` (valorile din `dev-infra` / `docker-compose.prod.yml:38-39`)? Consola `:9001` e expusa public? docs/08 spune "doar API presigned" dar nu documenteaza. → roteste cheile, verifica ca doar `:9000` are domeniu public, bucket `uploads` PRIVAT (nu `download` policy).
- `[P2] infra/storage/storage.service.ts` — CORS pe bucket pentru PUT din browser nu e configurat de aplicatie (MinIO permite implicit `*`; pe R2/S3 real trebuie regula CORS explicita `PUT, GET` de pe domeniul FE). → adauga in checklist-ul de migrare storage. Limita de marime e in schema (`MAX_ATTACHMENT_BYTES` in `packages/shared/src/request.schemas.ts:213`, 25MB cf. memorie) si legata in semnatura — OK. Nu exista job de curatare a obiectelor orfane (upload initiat fara finalize) — `[P2]`.

### Baza de date
- `[P1] (lipsa)` — niciun backup documentat/automat: Railway Postgres are backups doar pe planurile platite (si manual); nicio mentiune in docs/08 sau CHANGELOG. → activeaza backup-uri Railway (sau `pg_dump` zilnic dintr-un cron extern catre R2/B2) INAINTE de lansare; testeaza un restore o data.
- `[P2] apps/backend/prisma/schema.prisma:502-548` — `requests` are `@@index([status])` si `@@index([client_user_id])`, dar marketplace-ul (`marketplace.service.ts:153-195`) filtreaza `status IN claimable AND published_at IS NOT NULL ... ORDER BY published_at DESC` → la volum, index compus `(status, published_at DESC)` evita sortarea. La fel `messages(chat_thread_id, created_at)` pentru paginare chat si `refresh_tokens(expires_at)` / `geocoding_cache(expires_at)` pentru curatare (nu exista job de cleanup pentru refresh tokens expirate, idempotency keys, geocoding cache — cresc nelimitat; `[P2]`).
- `[P2] prisma/schema.prisma` — nu exista `directUrl`/`connection_limit` configurat pentru Railway (Postgres free/hobby are ~20-100 conexiuni); Prisma default = `num_cpus*2+1` per proces. Ok la 1 container; documenteaza `?connection_limit=10` in `DATABASE_URL` daca apar `too many connections`.

### Frontend / Next / domeniu
- `[P1] apps/frontend/next.config.mjs (diff necomis, linii 19-37)` — fixul `skipTrailingSlashRedirect` + rewrite `/socket.io/` exact (fara el Next face 308 pe `/socket.io/` → handshake-ul WebSocket moare, realtime-ul pe prod e degradat/ mort) este NECOMIS si NEDEPLOYAT. Impreuna cu cele ~4.800 linii de copy rescris. → commit + deploy + verificare `transport: websocket` in prod.
- `[P1] (lipsa)` — fara `robots.txt`, `sitemap.xml`, `manifest`, `favicon`/`icon` in `apps/frontend/public` sau `app/` (`git ls-files | grep robots|sitemap|manifest|favicon` → nimic; `public/` are doar `illustrations/`). `metadata` din `layout.tsx:31-35` nu are `metadataBase`, `openGraph`, `alternates` (RO/EN hreflang). `/admin/*`, `/dashboard`, `/marketplace/*` sunt accesibile crawlerelor (fara `robots`). → `app/robots.ts` (disallow `/admin`, `/dashboard`, `/marketplace`, `/requests`, `/api`), `app/sitemap.ts` (landing, `/partners`, `/inspiration`, `/studio`, `/sketch-guide`, `/terms`, `/privacy` x2 locale), `app/icon.png` + `apple-icon`, `metadataBase = new URL(FRONTEND_ORIGIN)`.
- `[P1] docs/08-deployment.md:7` — aplicatia traieste pe `backend-production-2813.up.railway.app`: niciun domeniu custom, HTTPS doar cel Railway, `FRONTEND_ORIGIN` trebuie schimbat la domeniul final (afecteaza CORS `main.ts:25`, linkurile din email `auth.service.ts:88`, cookie `secure`). → cumpara/leaga domeniul in Railway (CNAME), actualizeaza `FRONTEND_ORIGIN`, `MAIL_FROM`, restrictia de referrer a cheii Google Maps (`NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` e COAPTA in bundle — public by design, deci restrictia pe domeniu e obligatorie).
- `[P2] apps/frontend/next.config.mjs` — fara `headers()` de securitate pe raspunsurile Next (HSTS, `X-Frame-Options`/CSP `frame-ancestors`, `Referrer-Policy`, `Permissions-Policy`); helmet e doar pe backend (`main.ts:22`), iar HTML-ul vine de la Next. `poweredByHeader` ramane `true`. → `headers()` global + `poweredByHeader: false`. Compresia: Next standalone are `compress: true` implicit (ok); backendul nu comprima JSON (`compression` lipsa) — edge-ul Railway nu garanteaza gzip → `[P2]` adauga `compression()` in `main.ts`.
- `[P2] (GDPR)` — FE nu are analytics/pixel (grep `gtag|posthog|hotjar|plausible|fbq` → 0 rezultate; cookie-uri = doar auth httpOnly + `NEXT_LOCALE`) → banner de cookie NU e necesar azi, iar textul din `Legal.privacy.cookies` (ro/en.json:4162) spune corect asta. ATENTIE: scriptul Google Maps Places (`address-autocomplete.tsx:21`) se incarca din `maps.googleapis.com` cand exista cheie → transfer de IP catre Google; unele DPA-uri cer consimtamant/mentiune explicita. → mentioneaza Google Maps in Politica de confidentialitate; ideal incarca scriptul doar la focus pe campul de adresa.
- `[P2] (lipsa)` — nicio pagina de mentenanta/status; la crash-ul containerului unic (backend SAU frontend) Railway intoarce 502 generic. → Railway "maintenance mode"/pagina statica sau macar un status page extern.

### CI / calitate
- `[P1] (lipsa `.github/`)` — nu exista CI: `pnpm lint`/`typecheck`/`test` nu ruleaza la push; `railway up` se face de pe masina locala (fara gating). → workflow GitHub Actions minimal: `pnpm install --frozen-lockfile` → `build:shared` → `typecheck` → `lint` → `backend test` + `shared vitest` → (optional) `docker build -f Dockerfile.app` smoke. Deploy Railway din `main` dupa CI verde.
- `[P2] apps/backend/package.json:10` — `test` = jest pe 6 spec-uri (39 teste: calendar 5, credits 5, message-crypto 7, claims helpers 7, notification-emails 8, uploads 7) — zero teste pe claim concurrency (invarianta critica), auth/refresh rotation, idempotency interceptor, quote state machine; frontend: ZERO teste (niciun `*.test.tsx`, niciun Playwright). `packages/shared` are `vitest` (113 teste) dar NU e inclus in niciun script radacina (`package.json` root nu are `test`). → `"test": "pnpm -r --if-present test"` la radacina; un test de integrare pentru claim concurrent (docker Postgres in CI) inainte de lansare.

### Docs
- `[P1] docs/08-deployment.md:49-75` — sectiunea "Variabile backend (Railway)" spune `RAILWAY_DOCKERFILE_PATH=apps/backend/Dockerfile` si blocul "Frontend (build args)" `RAILWAY_DOCKERFILE_PATH=apps/frontend/Dockerfile` + `BACKEND_INTERNAL_URL=https://<backend-domain>`, desi arhitectura LIVE (linia 16-27) si practica (`railway up --service backend` cu `Dockerfile.app`, cf. memorie) folosesc UN container cu `BACKEND_INTERNAL_URL=http://127.0.0.1:3001` copt in ARG. Contradictie → cineva nou ar configura gresit. → rescrie blocul de variabile pentru `Dockerfile.app` (ARG-urile `NEXT_PUBLIC_*` se dau ca build args Railway), marcheaza `apps/*/Dockerfile` ca "doar pentru docker-compose.prod.yml / self-host". Lipsesc din docs/08: `MESSAGE_ENCRYPTION_KEY` (setat pe prod 2026-07-15), `NOMINATIM_*`, `DEMO_*` ca var de seed, `TWO_FACTOR_FEATURE_ENABLED`.
- `[P1] apps/backend/.env.example` — lipsesc `MESSAGE_ENCRYPTION_KEY`, `NOMINATIM_BASE_URL`, `NOMINATIM_USER_AGENT` (toate citite in `config.schema.ts:53-70`); `apps/frontend/.env.example` lipseste `BACKEND_INTERNAL_URL` (citit in `next.config.mjs:26`) si contine `NEXT_PUBLIC_SOCKET_URL` care NU mai e citit nicaieri (`use-socket.ts:9` deriva din `NEXT_PUBLIC_API_URL`). → sincronizeaza; comenteaza ca `NEXT_PUBLIC_*` sunt publice (nimic sensibil acolo azi — doar cheia Maps, publica by design).
- `[P2] CHANGELOG.md:3-8` — capul e "Studio 3D r6 — 2026-08-03"; commitul `afa58e0` (2026-08-04, "scoate pastila de scurtaturi") NU apare; rescrierea copy-ului 2026-08-16 (~1.900 stringuri, cf. memorie) + fisa cererii + review-rail sunt necomise si neconsemnate. → intrare CHANGELOG + commit inainte de deploy.
- `[P2] README-SETUP.md` — nu e un README de setup al proiectului, ci instructiunile "starter pack" pentru agent (cum sa pornesti Sprint 0). Un dev nou nu afla cum ruleaza local (docker compose → `.env` → `prisma migrate dev` → seed → `pnpm dev:*`). → README real (sau redenumeste in `docs/00-agent-starter.md`).

## DE STERS — fisiere/scripturi/config moarte + dovada

- `apps/backend/prisma/update-demo-credits.ts` + scriptul `seed:demo-credits` (`apps/backend/package.json:14`) — one-off PO r5 deja rulat pe prod (comentariul din fisier, linia 3-4: "rulat pe prod via railway run"); rerularea ar rescrie `credit_cost` pe toate cererile. Dovada: niciun alt import (`grep -rn update-demo-credits` → doar package.json).
- `NEXT_PUBLIC_SOCKET_URL` din `apps/frontend/.env.example:2`, `apps/frontend/Dockerfile:18,22`, `Dockerfile.app:26,30` — necitit nicaieri in `apps/frontend/src` (`grep -rn NEXT_PUBLIC_SOCKET_URL apps/frontend/src` → 0; `use-socket.ts:9` foloseste `NEXT_PUBLIC_API_URL`).
- `docs/08-deployment.md:74-75` blocul "Frontend (build args) ... RAILWAY_DOCKERFILE_PATH=apps/frontend/Dockerfile" — serviciu frontend separat nu exista pe Railway (limita 5 servicii, linia 16-27).
- `apps/backend/Dockerfile` + `apps/frontend/Dockerfile` — NU sunt folosite de deploy-ul Railway (doar de `dev-infra/docker-compose.prod.yml:84,103`). Nu le sterge daca self-host-ul e o optiune reala, dar marcheaza in header "folosit DOAR de docker-compose.prod.yml" si tine-le sincronizate cu `Dockerfile.app` (azi `apps/frontend/Dockerfile` nu are `mkdir -p public` diferit... are; ok — dar nu au fost testate din 2026-07-12, `git log` fara modificari ulterioare).
- `apps/frontend/src/app/[locale]/dev/piece-3d/page.tsx` — pagina de lucru dev; in prod `notFound()` (linia 17) dar ruta + bundle-ul ei intra in build si `generateStaticParams` o prerenderizeaza pentru ro/en. Sterge sau muta sub `(dev)` exclus din build de prod.
- `apps/frontend/src/app/[locale]/landing-v2/page.tsx` — conform commit `2a4ff8e` landing v2 a fost promovat pe `/` si `/landing-v2` doar redirectioneaza; nu e gasit niciun `robots: noindex` (`grep noindex` → 0). Pastreaza doar redirectul (in `next.config.mjs redirects()`) si sterge pagina.
- `.claude/launch.json` diff necomis (`-F frontend exec next dev` → `-F frontend dev`, scos `autoPort`) — local tooling, decide commit sau revert.

## INTREBARI PENTRU PO / DECIZIE NECESARA

1. **Domeniul final** (CozyHome? cozyfable.app? .ro?) — necesar pentru `FRONTEND_ORIGIN`, `MAIL_FROM`, SPF/DKIM, restrictia cheii Google Maps, `metadataBase`/sitemap. Fara el niciun pas de lansare nu e final.
2. **Provider SMTP** si bugetul: Brevo/Resend/Postmark/SES? Si adresa `reply-to`/suport (apare si in Termeni `[EMAIL CONTACT]`).
3. **Entitatea juridica** pentru Termeni/Confidentialitate si pentru `system_settings.seller_*` (denumire, CUI, Reg. Com., adresa, IBAN, seria reala de facturi). Facturile "mock" raman mock la lansare (fara procesator de plata real)? Daca da, textul facturii trebuie sa spuna explicit ca e proforma.
4. **Ce pastram din datele demo pe prod**: totul sters (recomandat) sau ramane un cont de firma demo pentru prezentari? Mobila Unicat ramane ca firma-partener cu galeria? Are CUI/Reg Com reale de completat?
5. **Storage la lansare**: ramanem pe MinIO (bitnamilegacy, fara backup) sau trecem pe R2/B2/Railway Object Storage (cost ~0 la volumul MVP)? Decizia afecteaza checklist-ul (migrare obiecte existente = pozele de inspiratie NU sunt in MinIO — sunt URL-uri CDN externe — deci migrarea e doar pentru uploads-urile demo, care oricum se sterg).
6. **Planul Railway**: ramanem pe Free/Hobby (1 container, limita 5 servicii, fara backup Postgres automat, sleep/limite de ore)? Lansarea publica cere cel putin Hobby/Pro + backups + replica separata pentru frontend/backend (ar permite si scalare + restart independent).
7. **Mailpit si MinIO console**: dupa SMTP real, Mailpit se sterge din Railway (elibereaza 1 din 5 sloturi — poate fi folosit pentru un worker separat)?
8. **Monitorizare**: acceptam Sentry (date personale in evenimente → DPA cu Sentry) sau doar uptime monitor + loguri Railway?
9. **Google Maps Places**: ramane activ la lansare (necesita cheie cu billing + restrictie domeniu + mentiune GDPR) sau lansam cu inputuri text (degradarea exista deja in cod)?
10. **Politica de retentie**: cat tinem cererile/chat-urile/fisierele dupa finalizare/anonimizare (exista `deleted_at`/`anonymized_at` in schema dar niciun job care sa le aplice)? Necesar pentru textul din Politica de confidentialitate.

## CHECKLIST DE LANSARE (pasi operationali in ordine)

1. **Commit + CHANGELOG** pentru tot ce e necomis (copy RO/EN, review-step, fisa cererii, `next.config.mjs` socket fix, launch.json) → `pnpm typecheck && pnpm lint && pnpm -F backend test && pnpm -F @marketplace/shared test`.
2. **Decizii PO** (intrebarile 1-6) — domeniu, SMTP, entitate juridica, storage, plan Railway.
3. **Backup baza de date** ACUM (`railway run --service Postgres pg_dump` sau backup din dashboard) — inainte de orice stergere/migrare.
4. **Curatare demo pe prod**: script `cleanup-demo.ts` (sau DB reset + `seed.ts` + `seed-inspiration.ts`); sterge `mock_billing_orders` demo; verifica `SELECT count(*) FROM users WHERE email LIKE '%@demo.ro'` = 0; goleste obiectele demo din bucket-ul `uploads`.
5. **Admin → Settings**: `seller_*` reale, `invoice_series` reala; creeaza contul ADMIN real (parola puternica, email real) si sterge `admin@demo.ro`.
6. **SMTP real**: variabile `SMTP_HOST/PORT/USER/PASS/SMTP_SECURE/MAIL_FROM` pe serviciul `backend`; DNS SPF + DKIM + DMARC pe domeniul de expediere; test: register → email primit in inbox real (nu spam) → verify → login. Apoi sterge serviciul Mailpit.
7. **Domeniu custom** in Railway (CNAME) → `FRONTEND_ORIGIN=https://<domeniu>`; redeploy; verifica cookie `Secure`, CORS, linkurile din email; restrictioneaza cheia Google Maps pe domeniu.
8. **Storage**: roteste cheile MinIO / sau migreaza pe R2/B2 (`S3_ENDPOINT/KEY/SECRET/BUCKET`, CORS PUT/GET de pe domeniu, bucket privat) → test upload schita + download presigned + snapshot 3D.
9. **Secrete**: regenereaza `JWT_ACCESS_SECRET`, `JWT_REFRESH_SECRET`, `PAYMENT_WEBHOOK_SECRET`, `IP_HASH_SALT` daca au fost vreodata in chat/loguri locale; confirma `MESSAGE_ENCRYPTION_KEY` setat si pastrat si intr-un seif (pierderea = chat ilizibil); `NOMINATIM_USER_AGENT` cu contact real.
10. **Hardening container** (P1-urile de mai sus): `trap`/tini in `start-combined.sh`, `USER node`, prune devDeps, `healthcheckPath=/api/v1/health` in Railway, `trust proxy` pe hop-uri.
11. **SEO/legal**: `robots.ts`, `sitemap.ts`, icon/apple-icon, `metadataBase` + OG; completeaza `[DENUMIRE LEGALĂ FIRMĂ]/[CUI]/[ADRESĂ]/[EMAIL CONTACT]` in Termeni/Confidentialitate (RO+EN) + mentiune Google Maps.
12. **Monitorizare**: uptime monitor extern pe `/api/v1/health` + pagina landing; Sentry (sau alert Railway pe crash/restart); `LOG_LEVEL=info`, exclude `/health` din access log.
13. **Backup automat** Postgres (plan Railway cu backups sau cron `pg_dump` → R2) + snapshot volum MinIO daca ramane; test de restore pe un DB temporar.
14. **CI**: GitHub Actions cu typecheck/lint/test pe PR; deploy doar din `main` verde.
15. **Smoke test e2e pe prod** (lista din docs/08 "Verificare post-deploy") + in plus: websocket conectat (`transport=websocket`), PDF oferta, flux firma: inregistrare → verificare admin → claim → oferta → acceptare; email-uri primite pentru fiecare pas.
16. **Dupa lansare**: actualizeaza `docs/08` (arhitectura finala, variabile reale, runbook restart/rollback = redeploy commit anterior + restore DB), sterge `update-demo-credits.ts`, `NEXT_PUBLIC_SOCKET_URL`, pagina `dev/piece-3d` din build-ul de prod.
