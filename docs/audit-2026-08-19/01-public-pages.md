# Aria: pagini publice + layout

Audit read-only, 2026-08-19. Fisiere citite integral: `app/[locale]/page.tsx`, `layout.tsx`, `landing-v2/`, `partners/`, `privacy/`, `terms/`, `sketch-guide/`, `unsubscribe/`, `inspiration/**` (3 pagini + 4 componente), 15 din cele 21 de fisiere din `_components/` citite integral (header/footer/shell/nav/lang/logo/hero/process/partners/legal/notification-bell/demo-state/hero-sheets/process-section), iar cele 6 componente de flux (chat-panel, claim-lifecycle-panel, client-*, offer-*) doar scanate prin grep (apartin mai degraba ariei cereri/oferte), `globals.css`, `middleware.ts`, `i18n/*`, `lib/mock-partner-meta.ts`, `lib/inspiration.ts`, `components/brand/logo.tsx`, `public/**`, `tailwind.config.ts`, `next.config.mjs`, plus namespace-urile `LandingV2/Landing/Nav/Partners/Legal/Inspiration/SketchGuidePage/Unsubscribe` din `ro.json`/`en.json`. Verificari suplimentare: `tsc --noUnusedLocals`, `next lint`, grep pe tot `src/`, randare in browser-pane la 375/660/768/1024 px (frontend dev server, backend oprit).

Toate caile de mai jos sunt relative la `apps/frontend/`.

## BINE FACUT (ce e solid si nu trebuie atins)

- Design system coerent: tokenii `globals.css` (ivorie/espresso/nuc/alama) + utilitare `.kicker/.label/.page-title/.serif` sunt folosite consecvent pe landing, parteneri, ghid, legale, inspiratie; acelasi `Button` (cva) peste tot, aceleasi carduri `rounded-xl border border-border bg-surface shadow-sm`.
- `prefers-reduced-motion` respectat la nivel global (`globals.css:336-361`) si local (hero-demo, process-band, partners-carousel, sketch-guide) cu stari finale explicite — nimic nu ramane la opacity 0 fara JS.
- Focus vizibil la tastatura (`globals.css:214`), `aria-pressed` pe chip-uri/optiuni, `aria-label` pe butoanele icon (carusel, lightbox, hamburger, clopotel), `aria-live` pe specificatia din hero-demo, `role="dialog" aria-modal` pe lightbox, `role="img" aria-label` pe SVG-urile informative.
- Toate imaginile au `alt` corect (landing: din i18n; inspiratie: `photo.title`; decorative: `alt=""`), `loading="lazy"` pe grile.
- Toate linkurile externe cu `target="_blank"` au `rel="noopener noreferrer"`/`noreferrer`; zero `dangerouslySetInnerHTML` in `src/`; niciun secret in client (doar `NEXT_PUBLIC_*` legitime).
- Toate rutele linkate din paginile publice exista (`/requests/new`, `/register?role=company` — parametrul e consumat in `(auth)/register/page.tsx:23`, `/login?redirect=...` validat cu `startsWith('/')`, `/inspiration/boards`, `/studio`, `/sketch-guide`, `/partners`, `/terms`, `/privacy`); `#proces` are tinta (`process-band.tsx:341`).
- `landing-v2` e doar un redirect catre `/` (`landing-v2/page.tsx:6-8`) — linkurile vechi raman functionale, fara continut duplicat indexabil.
- `dev/piece-3d` intoarce 404 in productie (`dev/piece-3d/page.tsx:17`).
- Middleware-ul de limba e simplu si corect (`middleware.ts`): RO implicit, EN doar la alegere explicita (cookie), `localeDetection: false` conform deciziei PO; `<html lang={locale}>` corect.
- Unsubscribe: fara login, semnat HMAC, one-shot, cu cele 3 stari (working/done/invalid) si buton inapoi.
- Termenii/Politica sunt scrise pe rolurile reale ale produsului (Client/Firma, credite, revendicare, contact vizibil doar firmelor care au preluat, chat criptat, ANSPDCP) — structura e buna, trebuie doar completate datele (vezi P0).
- Textele noi (LandingV2, Partners, Inspiration, SketchGuide) folosesc "firma", nu "atelier": 0 aparitii de `atelier`/`workshop` in valorile din `ro.json`/`en.json`.
- `public/` este curat: toate cele 11 ilustratii sunt referentiate (`components/configurator/illustrations/photo.tsx` + `_components/demo-state.ts`).
- `next lint` curat; `tsc --noUnusedLocals` gaseste o singura variabila nefolosita in toata aria (vezi DE STERS).

## DE MODIFICAT

### P0 — blocant lansare

- `[P0] src/messages/ro.json:4097,4134,4166` + `en.json:4097,4134,4166` — Termenii si Politica de confidentialitate contin placeholdere literale `[DENUMIRE LEGALĂ FIRMĂ]`, `[CUI]`, `[ADRESĂ]`, `[EMAIL CONTACT]` (apar de 4 ori in terms, 6 ori in privacy, inclusiv sectiunea "Contact"; in EN placeholderele sunt ramase in romana). → Completeaza datele juridice reale (denumire, CUI, sediu, email DPO/contact) in ambele limbi inainte de publicare; actualizeaza si `Legal.lastUpdated` (`ro.json:4091`).
- `[P0] src/app/[locale]/_components/partners-carousel.tsx:114-115,137,146` + `src/app/[locale]/partners/page.tsx:38-41` + `src/lib/mock-partner-meta.ts:26-28` — Ratinguri FICTIVE afisate public: backend-ul returneaza mereu `rating: null` (`apps/backend/src/modules/companies/companies.service.ts:106`), deci fallback-ul `mockPartnerMeta()` se aplica la 100% din firme si inventeaza rating 4.6–5.0, numar de recenzii 38–217 si o "specializare" derivate din hash-ul id-ului. Caruselul le eticheteaza drept "Rating Google Reviews" (`ro.json:137`), iar subtitlul sectiunii afirma "Ratingurile vin din recenziile Google" (`ro.json:135`, `en.json:135`). Pe `/partners` tooltip-ul spune "Rating Google in curand" (`ro.json:345`) in timp ce cifra e afisata — contradictie vizibila. → Pana la integrarea reala: ascunde steaua/recenziile/specializarea cand `rating === null` (foloseste `Partners.ratingPlaceholder` "—" care exista deja si e nefolosit), sterge fraza despre Google din `partnersSub`, sterge `mock-partner-meta.ts`. Afirmatii false despre recenzii pe o pagina publica = risc de publicitate inselatoare.
- `[P0] src/app/[locale]/_components/public-header.tsx:44-88` — Headerul public se sparge intre ~640 si ~900 px (tableta portret, ferestre inguste de desktop): 4 linkuri + LangSwitch + CTA + Autentificare + Inregistrare nu incap; verificat in browser la 660 si 768 px — linkurile se rup pe 2-3 randuri ("Ghid / de / schiță"), "Înregistrare" iese din ecran si TOATA pagina capata scroll orizontal (hero-ul e taiat la dreapta). → Muta linkurile de navigatie in hamburger pana la `md`/`lg` (`hidden lg:flex`), sau ascunde Autentificare/Inregistrare sub `md`, sau pune `whitespace-nowrap` + `overflow-x-auto` pe nav. `AppHeader` foloseste deja `md:` pentru nav (`app-header.tsx:66`) — aliniaza.

### P1 — important

- `[P1] src/app/[locale]/layout.tsx:31-35` — singurul `metadata` din toata aplicatia: titlu + descriere in romana, hardcodate, aplicate si pe `/en/*`; fara `metadataBase`, `openGraph`, `twitter`, `alternates.languages` (hreflang ro/en), fara `robots`. Nicio pagina nu are `generateMetadata` propriu (toate taburile se numesc "Cozy Home — Mobilier la comandă", inclusiv Termeni, Parteneri, Caietul de idei). → `generateMetadata` in layout pe baza de locale (titlu/descriere din i18n) + titluri per pagina publica + OG image + `alternates`.
- `[P1] src/app/ (lipsa)` — nu exista `not-found.tsx`, `error.tsx`, `global-error.tsx`, `loading.tsx` nicaieri in `src/app`. 404-ul e pagina default Next ("404: This page could not be found.", engleza, fara header/brand — verificat in browser pe `/ro/pagina-inexistenta`); orice exceptie de randare arata ecranul generic Next. → Adauga `app/[locale]/not-found.tsx` (in `PublicShell`, RO/EN, link acasa) + `error.tsx` cu buton "Incearca din nou".
- `[P1] src/app/ + public/ (lipsa)` — nu exista favicon/`icon.*`/`apple-icon`/`manifest` (serverul raspunde `GET /favicon.ico 404` — vazut in logurile dev), nici `robots.txt`/`robots.ts`, nici `sitemap.ts`. → Adauga `app/icon.svg` (exista deja marca `CozyHomeMark` in `components/brand/logo.tsx`), `app/robots.ts` (allow `/`, disallow `/admin`, `/dashboard`, `/marketplace`, `/requests`, `/dev`, `/unsubscribe`) si `app/sitemap.ts` cu paginile publice x 2 limbi.
- `[P1] src/messages/ro.json:4146,4158` (`Legal.privacy.sections.marketing`, `retention`; la fel in `en.json`) — Politica promite lucruri pe care produsul nu le are: "iti poti retrage consimtamantul... din setarile contului" (nu exista nicio setare de marketing; singurul toggle e pentru emailurile de notificare, in clopotel) si "La stergerea contului, datele... sunt sterse" (nu exista flux de stergere cont in frontend — 0 rezultate la grep `deleteAccount|Șterge contul`). → Fie rescrie (retragere prin email / link de dezabonare; stergere la cerere prin email), fie adauga functionalitatile. (Vezi si INTREBARI.)
- `[P1] src/lib/inspiration.ts:16,25-32` — cele 8 imagini din "Caietul de idei" de pe landing sunt hotlink-uite de pe CDN-ul Webflow al unui tert (`cdn.prod.website-files.com/60c4bb3c...` = site-ul Mobila Unicat). Daca partenerul isi schimba site-ul/CDN-ul, landing-ul ramane cu 8 imagini sparte; in plus, fiecare vizitator face request-uri catre un domeniu tert (privacy). → Copiaza imaginile in `public/` sau in MinIO/S3-ul propriu si serveste-le de pe domeniul tau (acordul PO pentru portofoliu exista deja conform memoriei; confirma ca acopera si gazduirea).
- `[P1] src/app/[locale]/partners/page.tsx:24-29` — pagina Parteneri nu are stare de eroare: daca API-ul pica, ramane doar titlul + subtitlul (nici loading, nici mesaj). La fel `inspiration/page.tsx:411-414` — la eroare de API afiseaza mesajul de "nicio idee" (`Inspiration.empty`), derutant. `inspiration/boards/page.tsx` nu are nici loading, nici eroare pentru lista de colectii. → Adauga `isError` → mesaj "Nu am putut incarca... Reincearca" (exista deja `Alert` in `components/ui`).
- `[P1] src/app/[locale]/_components/app-header.tsx:42-46` — `ROLE_LABEL` hardcodat (`'Client' | 'Firmă' | 'Admin'`), netradus: pe `/en/dashboard` pastila rolului arata "Firmă". → muta in `Nav.role.*` din i18n.
- `[P1] src/app/globals.css:148,172` — `border: 2px solid hsl(var(--surface))`: variabila `--surface` NU exista (doar `--surface-2/--surface-3`; `surface` e alias tailwind pentru `--card`). Declaratia e invalida → butonul slider-elor (buget, 3D, Studio) nu are inelul alb "de hartie" descris in comentariu. → `hsl(var(--card))`.

### P2 — polish

- `[P2] src/app/globals.css:65-108` + `tailwind.config.ts:5` — paleta `.dark` completa (43 de variabile) dar nimic nu pune vreodata clasa `dark` pe `<html>` (0 rezultate la grep `classList|'dark'|prefers-color-scheme` in `src/`). Dark mode e cod mort sau feature neterminat. → Decide: sterge blocul `.dark` sau adauga un toggle/`media` (vezi INTREBARI).
- `[P2] src/app/[locale]/_components/public-header.tsx:56` + `app-header.tsx:68` + `mobile-nav.tsx:57` — starea activa se calculeaza cu `pathname.endsWith(l.href)`: pe `/ro/inspiration/boards` linkul "Caietul de idei" nu e activ; pe `/ro/marketplace/claims/abc` niciun link nu e activ. → `pathname === l.href || pathname.startsWith(l.href + '/')` (dupa scoaterea prefixului de limba; `usePathname` din `@/i18n/routing` il scoate deja).
- `[P2] src/app/[locale]/_components/public-header.tsx:43-44` vs `app-header.tsx:59-60` — cele doua headere au inaltimi diferite (`py-3` vs `py-2.5`) si breakpoint-uri diferite pentru nav (`sm:` vs `md:`): la trecerea din landing in cont headerul "sare" cu ~4px. → unifica intr-un singur `SiteHeader` cu props (sau macar aceleasi padding/breakpoint).
- `[P2] src/app/[locale]/page.tsx:23-31` + `process-band.tsx:329-337,363-369` — motivul "hairline + romb de alama" e desenat de 3 ori inline (`SectionRule` local in page.tsx + doua variante in ProcessBand). → extrage `SectionRule` in `_components/` cu variante.
- `[P2] src/app/[locale]/page.tsx:212-238` vs `sketch-guide/page.tsx:228-248` — banda CTA "espresso cu lumina de alama" e copiata aproape identic (acelasi gradient radial, aceleasi clase). → componenta `CtaBand`.
- `[P2] src/app/[locale]/page.tsx:21`, `inspiration/page.tsx:29`, `inspiration/boards/[id]/page.tsx:21` — `PIN_ASPECTS` definit identic in 3 fisiere. → o constanta in `lib/inspiration.ts`.
- `[P2] src/app/[locale]/inspiration/page.tsx:411` si `inspiration/boards/[id]/page.tsx:129` — starea de incarcare e un "…" text; `boards/page.tsx` nu are deloc. → skeleton (cum are deja caruselul de parteneri, `partners-carousel.tsx:105-112`) sau `Loader2`.
- `[P2] src/app/[locale]/inspiration/boards/[id]/page.tsx:205` — stergerea colectiei foloseste `window.confirm` nativ (stil de sistem, rupe limbajul vizual). → `Dialog` din `components/ui/dialog.tsx`.
- `[P2] src/app/[locale]/_components/site-footer.tsx:13` — anul "© 2026" e hardcodat. → `new Date().getFullYear()`. Footerul nu are niciun link de contact/email (va fi necesar oricum dupa completarea datelor legale) si nici link catre Parteneri/Caiet — C2 a cerut "minimal", dar macar un email de contact ar trebui sa existe (vezi INTREBARI).
- `[P2] src/app/globals.css:45` — `--muted-2` (hsl 38 11% 62% ≈ #A89E94) pe alb are contrast ≈ 2.6:1 si e folosit pentru text la 10–12px (`partners/page.tsx:48` "Partener din...", `partners-carousel.tsx:136` numarul de recenzii, `hero-demo.tsx:400` kicker-ul plansei, lightbox). Sub AA (4.5:1) pentru text. → pastreaza `muted-2` doar pentru iconite/decor; textul mic pe `muted-foreground`.
- `[P2] src/app/[locale]/sketch-guide/page.tsx:35-163` vs `ro.json` `SketchGuidePage.steps.draw.title` ("Deseneaza planul vazut de sus... ca in exemplul alaturat") — plansa-exemplu este o perspectiva a peretelui (punct de fuga, comentariul din cod), nu un plan vazut de sus; textul trimite la "exemplul alaturat" care nu arata ce descrie. → fie schimba textul ("Deseneaza peretele din fata, ca in exemplu"), fie adauga o mica vigneta de plan.
- `[P2] src/app/[locale]/_components/lang-switch.tsx:26` — cookie-ul `NEXT_LOCALE` e setat fara `Secure`. → adauga `; secure` cand `location.protocol === 'https:'`.
- `[P2] next.config.mjs` — nu exista `headers()` cu `X-Frame-Options`/`X-Content-Type-Options`/`Referrer-Policy`/`Permissions-Policy` (si niciun CSP). → macar `X-Frame-Options: DENY` + `nosniff` + `Referrer-Policy: strict-origin-when-cross-origin` (CSP poate veni mai tarziu; atentie la Google Maps si la CDN-ul de imagini).
- `[P2] src/app/[locale]/_components/notification-bell.tsx:105` — panoul are `w-[22rem]` (352px) absolut la dreapta; pe ecrane sub ~360px iese din viewport in stanga. → `w-[min(22rem,calc(100vw-2rem))]`.
- `[P2] src/app/[locale]/_components/chat-panel.tsx:96` + `offer-card.tsx:149` — `href={a.downloadUrl ?? '#'}`: atasament fara URL devine link catre `#` cu `target=_blank` (deschide tab gol). → randeaza `<span>` cand nu exista URL.
- `[P2] src/messages/ro.json:2 / :82` — namespace-ul LIVE al landingului se numeste `LandingV2`, iar `Landing` (cel "v1") mai e folosit doar pentru footer + carusel parteneri. Confuz pentru cine editeaza copy. → redenumeste `LandingV2` → `Landing` si muta cele ~12 chei vii din vechiul `Landing` (vezi DE STERS).
- `[P2] src/app/[locale]/_components/public-header.tsx:21` + `page.tsx:166,207` + `partners-carousel.tsx:12` + `hero-demo.tsx:392` + `notification-bell.tsx:21` + `process-section.tsx:40,47,49` — comentarii de cod care folosesc inca "atelier"/"atelierele" (nu e copy vizibil, dar contrazice regula PO la urmatoarea rescriere). → inlocuieste cu "firma" cand se ating fisierele.

## DE STERS

- `src/app/[locale]/_components/hero-sheets.tsx` (211 linii) — 0 importuri in `src/` (grep `hero-sheets'`); landing v1, inlocuit de `hero-demo.tsx`. Comentariul din `page.tsx:17-18` ("raman in _components pentru refolosire") nu mai e justificat de niciun consumator.
- `src/app/[locale]/_components/process-section.tsx` (186 linii) — 0 importuri in `src/`; inlocuit de `process-band.tsx`. Contine si vigneta "trei ateliere" (`:40,47`).
- `src/lib/mock-partner-meta.ts` (30 linii) — de sters odata cu fix-ul P0 (singurii consumatori: `partners-carousel.tsx:8`, `partners/page.tsx:6`).
- `src/lib/inspiration.ts:6-14` — `FurnitureType` + `FURNITURE_TYPES` exportate, 0 utilizari in afara fisierului (grep `FURNITURE_TYPES|FurnitureType`).
- `src/app/[locale]/inspiration/boards/[id]/page.tsx:110` — `const tc = useTranslations('Configurator')` in `BoardDetailPage` e nefolosit (singurul TS6133 din aria, confirmat cu `tsc --noUnusedLocals`; `tc` din `BoardPin` de la linia 36 E folosit).
- `src/messages/ro.json:82-131` + `en.json:82-131` (namespace `Landing`) — ~45 de chei moarte, folosite doar de componentele moarte de mai sus sau de nimic: `title, subtitle, heroNote, kicker, ctaNewRequest, ctaCompanies, sizingKicker, sizingTitle, sizingSub, sizeSmall/Medium/Large(+Desc), creditsUnit(One), howTitle, step1-3Phase/Title/Body, heroSheetTitle, heroSheetClient, heroSheetsSwitcher, heroSheets.*, ctaBandTitle/Body/Button, inspirationKicker/Title/Sub/Cta` — inclusiv metricele INVENTATE `metricWorkshops: "340+"`, `metricProjects: "2.8k"`, `metricSatisfaction: "94%"` (`ro.json:91-96`; azi nu se afiseaza nicaieri, dar sunt o mina daca cineva le reactiveaza). Chei vii de pastrat din `Landing`: `footerTagline, partnersKicker, partnersTitle, partnersSub, partnersAll, googleReviews, reviewsShort, carouselPrev, carouselNext, partnerSpecialty.*` (ultimele 3 dispar si ele daca se scoate mock-ul).
- `src/messages/*.json` — chei orfane punctuale (0 referinte in `src/`): `LandingV2.previewBadge` (`:3`), `Nav.home` (`:159`), `Nav.forCompanies` (`:176`), `Inspiration.openPinterest` (`:354`), `Partners.ratingPlaceholder` (`:344` — SAU foloseste-l la fix-ul P0).
- `src/app/globals.css:65-108` — blocul `.dark` (43 de variabile) daca PO decide ca nu exista dark mode la lansare (vezi P2 + INTREBARI).

## INTREBARI PENTRU PO

1. Datele juridice pentru Termeni/Politica: denumirea legala a operatorului, CUI, sediul, emailul de contact/DPO. Se lanseaza sub brandul "Cozy Home" (doua cuvinte, cum e in logo/`Nav.home`/metadata) sau "CozyHome" (un cuvant, cum apare in textele legale `Legal.terms.intro.h/b`, `ip.b`)? Trebuie unificat.
2. Ratingurile partenerilor: acceptam sa NU afisam nicio stea pana la integrarea Google Reviews (recomandat), sau vrem un alt semnal real (ex. "X proiecte finalizate pe platforma", "membru din...")? Specializarea firmei ("Bucatarii la comanda...") — o adaugam ca camp real in profilul firmei sau o scoatem?
3. Politica de confidentialitate promite "setari de marketing in cont" si "stergerea contului". Le construim inainte de lansare sau rescriem textul (retragere consimtamant prin email/link; stergere la cerere prin email)?
4. Imaginile din "Caietul de idei" de pe landing: avem acordul Mobila Unicat si pentru a le gazdui pe serverul nostru (nu doar hotlink)? Daca da, le mutam in storage-ul propriu.
5. Footer: dorim sa ramana "minimal" (doar Termeni + Confidentialitate) sau adaugam email de contact, link Parteneri/Caiet/Ghid si — daca platforma vinde credite/abonamente online catre firme din RO — linkurile ANPC/SOL cerute de obicei pentru comert online? (Nu pot decide daca obligatia se aplica unui model B2B cu credite.)
6. Dark mode: exista paleta completa in CSS, dar niciun comutator. Il livram (toggle in header + `prefers-color-scheme`) sau stergem paleta ca sa nu ramana cod mort?
7. Pe `/en/*` titlul/descrierea din tab raman in romana (un singur `metadata` static). Vrem SEO/meta si pentru engleza, sau EN ramane "best effort" si indexam doar RO (`hreflang` + `robots` noindex pe `/en`)?
8. Ghidul de schita: textul pasului II cere "plan vazut de sus", plansa animata arata peretele in perspectiva. Schimbam textul sau desenul?
