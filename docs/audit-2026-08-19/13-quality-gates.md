# Aria: porti de calitate + igiena repo + docs

Audit read-only, 2026-08-19, repo `F:/Cozy Fable` @ `afa58e0` (main, sincron cu origin/main). Loguri brute: `01-shared-build.log`, `02-typecheck.log`, `03-lint.log`, `04-backend-test.log`, `04b-shared-test.log`, `05-outdated.log`, `06-audit.log`, `07-hygiene.log`, `08-git.log` (acelasi director).

## REZULTATE COMANDE (exact)

| Poarta | Comanda | Rezultat |
|---|---|---|
| Build shared | `pnpm -F @marketplace/shared build` | EXIT 0 (tsc, fara erori) |
| Typecheck | `pnpm typecheck` (shared + backend + frontend) | EXIT 0, **0 erori TS** in toate cele 3 workspace-uri |
| Lint | `pnpm lint` | EXIT 0 — **ruleaza DOAR pe frontend** (`next lint`, `next/core-web-vitals`): "No ESLint warnings or errors". Backend si shared NU au script `lint` si nici config ESLint → `pnpm -r --if-present lint` le sare tacit. |
| Teste backend | `pnpm -F backend test` (jest, ts-jest, fara DB/Redis) | EXIT 0 — **6 suite / 39 teste PASS** in 36s (memoria zicea 32; au fost adaugate 7 pentru message-crypto). Zgomot in output: 2 linii ERROR + 2 WARN de la Logger-ul Nest in testele negative (asteptate). |
| Teste shared | `pnpm -F @marketplace/shared test` (vitest) — rulat suplimentar, nu e in root | EXIT 0 — **3 fisiere / 113 teste PASS** in 5s |
| Root `pnpm test` | — | **NU EXISTA** script `test` in root `package.json`; nu exista CI (`.github/workflows` lipseste), nu exista husky/lint-staged/prettier. |
| `pnpm outdated -r` | ~15s | 63 pachete in urma; **majore in urma**: next 14.2.35→16.3.1, react/react-dom 18.3.1→19.2.8, @nestjs/* 10.4.22→11.2.1, @nestjs/config 3→4, @nestjs/bullmq 10→11, prisma/@prisma/client 5.22.0→7.9.1, zod 3.25.76→4.4.3, three 0.169→0.185, @react-three/fiber 8→9, drei 9→10, next-intl 3.26→4.13, tailwindcss 3→4, typescript 5.9→7.0, bullmq 5→6, ioredis 5→6, nodemailer 6.10→9.0, helmet 7→8, zustand 4→5, framer-motion 12→13, eslint 8→10, jest 29→30, vitest 2→4, uuid 10→14, otplib 12→13, cookie 1→2, pino-http 10→11. Minore/patch: radix, tanstack, aws-sdk, puppeteer, rhf, lucide etc. |
| `pnpm audit --prod` | ~10s | **55 vulnerabilitati: 0 critical, 22 high, 29 moderate, 4 low.** High pe pachet: **next@14.2.35** (8 high — DoS x4, SSRF x3, middleware bypass; patch DOAR in >=15.0.8 / >=15.5.21 → nu exista fix pe linia 14), **multer** (4 high, tranzitiv prin @nestjs/platform-express 10; patch >=2.2.0), **nodemailer@6.10.1** (2 high — DoS addressparser, raw bypass; patch >=7.0.11 / >=9.0.1), **postcss@8.4.31** tranzitiv prin next (2 high — file read, path traversal), **nanoid** tranzitiv prin next>postcss (2 high), **lodash@4.17.21** via @nestjs/config (1 high, code injection `_.template`; patch >=4.18), **ws** tranzitiv (1 high, patch >=8.21), **socket.io-parser** (1 high, patch >=4.2.7). Moderate notabile: next-intl (open redirect + prototype pollution), @nestjs/core (special elements), uuid, qs, file-type, 5x nodemailer, lodash PP. |

Mediu: pnpm 9.12.3 (= `packageManager`), node v21.6.0 local (engines `>=20`; Docker = node:20).

## BINE FACUT

- Typecheck verde cu `strict: true` in toate cele 3 tsconfig-uri (backend relaxeaza doar `strictPropertyInitialization: false`, normal pentru Nest DI). `skipLibCheck: true` peste tot (acceptabil). `noUnusedLocals`/`noUnusedParameters` NU sunt activate nicaieri (constatare).
- Lint frontend verde; doar 42 de `eslint-disable` (22 `@next/next/no-img-element` — galerie/pin-uri cu URL-uri externe unde `<img>` e intentionat, 9 `react-hooks/exhaustive-deps`, 11 `no-explicit-any` din care 8 in spec-uri backend pe mock-uri).
- Igiena exceptionala: **0 `console.log`**, **0 `console.error/warn`** in FE, **0 `debugger`**, **0 `@ts-ignore`/`@ts-expect-error`**, **0 TODO/FIXME/XXX** (singurul `HACK` e o valoare de test in `engine.test.ts:160`), **0 `: any`** in cod de productie (`any` apare in 10 linii backend — toate spec-uri/mock-uri; 1 in FE `lang-switch.tsx:28` `pathname as any` justificat de tipurile next-intl; `{ any: Condition[] }` din shared e un nume de camp, nu tipul).
- Niciun bloc de cod comentat: cele 7 blocuri de comentarii >10 linii sunt toate documentatie de decizii (message-crypto, geo, public-header, flows kitchen/bedroom/dressing v2, sketch-guide) — nu cod mort.
- Comentariile respecta conventia: ~98-99% romana (661 linii comentarii backend / 1361 FE / 925 shared; doar 6-12 linii per pachet par englezesti si sunt de fapt termeni tehnici); 26 de linii au diacritice (minor).
- Secrete: niciun `.env`/`.pem`/`.key` urmarit (doar `.env.example` x2); `apps/backend/.env` si `apps/frontend/.env.local` ignorate corect (`.gitignore:5/6`); `.env.example` backend e aliniat cu `config.schema.ts` (toate cheile obligatorii prezente); secretele au min-length validat la boot (fail-fast).
- Repo mic (.git 15MB, 504 fisiere urmarite, cel mai mare blob 398KB pnpm-lock) — nimic mare/binar ratacit in istoric.
- Git: main = origin/main (0 ahead/behind), 29 migrari Prisma, commit-uri descriptive in romana.
- Pagina `/dev/piece-3d` e protejata cu `notFound()` in productie.
- CHANGELOG foarte detaliat per sprint, cu checklist-uri de acceptare; docs 09/10/12 au bannere STATUS/UPDATE care spun clar ce e livrat.
- Teste shared: 113 teste pe motorul de chestionar + piece3d (logica de validare a cererii e acoperita bine).

## DE MODIFICAT — `[P0|P1|P2] cale:linie — problema → recomandare`

- **[P0] apps/frontend/package.json:26 (`next` ^14.2.5 → instalat 14.2.35)** — 8 vulnerabilitati HIGH (DoS Server Components x3, SSRF in Server Actions/rewrites x3, middleware bypass, deserializare) + ~12 moderate, toate cu patch DOAR pe 15.x. Linia 14 e end-of-life pentru aceste CVE-uri. → planifica upgrade Next 15 (React 18 ramane compatibil cu 15.x; atentie la `next-intl` 3→4 si `eslint-config-next`). Rewrite-urile `/api/v1` si `/socket.io` din `next.config.mjs` sunt exact suprafata SSRF-in-rewrites mentionata de advisory — de verificat ca `BACKEND_INTERNAL_URL` nu e influentabil de request.
- **[P0] apps/backend/package.json:33 (`nodemailer` ^6.9.14 → 6.10.1)** — 2 high + 5 moderate (SMTP command injection, CRLF, TLS validation, domeniu neintentionat). → bump la `^7.0.11` (API compatibil) sau 9.x; ruleaza `notification-emails.service.spec.ts` dupa.
- **[P1] apps/backend/package.json:15,19 (`@nestjs/config` 3.3.0 → lodash 4.17.21; `@nestjs/platform-express` 10 → multer <2.2)** — lodash `_.template` code injection HIGH (nu folositi `_.template`, deci exploatabilitate practica scazuta) si multer DoS HIGH x4 (backend-ul foloseste presigned URLs, nu multer pentru upload — tot scazuta). → `pnpm.overrides` pe `lodash >=4.18`, `multer >=2.2.0`, `ws >=8.21`, `socket.io-parser >=4.2.7` in root package.json ca mitigare rapida, pana la Nest 11.
- **[P1] package.json (root) — lipsa `test` si lipsa CI** — `pnpm lint` sare tacit backend+shared, nu exista `pnpm test` la radacina, nu exista workflow care sa ruleze build shared → typecheck → lint → jest → vitest la PR/push. Portile trec azi, dar nimic nu le impune. → adauga `"test": "pnpm -r --if-present test"` + un workflow GitHub Actions minimal (sau macar un script `verify` in root).
- **[P1] apps/backend (fara ESLint) + packages/shared (fara ESLint)** — singurul lint e frontend. → config ESLint minimal (`@typescript-eslint/recommended`) + script `lint` in ambele; costul e mic pentru ca typecheck-ul e deja verde.
- **[P1] Acoperire teste backend pe logica critica (doar enumerare, conform cerintei):**
  - Testat: `claims.helpers.spec.ts` (haversine, recompute status dupa claim, republish dupa mass breach 4.11), `credits.service.spec.ts` (reserve/refund/consume pay-to-play, 402), `uploads.service.spec.ts` (presign limita, scan mock, relink/ownership), `business-calendar.service.spec.ts` (zile lucratoare, sarbatori, override admin, 23:59 Bucuresti), `notification-emails.service.spec.ts` (semnatura unsubscribe HMAC, destinatari per eveniment, deep-link), `message-crypto.service.spec.ts` (AES-GCM roundtrip, dual-read, chei gresite).
  - NETESTAT din logica critica: **claim concurrency** (`claims.service.ts:72-206` — Serializable + `FOR UPDATE`, timeout 10s, backstop index unic la :415; nu exista test nici macar pe mock-ul tranzactiei), **idempotency** (`common/idempotency/idempotency.interceptor.ts` — replay/acelasi body/hash diferit), **SLA** (`claims/sla-breach.processor.ts`, `quote-validity.processor.ts`, `consultation-expiry.processor.ts`, `request-expiration.processor.ts`, `withdrawal-reminder.processor.ts` — niciun worker BullMQ testat), **penalizari** (`penalties/penalties.service.ts` 125 linii), **payments webhook + semnatura** (`billing/payments.service.ts` 200 linii), **subscriptions guard** (`subscription-active.guard.ts`, `company-approved.guard.ts`), **auth/token rotation** (`auth/token.service.ts`, refresh reuse detection), **quotes lifecycle** (`quotes.service.ts` 1140 linii — accept/supersede/versiuni), **requests publish/scoring** (`requests.service.ts` 1006 linii, `configurator.service.ts`), **withdrawals** (`withdrawals.service.ts` 300 linii), **studio drafts** (limite 20/300KB). Frontend: 0 teste (nici un `*.test.tsx`).
- **[P1] CHANGELOG.md — nu e la zi**: ultima intrare e "Studio 3D r6 — 2026-08-03". Lipsesc commit-urile `a319dae` (slidere identice cross-browser), `83ea274` (r5: pozitionare la cm exact + hint camera goala), `fefa53a` (goluri pe pereti ascunsi neselectabile), `afa58e0` (pastila scurtaturi scoasa) si TOATA munca necomisa din working tree (rescrierea copy-ului "firma, nu atelier" ~2.170 stringuri RO+EN, `review-rail.tsx` nou, redesign `requests/[id]/page.tsx` + `review-step.tsx`, fix rewrites socket.io in `next.config.mjs`). → o intrare "Studio 3D r7 + terminologie firma" inainte de commit.
- **[P1] Working tree necomis (9 fisiere, +2.460/-2.331 linii)** — `next.config.mjs` (fix REAL de productie: `skipTrailingSlashRedirect` + rewrite `/socket.io/` separat, altfel WebSocket-ul moare in prod), `app-header.tsx` (Atelier→Firmă), `layout.tsx` (meta description), `requests/[id]/page.tsx` (162 linii), `review-step.tsx` (265) + `review-rail.tsx` (95, nou), `en.json`/`ro.json` (rescriere completa), `.claude/launch.json`. Fixul de socket.io e amestecat cu rescrierea de copy intr-un singur diff. → commit separat pentru `next.config.mjs` (bugfix deploy) vs. copy/UI; apoi deploy — memoria spune ca ultimul deploy pe prod e `4b585b1a` de pe 2026-08-03, deci produl ruleaza fara fixul de socket.io si cu terminologia veche.
- **[P1] Sursa de adevar (docs/03, 04, 07) NU a fost actualizata cu deciziile din sprinturile de feedback** — CLAUDE.md spune ca 02/03/04/07 sunt "sursa de adevar" si ca 07 e FINAL, dar: `docs/03-business-rules.md:91` inca spune "cost = claim_cost_credits per marimea proiectului (seed 1/2/4)", in timp ce regula in vigoare (docs/15 V1, 2026-07-13, LIVRATA si pe prod) e **1 credit = 1.000 lei din bugetul minim estimat**; `docs/04-data-model.md` nu contine tabelele `studio_drafts` si `request_studio_scenes` (migrari 2026-08-02) — ultima entitate adaugata e `chat_thread_reads` (2026-07-12); `docs/07-decisions-log.md` are ultima decizie datata 2026-07-12 — lipsesc decizia "criptare at-rest, NU e2e" (docs/16), regula creditelor (docs/15), 200 credite demo, `ownProject`/`designBrief` (Y4), studio. Un agent nou care citeste DOAR docs/03 (cum cere CLAUDE.md) va implementa regula veche. → un pass de sincronizare 03/04/07 cu docs/15-16 + CHANGELOG (30 min), apoi arhivarea worksheet-urilor devine sigura.
- **[P1] README-SETUP.md — complet stale** (17 linii): e nota "starter pack" pentru Sprint 0 (iunie), nu un ghid de setup. Nu contine comenzile de infra (`docker compose -f dev-infra/docker-compose.yml up -d`), porturile (3000 FE / 3001 BE / 5432 / 6379 / 9000-9001 MinIO / 1025-8025 Mailpit), pasii `.env`, `prisma migrate`, seed, nici `build:shared` obligatoriu inainte de typecheck FE. Referinta la `docs/PRODUCT_BIBLE_MASTER_V6.docx` e moarta (fisierul nu exista; docx-urile sunt in `Design/uploads/`). → rescrie ca README real (Quick start local + Deploy → docs/08) sau sterge si pune setup-ul in CLAUDE.md/docs/08.
- **[P2] apps/backend/.env.example — lipsesc 3 chei optionale din `config.schema.ts`**: `MESSAGE_ENCRYPTION_KEY` (fara ea mesajele se stocheaza NECRIPTAT — warn la boot; pe prod e setata conform memoriei), `NOMINATIM_BASE_URL`, `NOMINATIM_USER_AGENT`. → adauga-le comentate, ca sa nu fie uitate la un mediu nou (staging).
- **[P2] apps/frontend/.env.example — lipseste `BACKEND_INTERNAL_URL`** (folosit in `next.config.mjs`, setat doar in Dockerfile). → o linie comentata "doar la deploy".
- **[P2] .claude/settings.local.json — URMARIT in git** (fisier `*.local` = per-masina; contine allowlist de permisiuni cu cai Windows absolute, un email de test si parola de test `parola1234` in comenzi curl). Nu e secret real, dar nu are ce cauta in repo. → `git rm --cached` + `.gitignore`.
- **[P2] `.claude/worktrees/` e ignorat doar prin `.git/info/exclude`** (local), nu prin `.gitignore` → pe alt clone poate ajunge urmarit. → adauga `.claude/worktrees/` in `.gitignore`.
- **[P2] Fisiere >800 linii (candidati refactor, doar lista)**: `apps/frontend/src/components/studio/studio-page.tsx` 1844, `components/configurator/dimension-figures.tsx` 1145, `apps/backend/src/modules/quotes/quotes.service.ts` 1140, `components/configurator/piece3d/configurator3d-step.tsx` 1107, `packages/shared/src/questionnaire/engine.test.ts` 1037, `apps/backend/src/modules/requests/requests.service.ts` 1006, `components/studio/room-canvas.tsx` 1004, `packages/shared/src/questionnaire/piece3d/config.ts` 928, `apps/frontend/src/stores/studio-store.ts` 919.
- **[P2] `react-hooks/exhaustive-deps` dezactivat in 9 locuri** (verify-email, inspiration-lightbox, save-button, chat-panel, address-autocomplete, configurator-wizard:271, details-step:136/152, room-canvas:800) → fiecare e un posibil stale-closure; de revazut la refactor, nu blocant.
- **[P2] tsconfig — `noUnusedLocals`/`noUnusedParameters` off** peste tot → codul mort nu e detectat de tsc si nici de ESLint (backend/shared nu au lint). Activarea probabil scoate cateva zeci de erori; de facut odata cu ESLint-ul pe backend.
- **[P2] Dependinte majore in urma** (fara CVE direct, dar cost de upgrade creste): NestJS 10→11, Prisma 5→7 (breaking: config file, driver adapters), zod 3→4 (shared e sursa unica FE/BE — upgrade coordonat), three 0.169→0.185 + R3F 8→9 + drei 9→10 (studio 3D e cea mai mare suprafata), tailwind 3→4, next-intl 3→4, zustand 4→5. → nu inainte de lansare; planifica un sprint de "dependency refresh" post-lansare, incepand cu Next 15 + nodemailer (securitate).

## DE STERS — console.log/debugger/cod comentat/docs stale/fisiere urmarite gresit

- console.log / console.error / debugger / cod comentat: **NIMIC de sters** (0 gasite).
- Branch-uri/worktree moarte: `feat/configurator-atelier-and-sketch-guide` (local + `origin/`) — 0 commit-uri ahead, 34 behind main → sterge local si remote; `claude/unruffled-bardeen-4fff21` + worktree-ul `.claude/worktrees/unruffled-bardeen-4fff21` — 0 ahead, 54 behind, cu o modificare necomisa in `apps/frontend/src/hooks/use-fulfillment.ts` (verifica daca e ceva util, apoi `git worktree remove` + `git branch -D`).
- `.claude/settings.local.json` — scoate din tracking (vezi P2).
- `Design/uploads/*.docx` (5 fisiere, ~490KB, duplicate cu sufix hash: `Product_Bible_Master (1)-8c1a2301.docx` + `Product_Bible_Master (1).docx`, `Continuitate_Refinement_V4_V5-fee4ec19.docx` + `Continuitate_Refinement_V4_V5.docx`, `product_bible.docx`) si `Design/.thumbnail` — artefacte din tool-ul de design (iunie); `.claudeignore` deja exclude `*.docx`. → pastreaza un singur Product Bible (mutat in `docs/`) sau sterge tot `Design/` daca mockup-urile .jsx nu mai sunt referinta (design system-ul real e in cod).
- Docs stale / arhivabile (toate sunt worksheet-uri de feedback PO deja executate; continutul util e in CHANGELOG):
  - `docs/09-po-review-todo.md` — "PO a aprobat TOT", Q1-Q6 livrate; ramane util doar pentru D4 (post-MVP) si D6 (cheie GCP) → muta cele 2 iteme deschise in docs/07 si arhiveaza.
  - `docs/11-worksheet-intrebari-formular.md` — **3.712 linii / 165KB**, worksheet de completat de PO (347 campuri "Text nou"); raspunsurile au fost executate in S1-S6 (docs/12) → arhiveaza (`docs/archive/`) — e al 2-lea cel mai mare fisier text din repo si intra in contextul agentilor.
  - `docs/12`, `13`, `14`, `15`, `16` — sprinturi de feedback PO livrate (S, T, U, V, W); `docs/16` contine decizia de criptare (NU e2e) care merita mutata in `docs/07-decisions-log.md` daca nu e deja. → `docs/archive/` cu un index de 5 linii.
  - `docs/10-tylko-3d-configurator-sprints.md` — plan R1-R6 livrat; pastreaza doar daca e referinta pentru modelul parametric (altfel arhiveaza).
  - `docs/sprint-0-architecture.md` — probabil depasit de schema.prisma reala (291 linii); de verificat/arhivat.
  - `docs/06-seed-scenarios.md` — relevant pentru demo/prod-seed (memoria: seed-ul demo a rulat pe prod cu `railway run`). Contine "Parole demo identice: Demo1234!" iar `seed-demo.ts:32` are acelasi default. Daca produl a fost seed-uit cu parola default (nu cu `DEMO_PASSWORD`), conturile demo de pe prod sunt publice → de verificat (vezi INTREBARI). Pastreaza documentul, dar scoate parola din el.
  - `README-SETUP.md` — stale (vezi P1).
- CLAUDE.md (root): toate referintele (`docs/02,03,04,05,07`) exista → OK. `apps/backend/CLAUDE.md` si `apps/frontend/CLAUDE.md` exista si sunt valide (frontend-ul mentioneaza "STATE_CONVENTIONS.md (dupa Sprint 0)" — fisierul exista in `docs/`, calea e implicita).

## INTREBARI PENTRU PO

1. **Upgrade Next 14 → 15 inainte sau dupa lansare?** Singurul fix pentru cele 8 HIGH (DoS/SSRF) e 15.x; e un upgrade cu risc mediu (next-intl 4, eslint-config-next, posibile schimbari de caching). Recomandarea mea: imediat dupa lansare, intr-un sprint dedicat, cu nodemailer 7 si overrides pe lodash/multer/ws facute ACUM (risc zero).
2. **Conturile demo de pe prod** (admin@demo.ro, ana.popescu@demo.ro etc., parola `Demo1234!` documentata in docs/06 si default in seed-demo.ts): sunt inca active pe prod si cu ce parola? La lansare publica trebuie dezactivate sau cu parola din `DEMO_PASSWORD`/`DEMO_ADMIN_PASSWORD` (docs/08 spune ca se seteaza in Railway — de confirmat ca s-a facut).
3. **Arhivam docs/09-16 + Design/?** Sunt ~4.500 linii de worksheet-uri executate; propun `docs/archive/` + stergerea docx-urilor duplicate. Nimic nu se pierde (CHANGELOG are tot), dar vreau OK-ul pentru ca docs/07 spune ca deciziile sunt finale si nu vreau sa par ca le "ascund".
4. **CI minim (GitHub Actions: shared build → typecheck → lint → jest → vitest)** — e ok sa-l adaug? Azi portile sunt verzi doar pentru ca le rulam manual.
5. **Teste pe claim concurrency / idempotency / SLA workers** (neacoperite) — le vrei inainte de lansare (cu DB de test in Docker, ~1 sprint) sau acceptam riscul si le facem post-lansare? Memoria: D3 a aprobat doar unit-teste cu mock-uri, fara DB.
6. **Fixul socket.io din `next.config.mjs`** (necomis) — a fost validat pe prod ca realtime-ul era rupt? Daca da, e un P0 de deploy separat de rescrierea copy-ului.
