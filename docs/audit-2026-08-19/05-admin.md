# Aria: admin (FE+BE)

Scop: `apps/frontend/src/app/[locale]/admin/**`, `hooks/use-admin.ts`, `hooks/use-admin-inspiration.ts`, `apps/backend/src/modules/{admin,audit,inspiration}/**`, `apps/backend/src/common/settings/**`, `packages/shared/src/admin.schemas.ts` + controllerele `admin/*` din companies/fulfillment/billing/claims (citite integral). Verificari dinamice facute pe backend-ul local (localhost:3001, login admin@demo.ro / client ana.popescu@demo.ro), DOAR apeluri read-only sau cu id inexistent (nimic modificat).

## BINE FACUT
- Toate controllerele admin au `@Roles(UserRole.ADMIN)` la nivel de clasa si nicio metoda nu suprascrie cu alt rol: `admin.controller.ts:30`, `admin-companies.controller.ts:13`, `fulfillment.controller.ts:55`, `payments.controller.ts:81`, `lifecycle.controller.ts:87`, `inspiration.controller.ts:108`. `RolesGuard` + `JwtAuthGuard` sunt APP_GUARD globale (`auth.module.ts:26-28`). Verificat live: CLIENT primeste 403 pe toate cele 9 prefixe `/admin/*`, anonim 401. `RegisterDto` refuza rolul ADMIN (`register.dto.ts:25`).
- Audit garantat (scriere inainte de raspuns, `concatMap`) pe: setari, penalty rules, pachete, planuri, praguri, retry job, approve/reject firma, dispute, confirmare plata, retragere (`audit.interceptor.ts:40-55` + `@Audit` pe fiecare handler de scriere). IP hash-uit cu salt din env, fara fallback (`audit.service.ts:98`). `audit_logs` append-only (trigger DB, per comentariul din schema).
- `ValidationPipe({ whitelist, forbidNonWhitelisted, transform })` global (`main.ts:31`); DTO-urile admin au `@IsInt/@Min/@IsBoolean/@MaxLength`; `ParseUUIDPipe` pe id-urile din disputes/payments/withdrawals.
- Audit viewer paginat cu clamp 1..100 (`admin.service.ts:93-94`); jobs limitate la 50/coada (`admin.service.ts:212`); toate cele 7 cozi BullMQ sunt acoperite in `/admin/jobs` (verificat ca nu exista alte cozi inregistrate).
- Tranzitii de stare protejate la BE: dispute (409 `DISPUTE_ALREADY_DECIDED`), plata (409 `PAYMENT_ALREADY_CONFIRMED`), retragere (409 `WITHDRAWAL_ALREADY_DECIDED`); refund credite + recompute status cerere in aceeasi tranzactie la retragere aprobata (`withdrawals.service.ts:228-242`).
- Inspiratie admin: upload prin presign -> PUT -> confirm (invarianta 3.4), MIME whitelist + cap dimensiune (`inspiration.dto.ts:182-196`), soft delete (`inspiration.service.ts:326`), publicul vede doar `published && deletedAt null && cu imagine`.
- Parola admin demo suprascriibila din env (`seed-demo.ts:33`), documentat in `docs/08-deployment.md:10,52,86`; `seed.ts` NU suprascrie `system_settings` ajustate din admin (`seed.ts:201`).
- i18n: toate cele 6 namespace-uri Admin* au paritate RO/EN 100% (verificat programatic). Tabelele folosesc `Table` cu `overflow-x-auto` (`components/ui/table.tsx:7`).
- Termenul "atelier" NU apare in niciun text UI din zona admin (doar in 3 comentarii de cod).

## DE MODIFICAT

### Securitate / integritate
- `[P0] apps/frontend/src/hooks/use-admin.ts:64-65, 76-77, 88-89` — `useUpdatePlan/useUpdatePackage/useUpdateThreshold` trimit in body obiectul intreg `v` (inclusiv `id`); cu `forbidNonWhitelisted: true` backend-ul raspunde 400 `VALIDATION_ERROR "property id should not exist"` (reprodus live pe toate 3). Rezultat: Salveaza pe Planuri / Pachete / Praguri NU functioneaza niciodata, iar UI nu arata nicio eroare (`settings/page.tsx:120-131` SaveBtn fara onError) → adminul crede ca a salvat. → `const { id, ...body } = v` inainte de `JSON.stringify`; + feedback succes/eroare (toaster exista in `components/ui/toaster.tsx`).
- `[P1] apps/backend/src/modules/inspiration/inspiration.controller.ts:107-145` — `InspirationAdminController` are `@Audit(...)` pe create/update/delete/confirmImage dar NU are `@UseInterceptors(AuditInterceptor)` (nici pe clasa, nici pe metode; interceptorul nu e global — `grep APP_INTERCEPTOR` = 0). Decoratorii sunt inerti: scrierile din galeria admin nu ajung in audit_logs (live: 0 intrari INSPIRATION_* cu 162 poze in DB). → adauga `@UseInterceptors(AuditInterceptor)` pe clasa.
- `[P1] apps/backend/prisma/seed-demo.ts:27-58` — singura cale de a crea un ADMIN este `seed-demo.ts` (email hardcodat `admin@demo.ro`, impreuna cu toate datele demo; `seed.ts` nu creeaza admin; nu exista CLI/endpoint/`seed-admin`). Pe un prod "curat" (fara date demo) nu exista admin; daca `DEMO_ADMIN_PASSWORD` lipseste, parola cade pe `DEMO_PASSWORD`/`Demo1234!`. → script separat `prisma/seed-admin.ts` (ADMIN_EMAIL + ADMIN_PASSWORD obligatorii din env, fail daca lipsesc) si/sau comanda de promovare rol; de documentat in docs/08.
- `[P1] apps/frontend/src/app/[locale]/admin/layout.tsx` — layout-ul admin NU verifica rolul si NU exista middleware pe `/admin` (`middleware.ts` face doar i18n). Paginile `audit`, `jobs`, `settings`, `companies/[id]` nu au nici ele `useMe`/redirect (celelalte 6 pagini il duplica fiecare). Un client logat vede shell-ul consolei (sidebar, titluri; datele raman 403). → un singur guard in `layout.tsx` (redirect la /login sau /dashboard) si eliminarea celor 6 copii din pagini.
- `[P1] apps/frontend/src/app/[locale]/admin/layout.tsx:72-85` — nu exista buton de logout / meniu utilizator in consola admin (doar LangSwitch + clopotel); `useLogout` exista in `use-auth.ts:37`. → adauga logout in sidebar/header.
- `[P1] apps/frontend/src/app/[locale]/admin/{companies/[id]/page.tsx:181, payments/page.tsx:41, disputes/page.tsx:265-273, withdrawals/page.tsx:261-269, inspiration/page.tsx:280}` — actiuni ireversibile la un singur click, fara confirmare: aproba firma (porneste trial Gold + credite bonus), confirma plata (crediteaza portofel/abonament + consuma numar de factura), rezolva/respinge disputa, aproba retragere (refund credite), sterge poza. → dialog de confirmare (AlertDialog) cu rezumat; la disputa/retragere eventual nota obligatorie.
- `[P1] apps/backend/src/modules/admin/dto/admin.dto.ts:3-6` + `admin.service.ts:121-128` — setarile de sistem se salveaza ca string liber (max 500) fara tipare/limite per cheie: `vat_rate=abc` → `getNumber` cade silentios pe 21; `max_claims_per_request=0` → nicio preluare posibila; `eur_ron_rate=0`, `trial_duration_days=-5`, `company_penalty_threshold=1` etc. sunt acceptate. → registru de chei (tip, min, max, enum) validat la BE si input tipizat la FE (`settings/page.tsx:134-144` foloseste `<input>` text pentru toate).
- `[P1] apps/backend/src/modules/admin/dto/admin.dto.ts:57-61` + `admin.service.ts:198-206` — praguri: fara validare `minScore <= maxScore`, fara verificarea suprapunerii/golurilor intre S/M/L, fara max; `UpdatePlanDto/UpdatePenaltyRuleDto` fara max (zod-ul din shared are `max(100)`/`max(100000)` dar nu e folosit). → validare cross-field in service (intervalele sa acopere 0..inf fara suprapunere), max-uri aliniate cu shared.
- `[P1] apps/frontend/src/app/[locale]/admin/settings/page.tsx:195,202` — `ThresholdRow` initializeaza `max = th.maxScore ?? 0`; pragul LARGE are `maxScore=null` (seed, verificat live) → apasand Salveaza pe LARGE se trimite `maxScore: 0` (dupa fixul P0 de mai sus chiar ar reusi) si LARGE devine [30..0]; `sizing.service.ts:64-67` ar mapa prin fallback, dar datele raman corupte. → checkbox "fara limita" / trimite `null` cand campul e gol.
- `[P1] apps/backend/src/modules/companies/admin-companies.controller.ts:19` — `@Query('status') status?: CompanyStatus` nevalidat → `?status=XYZ` produce 500 `INTERNAL_ERROR` (reprodus live). → `@IsEnum(CompanyStatus)` intr-un query DTO (sau `ParseEnumPipe`).
- `[P2] apps/backend/src/modules/companies/companies.service.ts:571-597` — `adminApprove` nu verifica statusul curent: API-ul permite aprobarea unei firme REJECTED sau SUSPENDED (suspendarea din penalizari poate fi anulata printr-un POST; UI ascunde butonul doar daca nu e PENDING). → 409 daca status != PENDING_VERIFICATION (sau actiune explicita "ridica suspendarea", auditata separat).
- `[P2] apps/backend/src/modules/companies/companies.service.ts:530-551` + `inspiration.service.ts:274-281` — `GET /admin/companies` si `GET /admin/inspiration` fara paginare (toata tabela; 162 poze live, fiecare cu presign). → paginare ca la audit-logs.
- `[P2] apps/backend/src/modules/audit/audit.interceptor.ts:42-43` — `entityId = result.id ?? params.id`; pentru `PUT /admin/settings/:key` parametrul e `key` si raspunsul nu are `id` → `entityId=null` la SETTING_UPDATED (cheia e doar in `after`). → fallback pe `params.key`/primul param.
- `[P2] apps/backend/prisma/seed.ts:180-196` — la re-rulare, `seed.ts` SUPRASCRIE planurile (pret/credite/gating) si pragurile cu valorile din cod (`update: {...}`), spre deosebire de settings (`update: {}`); modificarile admin se pierd la urmatorul seed. → `update: {}` (sau flag explicit `--reset`).
- `[P2] apps/backend/src/modules/inspiration/inspiration.service.ts:372-377` + FE `inspiration/page.tsx:63,126` — `assertCompany` verifica doar existenta; dropdown-ul listeaza toate firmele (PENDING/REJECTED/SUSPENDED) → galeria publica poate atribui lucrari unei firme neaprobate. `@IsUrl()` pe `imageUrl/sourceUrl` accepta `http://` (continut mixt pe pagina publica). → filtreaza APPROVED; `require_protocol` + `protocols: ['https']`.

### Completitudine (4.19: "firme, verificari, cereri, claim-uri, credite, abonamente, penalizari, dispute, audit, settings, jobs")
- `[P1] apps/frontend/src/app/[locale]/admin/layout.tsx:13-23` — lipsesc din consola: cereri (lista/detaliu/interventie), claim-uri, credite (ajustare manuala portofel, istoric tranzactii), abonamente per firma, penalizari aplicate per firma/angajat, calendar business (override "punti guvernamentale" din docs/02 linia 31). Nu exista nici endpoint BE pentru acestea (verificat `grep Controller('admin`). → DECIZIE NECESARA: ce intra in MVP.
- `[P1] notificari catre admin` — nu exista niciun canal prin care adminul afla ca are de lucru: firma noua in asteptare, plata PENDING, disputa noua, retragere custom. `withdrawal.reminder` (48h, 4.15) e publicat FARA targets (`withdrawal-reminder.processor.ts:31`) → `notifications.processor.ts:24` il ignora (fara persistenta) → adminul nu primeste reminderul; nu exista email de admin (`grep ADMIN_EMAIL` = 0). → targets = toti userii ADMIN + email; badge-uri cu numar in sidebar (KPI-ul exista deja).
- `[P1] apps/frontend/src/app/[locale]/admin/companies/[id]/page.tsx:153-174` — "Detalii firma" afiseaza doar numere (membri/locatii/portofoliu) desi DTO-ul contine membrii cu email/rol, locatiile, portofoliul, `rejectionReason`, `suspendedUntil` (`companies.service.ts:284-330`). Pentru REJECTED nu se vede motivul, pentru SUSPENDED nu se vede pana cand/cate puncte; nu exista istoricul deciziei (reviewedBy/At/decisionNote din `company_verification_profiles`). Adminul aproba fara sa poata vedea efectiv ce verifica. → sectiuni: membri, locatii, portofoliu (cu imagini), profil verificare, abonament, portofel, penalizari.
- `[P1] apps/frontend/src/app/[locale]/admin/disputes/page.tsx` — fluxul dispute e incomplet: se deschide automat la review < 3 stele (`fulfillment.service.ts:99-113`), dar adminul vede doar numele firmei + stele + comentariu; nu are link la cerere (`d.requestId` exista in DTO si nu e folosit), nu vede partea firmei, nu poate intra in chat (4.19: "adminul intra in chat DOAR in dispute" — nu exista nicio cale in `modules/chat`, doar comentariul din `message-crypto.service.ts:12`). `RESOLVED` si `DISMISSED` au EXACT acelasi efect (`fulfillment.service.ts:174-184`: cererea → COMPLETED, review-ul ramane neschimbat, fara penalizare/ascundere review, fara notificare explicita partilor). → defineste efectele (ex. DISMISSED = review-ul ramane public; RESOLVED = review ascuns/recalculat + eventual penalizare), link la cerere, acces chat read-only.
- `[P1] apps/frontend/src/app/[locale]/admin/payments/page.tsx` — fluxul e "mock"/manual (`mock_billing_orders`, invarianta 3.7 calea A) dar UI nu spune nimic despre asta: adminul vede "Firma · Abonament · · 500 RON" si un buton "Confirma plata" fara referinta de plata, IBAN, data comenzii, plan/pachet, detaliu TVA; nu exista actiune "anuleaza comanda" desi enum-ul are CANCELLED (`schema.prisma:270-274`, folosit doar de webhook) → comenzile neplatite raman PENDING la infinit si umfla KPI-ul. → afiseaza referinta/suma/data + nota "confirmare manuala (transfer bancar)", adauga cancel auditat; `{o.credits ?? ''}` lasa un segment gol la abonamente (`payments/page.tsx:37`).
- `[P2] apps/frontend/src/app/[locale]/admin/audit/page.tsx` — viewer-ul arata doar actiune/entitate/rol/data; nu arata `userId`, `entityId` (DTO-ul le are) si nici `before/after` → nu poti afla CE firma a fost aprobata sau CE valoare a primit o setare. Filtrele BE `entityType`/`userId` (`admin.controller.ts:44-46`) nu sunt expuse in UI. → coloane userId/entityId + rand expandabil cu `after`, filtre entitate/utilizator.
- `[P2] apps/frontend/src/app/[locale]/admin/companies/page.tsx:95-99` + `companies.service.ts:50-55` — `computeRiskFlags(portfolioCount, reviewCount = 0)`: `reviewCount` e MEREU 0 (review-urile exista de la Sprint 8 dar nu sunt numarate) → "Prea putine recenzii" apare la TOATE firmele, iar LOW_RATING nu e calculat niciodata → coloana "Semnale de risc" e zgomot. → numara `review` per firma (si rating mediu) sau scoate flag-ul pana e implementat.
- `[P2] apps/frontend/src/app/[locale]/admin/jobs/page.tsx` — util minim (cozi/nume/motiv/incercari/retry), dar nu arata `timestamp` (exista in DTO), payload-ul jobului, nici contoare waiting/active/delayed per coada (sanatatea cozilor); fara "discard". → coloana data + counts per coada; detaliu payload.
- `[P2] apps/frontend/src/app/[locale]/admin/page.tsx:110-111` — dashboard: la eroare (403/500) ramane "Se incarca…" la infinit (`!kpi.data` → loading); `Breakdown` afiseaza cheile brute de enum (`PENDING_VERIFICATION`, `OFFER_SENT`...) netraduse; la fel badge-ul de status din lista firme (`companies/page.tsx:92`). → stare de eroare + `t()` pe statusuri.
- `[P2] stari loading/empty/error` — `audit/page.tsx`, `jobs/page.tsx`, `settings/page.tsx` nu au loading (sectiuni goale pana vin datele) si nicio stare de eroare; `disputes/payments/withdrawals` arata loading doar pentru `me`, nu pentru lista; `companies/page.tsx:41` si `companies/[id]/page.tsx:136` folosesc "…" ca loading; `companies/[id]/page.tsx:139` afiseaza "Nicio firma de afisat" la eroare de retea.
- `[P2] apps/frontend/src/app/[locale]/admin/settings/page.tsx` — fara niciun feedback la salvare (succes/eroare) pe toate cele 5 sectiuni; dupa refetch valorile locale din `useState(s.value)` nu se resincronizeaza; `PenaltyRow` nu explica ce inseamna fiecare `ruleKey`; thresholds randate in ordinea DB (`admin.service.ts:194` fara orderBy) — live iese S/M/L din intamplare.

### UX / design / texte
- `[P2] apps/frontend/src/app/[locale]/admin/layout.tsx:31` — `grid-cols-[260px_1fr]` fix, fara breakpoint: pe < ~900px sidebarul de 260px mananca ecranul (restul aplicatiei e responsive). → sidebar colapsabil/drawer sub `lg`.
- `[P2] texte hardcodate (nu t())`: `layout.tsx:48` "Admin", `layout.tsx:73` "Plan · panou operational" (filler fara sens), `settings/page.tsx:155,187` "activ", `:170-172` "RON"/"credite"/"gating", `:185-186`, `:200-201` "min"/"max", `inspiration/page.tsx:125` "—", `audit/page.tsx:216,226` "←"/"→" (fara aria-label). → chei in `Admin.settings.*`.
- `[P2] apps/frontend/src/app/[locale]/admin/companies/[id]/page.tsx:208-209` — mesajele "Firma a fost aprobata/respinsa" sunt randate in interiorul blocului `pending && (...)`; dupa invalidare statusul se schimba si blocul dispare → mesajul nu apuca sa fie vazut. → toast sau mesaj in afara blocului.
- `[P2] apps/frontend/src/app/[locale]/admin/inspiration/page.tsx:187-201` — 162 carduri randate dintr-o data, fara cautare/filtru/paginare; nu se poate edita titlul/firma/etichetele dupa creare (UI expune doar published/featured/upload/sterge, desi `PATCH` accepta tot). → filtre + editare inline.
- `[P2] apps/frontend/src/app/[locale]/admin/withdrawals/page.tsx:252` — `reason: <em>{w.customReason}</em>` fara link la cerere/firma; nu se vede cand expira SLA-ul de 48h (4.15). → deadline vizibil + link.

## DE STERS
- `packages/shared/src/admin.schemas.ts:57-86` — cele 5 scheme Zod (`updateSettingSchema`, `updatePenaltyRuleSchema`, `upsertCreditPackageSchema`, `updatePlanSchema`, `updateThresholdSchema`) nu sunt importate nicaieri in apps/ (grep = 0 rezultate in afara fisierului); doar tipurile `z.infer` sunt folosite in `admin.service.ts`. Fie se folosesc la FE (validare inainte de mutate) si se aliniaza limitele cu DTO-urile BE, fie se inlocuiesc cu interfete simple.
- `apps/backend/src/modules/admin/admin.controller.ts:82-86` + `admin.service.ts:152-157` — `POST /admin/credit-packages` (creare pachet) nu are niciun apelant in FE (grep `credit-packages` in apps/frontend = doar `/billing/credit-packages`); UI-ul poate doar edita pachete existente. Ori se adauga formular "Pachet nou", ori se scoate endpoint-ul.
- `apps/backend/src/modules/admin/admin.controller.ts:45-46` — filtrele `entityType` si `userId` din `GET /admin/audit-logs` nu sunt folosite de FE (`audit/page.tsx` trimite doar `action`; hook-ul `useAuditLogs` accepta `entityType` dar nimeni nu-l paseaza). De expus in UI (vezi P2 mai sus) sau de scos.
- `system_settings` `employee_penalty_threshold` si `employee_block_months` (`seed.ts:159-160`) — niciun consumator in `apps/backend/src` (grep = 0; memoria proiectului: "Sprint 7: no employee penalty block"). Apar in UI-ul de setari si pot fi editate fara niciun efect. De scos din seed (si o migrare de curatare) sau de marcat ca inactive.
- `apps/backend/src/modules/admin/dto/admin.dto.ts:60` `UpdateThresholdDto.creditCost` + `admin.service.ts:203` — FE nu mai trimite `creditCost` (PO r5: 1 credit = 1.000 lei din bugetul minim; `sizing.service.ts` nu mai citeste `creditCost`) → camp mort in API/UI-ul de praguri (`AdminThreshold.creditCost` nu e afisat nicaieri).
- Comentarii cu "atelier" (terminologie PO = firma): `admin/inspiration/page.tsx:31`, `inspiration.dto.ts:73,123` — doar comentarii, cosmetic.
- `apps/frontend/src/app/[locale]/admin/layout.tsx:72-74` — caseta "Plan · panou operational" este filler fara functie.
- Verificat si NU s-a gasit: `console.log`, `TODO/FIXME`, cod comentat in fisierele din aria admin (FE+BE).

## INTREBARI PENTRU PO
1. Scopul consolei pentru lansare: din lista 4.19 lipsesc cereri, claim-uri, credite (ajustare manuala), abonamente, penalizari per firma, calendar business. Care intra in MVP si care raman post-lansare?
2. Dispute: ce efect concret trebuie sa aiba RESOLVED vs DISMISSED (review ascuns? penalizare firma? notificare partilor?) si vrei chat admin read-only/participativ in disputa (4.19)?
3. Plati: ramane fluxul manual (transfer bancar + confirmare admin) la lansare? Daca da, ce referinta trebuie sa vada adminul ca sa potriveasca plata (cod comanda in detaliile de plata, IBAN afisat firmei) si vrei "anuleaza comanda" dupa X zile?
4. Cum se creeaza contul de admin pe prod: acceptam `admin@demo.ro` din seed-demo sau vrei un script `seed-admin` cu email/parola din env (si eventual 2FA obligatoriu pentru ADMIN — `twoFactorEnabled` exista pe user)?
5. Suspendare/ridicare suspendare manuala a unei firme de catre admin (acum doar automat din penalizari): o vrei in MVP?
6. Risk flag "Prea putine recenzii" apare la toate firmele (review-urile nu sunt numarate): il pastram si il calculam real, sau il scoatem pana la lansare?
7. Cum vrea adminul sa fie anuntat (email / clopotel / ambele) pentru: firma noua, plata in asteptare, disputa, retragere custom (reminder 48h)?
8. Setarile `employee_penalty_threshold`/`employee_block_months` (fara efect, decizie Sprint 7) — le scoatem din UI?
