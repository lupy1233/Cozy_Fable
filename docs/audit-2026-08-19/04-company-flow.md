# Aria: flux firma (marketplace) FE

Audit read-only, 2026-08-19. Fisiere citite integral: `apps/frontend/src/app/[locale]/company/**`, `marketplace/**` (page, [id], claims, claims/[claimSlotId], messages, wallet), hooks `use-marketplace/claims-lifecycle/fulfillment/chat/billing/company/penalties/socket/notifications/quotes`, `lib/relative-time|utils|providers|api`, `_components/app-header|app-shell|mobile-nav|notification-bell|chat-panel|claim-lifecycle-panel|offer-builder|offer-card`, plus verificari in backend (`modules/marketplace|claims|quotes|billing|companies|fulfillment|chat`, `common/guards`) si `packages/shared`. Toate caile de mai jos sunt relative la `apps/frontend/src/` daca nu e specificat altfel.

## BINE FACUT

- **Invarianta 4.2 respectata pre-claim**: `marketplace/[id]/page.tsx` nu afiseaza contact/adresa; backend `marketplace.service.ts` nu le serveste; contactul apare doar in fisa de lucru din `ClaimQuoteContextDto.client`, gated de `OCCUPYING_CLAIM_STATUSES` (`quotes.service.ts:661`). ID-urile din URL sunt validate `ParseUUIDPipe` + ownership pe companyId (`quotes.controller.ts:63`, `claims.controller.ts:49`).
- **Idempotency-Key** pe toate POST-urile critice: claim (`use-marketplace.ts:43`), oferte (`use-quotes.ts:19`), cumparare credite (`use-billing.ts:110`).
- **Toate endpoint-urile apelate din FE exista in backend** (verificat 1:1 pe `@Controller/@Get/@Post` din `billing`, `chat`, `claims`, `companies`, `fulfillment`, `marketplace`, `notifications`, `penalties`, `quotes`).
- Guard de rol pe fiecare pagina (`useMe` + redirect) dublat de `@Roles(COMPANY_USER)` + `CompanyApprovedGuard`/`SubscriptionActiveGuard` in backend — rolul NU e verificat doar in UI.
- Fisa de lucru (`claims/[claimSlotId]/page.tsx`) e bine structurata: bara de lucru (atribuire/SLA/buget), card client, oferta, chat, lifecycle; atribuirea doar pentru OWNER/MANAGER (aliniat cu `claims.service.ts:269`).
- Chat: indicator citit/necitit real (`chat_thread_reads`) — marcare la intrarea in viewport (`chat-panel.tsx:34-55`), badge necitite in lista de claims (`claims/page.tsx:232-239`) si in mesagerie; realtime prin socket (`use-socket.ts`) + polling 30s pe unread-count (`use-notifications.ts:85`).
- Mesagerie master-detail responsive (lista ascunsa pe mobil cand e deschisa o conversatie), tab "Echipa firmei" separat (`messages/page.tsx`).
- Offer builder: defalcare pe camere cu total auto + blocare pret (`offer-builder.tsx:59-79`), bug-ul `z.coerce` pe campuri optionale e tratat (`optionalPositive`), erori API mapate prin `t.has('apiErrors.X')`.
- Matricea de permisiuni: stare dirty, "Renunta", buton Save dezactivat cand nu e nimic de salvat (`company-dashboard.tsx:377-456`).
- Bannere de status firma (PENDING/REJECTED/SUSPENDED) pe `/company` (`company-dashboard.tsx:54-70`), cu textul rejectedNotice care explica blocajul de 3 luni (4.6).
- `api.ts`: compunere explicita a headerelor (fix F7), refresh dedupe pe 401, body gol/204 tratat.
- Tabelul de claims foloseste `Table` cu `overflow-x-auto` (`components/ui/table.tsx:6`) — nu sparge layoutul pe mobil.
- Nu exista `console.log`, cod comentat sau `TODO/lorem/coming soon` in fisierele ariei; termenul "atelier" apare doar intr-un comentariu (vezi DE STERS).

## DE MODIFICAT

### P0

- `[P0] marketplace/wallet/page.tsx:309-348 — Cumpararea de credite PARE reala dar e mock: click pe "10 credite · 100 RON" creeaza instant un `mock_billing_order` PENDING (`payments.service.ts:53-74`), fara dialog de confirmare, fara instructiuni de plata (IBAN/transfer/link), fara toast; singurul feedback e un rand "Pachet de credite · 121 RON · In asteptarea platii" (total CU TVA 21%, desi butonul arata pretul NET 100 RON). Nu exista anulare comanda. → Pana la integrarea reala: (a) confirmare explicita cu total cu TVA; (b) ecran/sectiune "Cum platesti" (date furnizor din Admin Settings, referinta = nr. comanda) sau, daca PO vrea sa ascunda mock-ul la lansare, ascunde butoanele si lasa doar istoric+facturi. (c) pret afisat pe buton = total cu TVA sau "+TVA".`
- `[P0] marketplace/wallet/page.tsx:287-307 + (lipsa pagina) — Nu exista NICIO cale in FE de a cumpara/reinnoi abonamentul (`POST /billing/subscription/purchase` exista in backend, `payments.controller.ts:64`, dar nu e apelat nicaieri in FE; nu exista pagina de preturi/upgrade; nici adminul nu are UI de a acorda abonament). Trialul se creeaza la approve pentru 30 zile (`subscriptions.service.ts:21-52`); la expirare `SubscriptionActiveGuard` blocheaza marketplace + claims si firma vede doar "Niciun abonament activ." fara CTA. → Sectiune "Abonament" cu planuri + buton de cumparare (chiar si mock, ca la credite) SAU macar CTA "Contacteaza-ne" + text explicativ; docs 4.10 cere CTA de upgrade pe dashboard/pagina de preturi.`

### P1

- `[P1] marketplace/page.tsx:182-187 — `list.isError` nu e tratat: pentru firma PENDING_VERIFICATION / SUSPENDED / fara abonament / fara firma, `/marketplace/requests` raspunde 403/404 (`company-approved.guard.ts:29-52`, `subscription-active.guard.ts:29`) si pagina ramane GOALA sub titlu (nici "loading", nici mesaj). Aceeasi pagina e si "acasa" pentru firma (`app-header.tsx:38` HOME_BY_ROLE → /marketplace). → Ecran de asteptare/blocaj: mapeaza `ApiError.code` pe `Marketplace.apiErrors.{COMPANY_NOT_APPROVED|COMPANY_SUSPENDED|SUBSCRIPTION_INACTIVE}` (textele EXISTA deja in ro/en.json) + link spre /company sau /marketplace/wallet. La fel `marketplace/claims/page.tsx:206-212` (nici loading, nici error → pagina goala) si `messages/page.tsx:64-72` (tabul Echipa ramane pe "Se incarca…" la nesfarsit cand `useTeamThread` da 403).`
- `[P1] messages/ro.json:902-909 (si en.json) — `Marketplace.claimStatus` nu are cheile `WITHDRAWN` si `SLA_EXPIRED`, desi backend le seteaza (`withdrawals.service.ts:117,232`, `quotes.service.ts:456`, `sla-breach.processor.ts:50`). Folosite in `claims/page.tsx:256`, `claims/[claimSlotId]/page.tsx:71`, `messages/page.tsx:113-116`, `dashboard/page.tsx:158` → eticheta apare ca cheie bruta "Marketplace.claimStatus.SLA_EXPIRED". → Adauga cele 2 chei (exista deja in `Lifecycle.claimStatus`) sau foloseste un singur namespace.`
- `[P1] lib/relative-time.ts:135-136 + claims/[claimSlotId]/page.tsx:194,218 — `useRelativeTime` face `Math.max(0, now - then)` → pentru date VIITOARE returneaza intotdeauna "chiar acum". In bara de lucru: "Fara responsabil — preluarea se anuleaza automat chiar acum" (desi mai sunt pana la 60 min) si sub termenul SLA "chiar acum". Alarma falsa pe cel mai important ecran. → Suport pentru viitor ("in X min/ore/zile") sau countdown dedicat pentru assignDeadlineAt/slaDeadlineAt.`
- `[P1] marketplace/[id]/page.tsx:128-134 — Claim-ul se trimite FARA `assignToUserId` (desi `CreateClaimInput` il suporta) → orice preluare porneste neatribuita cu auto-anulare la 1h (`claims.service.ts:189-191`). Un EMPLOYEE_TRUSTED care preia nu poate atribui (FE `canAssign` doar OWNER/MANAGER, la fel backend `claims.service.ts:269`) → preluarea lui se anuleaza in 1h daca nu intervine un manager. → In ClaimPanel: selector "Cine se ocupa?" (implicit eu insumi; pentru owner/manager lista membrilor), conform 4.9 self-assign.`
- `[P1] marketplace/claims/[claimSlotId]/page.tsx:106 — `ChatPanel` in fisa de lucru NU primeste `readOnly` (contextul `ClaimQuoteContextDto` nu are flagul; `ChatThreadDto.readOnly` exista doar in lista din mesagerie). Dupa ce clientul accepta alta firma, thread-ul devine readOnly (`quotes.service.ts:578`) dar firma pierzatoare vede composer activ; trimiterea esueaza cu THREAD_READ_ONLY si `chat-panel.tsx:57-63` nu afiseaza NICIO eroare (`mutateAsync` fara catch). → expune `threadReadOnly` in context (sau ia-l din `useThreads`) + afiseaza erorile de send/upload in ChatPanel.`
- `[P1] fisa de lucru (claims/[claimSlotId]/page.tsx) — NU exista stare "ai pierdut"/"cerere inchisa": la acceptarea altei oferte, slotul ramane `OFFER_SENT` (`quotes.service.ts:572-583` nu schimba statusul), oferta devine SUPERSEDED ("Inlocuita" — `offer-card.tsx:246`), chatul readOnly, creditele consumate (OFFER_LOST) — dar lista de claims arata in continuare "Oferta trimisa", panoul lifecycle ofera "Retrage preluarea", iar contactul clientului ramane vizibil. → Banner explicit "Clientul a ales alta firma — creditele au fost consumate" (regula pay-to-play) + ascunde actiunile; cere backendului un status dedicat sau foloseste `quote.status === 'SUPERSEDED'`.`
- `[P1] claims/[claimSlotId]/page.tsx:100,374-387 — Butonul "Marcheaza ca livrata" apare ori de cate ori `quote.status === 'ACCEPTED'`; contextul nu contine statusul cererii → dupa refresh butonul reapare desi s-a livrat deja (`DELIVERED_BY_COMPANY`), al doilea click da `DELIVERY_NOT_ALLOWED` si `DeliverButton` nu afiseaza eroarea. Firma nu vede nicaieri ca clientul a confirmat / a lasat recenzie / a deschis disputa (singurul semnal = badge COMPLETED). → `requestStatus` in `ClaimQuoteContextDto` + pasi de finalizare (Acceptata → Livrata → Confirmata/Disputa) + recenzia primita.`
- `[P1] _components/offer-builder.tsx (tot fisierul) — Nu exista UI pentru atasamente la oferta, desi backend suporta `attachmentIds` (`quote.schemas.ts:54-69`) si presign `POST /quotes/claims/:claimSlotId/attachments` (`quotes.controller.ts:159-168`); `offer-card.tsx:146-154` stie sa le afiseze. Firma nu poate trimite schita/PDF odata cu oferta. → Upload presign→PUT→confirm ca in `useUploadChatAttachment`, max `MAX_ATTACHMENTS_PER_QUOTE`.`
- `[P1] _components/offer-builder.tsx:159-199 — Formularul ignora matricea de permisiuni pe campuri: backend verifica `assertFieldPermissions` (`quotes.service.ts:791-808`), iar `touchedFieldKeys` include MEREU PRICE+DESCRIPTION (`quotes.helpers.ts:21`). Cu defaults (`companies.constants.ts:18-19`) EMPLOYEE_TRUSTED si EMPLOYEE_MANAGED nu pot trimite NICIODATA o oferta, dar vad formularul complet si primesc doar "Nu ai permisiunea de a edita unele campuri" dupa completare. → dezactiveaza campurile/formularul pe baza `company.offerFieldPermissions` + `myRole` (ambele in `CompanyDto`) cu mesaj clar "doar X poate trimite oferta". Vezi si INTREBARI.`
- `[P1] _components/offer-card.tsx:220-341 — `CompanyActions` nu afiseaza NICIO eroare pentru reject/extend/withdraw/endNegotiation/invite (doar `ClientActions` are `err()`): ex. retragere dupa 1 zi lucratoare → `QUOTE_WITHDRAW_WINDOW_CLOSED` exista in `Quotes.apiErrors` dar nu se vede; utilizatorul apasa si "nu se intampla nimic". "Inchei negocierea" (ireversibil, chat read-only pentru ambii) si "Retrage oferta" nu au confirmare. → mapare erori ca la client + confirm dialog.`
- `[P1] company/_components/company-dashboard.tsx:99-106,161-185 — `EMPTY_LOCATION` are `lat: 0, lng: 0` si inputurile afiseaza "0"; schema accepta 0/0 (`company.schemas.ts:19-20`) → daca utilizatorul nu modifica, se creeaza o locatie in Golful Guineei si firma nu vede nicio cerere (filtrul Haversine 4.8). Adaugarea se face fara validare client si FARA afisare erori. → lat/lng goale + obligatorii, sau geocodare (exista `components/configurator/address-autocomplete.tsx` folosit la client).`
- `[P1] company/page.tsx:97-110 — Onboardingul cere firmei sa tasteze manual Latitudine/Longitudine (fara hint, fara autocomplete), iar dupa aprobare NU se creeaza automat nicio locatie (`companies.service.ts:235-258` creeaza doar company/member/profile/permissions) → firma aprobata vede marketplace gol ("Nicio cerere disponibila momentan.") fara sa i se spuna ca trebuie sa adauge o locatie cu raza. → (a) creeaza locatia din adresa de onboarding cu raza default 50 km sau (b) empty-state din marketplace sa trimita la "Adauga locatie"; (c) inlocuieste lat/lng cu autocomplete.`
- `[P1] company/_components/company-dashboard.tsx:285-298 — "Adauga membru" cere ca emailul sa apartina unui COMPANY_USER deja inregistrat (`companies.service.ts:400-405` → NOT_FOUND) si nu exista invitatie prin email; UI nu afiseaza eroarea (nicio mutatie din Locations/Team/Portfolio nu are error UI — 0 aparitii `.error/isError` in fisier) si nu explica regula. → text explicativ "persoana trebuie sa aiba deja cont de firma cu acest email" + afisare `Company.apiErrors.*`; ideal invitatie prin email.`
- `[P1] hooks/use-company.ts:224-228 + company-dashboard.tsx:72-80 — Profilul firmei (nume, adresa, oras, lat/lng) NU poate fi editat post-onboarding: `useUpdateCompany` exista (PATCH /companies/me exista in backend) dar nu e folosit nicaieri; cheile `Company.edit/save` exista in ro/en.json. O greseala la onboarding nu poate fi corectata de firma. → ecran de editare profil (CUI/RegCom read-only conform schemei).`
- `[P1] marketplace/wallet/page.tsx:313-323 vs 329-333 — Pretul pachetului afisat NET (`p.priceRon`), comanda afisata cu TVA (`o.totalRon`) fara mentiune TVA; nu e clar ce plateste firma. → afiseaza "100 RON + TVA = 121 RON".`
- `[P1] _components/claim-lifecycle-panel.tsx:247-255 — "Retrage preluarea" fara confirmare si fara avertisment asupra consecintelor (4.15: VOLUNTARY dupa 30 min = fara refund + 2 puncte penalizare; CUSTOM = slot ramane ocupat pana la decizia adminului). → dialog de confirmare cu consecinta calculata (timp scurs de la claim e disponibil in `ClaimSlotDto.createdAt`).`

### P2

- `[P2] messages/ro.json:165 Nav.claims = "Revendicari" vs `Marketplace.myClaims.title` "Preluarile mele", `Dashboard.company.toClaims` "Preluarile mele", buton "Preia" — doua terminologii pentru acelasi concept in meniul principal. → uniformizeaza pe "Preluari".`
- `[P2] _components/app-header.tsx:42-46 — `ROLE_LABEL` hardcodat in TSX ("Firmă"/"Client"/"Admin", cu diacritice), afisat si in locale EN. → `t('role.X')` (exista deja `Auth.role.*` folosit in dashboard).`
- `[P2] _components/app-header.tsx:23-30 — Firma PENDING/SUSPENDED vede toate cele 6 linkuri (Marketplace, Revendicari, Mesaje, Portofel) care duc la pagini goale/blocate. → ascunde sau marcheaza linkurile pana la APPROVED + abonament activ (statusul e in `useMyCompany`).`
- `[P2] _components/notification-bell.tsx:256-260 — deep-link pentru firma: orice notificare ne-mesaj duce la lista generica /marketplace/claims, nu la fisa claim-ului; payload-ul (`quotes.service.ts:591-598`) nu contine claimSlotId. → adauga claimSlotId in payload si linkeaza `/marketplace/claims/{id}`. Si `TYPE_ICON` nu are `claim.withdrawn`/`request.status_changed` (fallback Bell — ok, dar inconsistent).`
- `[P2] hooks/use-socket.ts:41-46,53 — `claim.created`/`request.status_changed` invalideaza `requests/marketplace/chat/notifications`, dar NU `['claims']` si `['quotes']` → lista de claims si fisa de lucru nu se actualizeaza realtime la auto-anulare (1h), SLA expirat, anulare de client; evenimentul `claim.withdrawn` (publicat in `sla-breach.processor.ts:80`) nu e ascultat deloc. `useMessages` nu are polling de rezerva daca socketul pica.`
- `[P2] claims/page.tsx:259, [claimSlotId]/page.tsx:215, claim-lifecycle-panel.tsx:184, wallet/page.tsx:300,366,376, offer-card.tsx:133,144,354, chat-panel.tsx:105 — date formatate cu `toLocaleString()/toLocaleDateString()` (locale+timezone-ul browserului), in timp ce `company-dashboard.tsx:67` foloseste `useFormatter` din next-intl; fara timeZone in `i18n/request.ts`. Pe un browser en-US in pagina RO SLA apare "8/19/2026, 3:00:00 PM". → `useFormatter().dateTime` peste tot + `timeZone: 'Europe/Bucharest'` in config (3.3).`
- `[P2] claims/page.tsx:258-261 + [claimSlotId]/page.tsx:209-221 — SLA afisat doar ca data absoluta, fara timp ramas/urgenta (culoare la <24h, depasit) si fara grace 12h; `StatusBadge` nu are ton pentru ACTIVE/SLA_EXPIRED/CANCELLED_* (`components/ui/badge.tsx:42-56`) → toate "muted".`
- `[P2] marketplace/page.tsx (lista) + claims/page.tsx (tabel) — fara filtre/sortare/paginare: marketplace aduce pana la 200 cereri (`marketplace.service.ts:185`) intr-un grid fara filtru pe marime/buget/distanta; claims listeaza toate statusurile (si cele inchise) fara filtru "active".`
- `[P2] marketplace/[id]/page.tsx:27-29 — orice eroare (403 firma neaprobata, 404, client ratacit — pagina nu are guard de rol ca restul) afiseaza "Nicio cerere disponibila momentan." → mesaj de eroare distinct.`
- `[P2] marketplace/[id]/page.tsx:125-139 — EMPLOYEE_MANAGED vede butonul "Preia"; backend refuza (`claims.service.ts:78`) cu FORBIDDEN, iar textul `Marketplace.apiErrors.FORBIDDEN` = "Doar proprietarul sau un manager poate atribui preluarea." (mesaj despre atribuire, nu despre preluare). → ascunde butonul pe `myRole === 'EMPLOYEE_MANAGED'` (4.7) + text dedicat. Lipseste si `MANAGER_UNASSIGNED_CAP_REACHED` din `Marketplace.apiErrors` (aruncat in `claims.service.ts:404`) → cade pe "A aparut o eroare".`
- `[P2] marketplace/wallet/page.tsx:276-284,290-306 — in timpul incarcarii (`isPending`) se afiseaza "Inca nu ai un portofel." / "Niciun abonament activ." (flash de empty-state); `Metric` "Disponibile" si "Sold liber" arata MEREU aceeasi valoare (`credits.service.ts:102`: available = balance) → metrica redundanta/confuza; "Intarziere +30 min" (subscription.gating) neexplicat.`
- `[P2] marketplace/wallet/page.tsx:373-378 — `e.ruleKey` afisat brut ("SLA_MISS", "MASS_SLA_MISS", "VOLUNTARY_WITHDRAWAL") + `e.reason` ignorat. → traduceri per ruleKey.`
- `[P2] marketplace/wallet/page.tsx:336, offer-card.tsx:35,50 — linkurile de PDF (factura, oferta) sunt `<a href>` direct catre API: ocolesc logica de refresh din `api.ts` → daca access-cookie-ul (15 min, `auth.constants.ts:2`) a expirat in tab, se deschide un JSON 401 in loc de PDF. → fetch cu `api()` + blob URL, sau un endpoint care accepta refresh.`
- `[P2] _components/chat-panel.tsx:79-109 — lista de mesaje nu face auto-scroll la ultimul mesaj (container `max-h-80 overflow-y-auto`, fara `scrollIntoView`) → mesajele noi raman sub fold; timpul doar `toLocaleTimeString()` fara data (conversatii pe mai multe zile nedistinse); 📎 emoji in loc de iconita lucide (si in `offer-card.tsx:150`); Enter nu trimite; inputul de fisier accepta orice tip desi `PresignUploadInput.mimeType` e enum → presign 400 netratat.`
- `[P2] _components/offer-builder.tsx:215-229 — `<style jsx>` cu `:global(.input)` injectat din fiecare instanta a formularului (3 instante posibile pe aceeasi pagina) in loc de componenta `Input` din `components/ui` (folosita in company-dashboard) → inconsistenta de stil intre formulare; `company-dashboard.tsx:337-361` foloseste `<input>`/`<button>` brute langa `Input`/`Button` din design system.`
- `[P2] company/page.tsx:64-87 — componenta `Field` definita in interiorul `OnboardingForm` → tip nou la fiecare render → inputurile se remonteaza (pierdere focus cand se schimba `errors`/`onboard.isPending`). → muta in afara componentei.`
- `[P2] company/_components/company-dashboard.tsx:127,253,327 — stergere locatie/membru/proiect portofoliu fara confirmare; `PortfolioSection` nu valideaza client-side (titlu min 2, URL) si nu afiseaza erori; lipseste "upload imagine" (doar URL).`
- `[P2] company/_components/company-dashboard.tsx:368-459 — matricea de permisiuni sugereaza editare pe camp, dar PRICE e obligatoriu la trimitere (vezi P1) → "Angajat de incredere: Pret = nu" inseamna de fapt "nu poate trimite oferte". → copy explicit sub matrice.`
- `[P2] _components/claim-lifecycle-panel.tsx:202-220 — "Cere clarificare" nu mentioneaza ca pune SLA pe pauza si adauga +1 zi lucratoare (4.11) — informatie utila firmei.`
- `[P2] _components/app-header.tsx:68 — `pathname.endsWith(l.href)` → pe fisa de lucru (/marketplace/claims/<id>) niciun link nu e activ; `/dashboard` "Contul meu" + `/company` "Firma mea" + logo → /marketplace: trei "acasa" pentru firma.`
- `[P2] messages/page.tsx:224-226 + chat-panel.tsx:111-114 — "doar citire" nu spune DE CE (oferta altei firme acceptata vs negociere incheiata) desi `ChatThreadDto.negotiationEndedByCompany` exista.`

## DE STERS

- `hooks/use-company.ts:224-228 useUpdateCompany, :236-243 useUpdateLocation — exportate, 0 utilizari in afara hooks/ (grep); fie se construieste editarea profilului (vezi P1), fie se sterg.`
- `hooks/use-quotes.ts:27-33 useCompanyQuotes (+ endpoint GET /quotes/mine) — 0 utilizari in FE.`
- `hooks/use-claims-lifecycle.ts:196-199 — `onSuccess: (_d, v) => { ...; void v; }` parametru inutil pastrat doar ca sa evite lint.`
- `messages/ro.json:885 + en.json:885 Marketplace.publishedAgo — nefolosit (inlocuit de useRelativeTime, comentariul din relative-time.ts:131 o confirma).`
- `messages/ro.json (Company) edit/save/role — chei nefolosite (0 `t('edit')/t('save')/t('role')` in zona Company); tin de ecranul de editare profil inexistent.`
- `marketplace/[id]/page.tsx:150 `claim.isSuccess && t('claimSuccess')` — practic mort: `onSuccess` navigheaza imediat la fisa de lucru (:133).`
- `messages/page.tsx:36-37 — comentariu "implicit: prima conversatie cu necitite, altfel prima din lista (doar desktop)" fara cod corespunzator (`selectedId` porneste null si nu e auto-selectat niciodata).`
- `_components/notification-bell.tsx:21 — comentariu "(atelier · cerere)" — terminologie interzisa de PO (doar in comentariu).`
- `company/_components/company-dashboard.tsx:328 — `t('deleteLocation')` refolosit ca eticheta pentru stergerea unui proiect din portofoliu (cheie semantic gresita; merge doar pentru ca textul e "Sterge").`
- `marketplace/wallet/page.tsx:243, _components/offer-card.tsx:19 — `const API = process.env.NEXT_PUBLIC_API_URL ?? 'http://localhost:3001/api/v1'` duplicat din `lib/api.ts:3` (BASE_URL) → exporta BASE_URL.`
- `_components/app-header.tsx:42-46 ROLE_LABEL — hardcodat; de inlocuit cu i18n (vezi P2), nu de pastrat.`

## INTREBARI PENTRU PO

1. **Abonamente la lansare**: dupa cele 30 de zile de trial firmele raman blocate (nu exista cumparare/reinnoire in FE si nici in Admin). Vrem mock de cumparare abonament (ca la credite), vanzare asistata (admin seteaza manual) sau lansam fara expirare de trial?
2. **Cumparare credite**: la lansare o lasam vizibila cu flux mock (comanda PENDING confirmata de admin) — si atunci ce instructiuni de plata afisam (transfer bancar pe datele furnizorului din Admin Settings?) — sau o ascundem pana la integrarea unui procesator?
3. **Cine poate trimite oferte**: cu permisiunile default, doar OWNER/MANAGER pot trimite oferte (PRICE e mereu obligatoriu). E intentionat ca EMPLOYEE_TRUSTED sa NU poata trimite deloc, sau ar trebui sa poata trimite cu pretul completat/aprobat de manager?
4. **Firma pierzatoare**: dupa ce clientul accepta alta oferta, slotul ramane OFFER_SENT si firma care a pierdut continua sa vada numele + telefonul/emailul clientului (contextul e "occupying"). Confirmam ca se ascund contactele si afisam explicit "pierdut + credite consumate"?
5. **Onboarding firma**: nu se cere niciun document (act constitutiv, CUI) — verificarea e doar pe CUI/RegCom + portofoliu (4.6). Ramane asa la lansare? Si: se creeaza automat prima locatie din adresa de onboarding (raza 50 km) sau firma o adauga manual?
6. **Invitatii in echipa**: regula actuala cere ca angajatul sa-si faca singur cont de firma inainte sa poata fi adaugat. Vrem invitatie prin email (link de inregistrare pre-asociat)?
7. **Recenzii**: firma nu isi vede nicaieri ratingul/recenziile primite (si nici clientul nu le vede la alegerea firmei din oferta, in afara numelui). E in scope pentru lansare?
8. **Atasamente la oferta**: backendul suporta fisiere la oferta, FE nu. Il vrem la lansare (schita/deviz PDF)?
9. **Terminologie**: "Revendicari" (meniu) vs "Preluari" (restul) — care ramane?
