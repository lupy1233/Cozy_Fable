# CHANGELOG

## Sprinturi V1–V4 — 2026-07-13 — feedback PO runda 5 (docs/15): cost credite = buget minim / 1.000, 200 credite demo, email cont blocat ca prim contact, geocodare robusta
- **V1 Cost cerere in credite**: 1 credit = 1.000 lei din bugetul MINIM estimat (ex. 33.000 → 33 credite, minim 1) — `creditCostFromBaseScore` + `CREDIT_VALUE_RON` in shared; `SizingService` separa scorul de baza (camerele — identic cu `minRon/1.000` din `/requests/estimate`) de scorul total (+ buget + design platit) care ramane DOAR pentru marimea S/M/L (SLA). Costul de pe praguri nu se mai foloseste: input scos din Admin → Praguri, inlocuit cu nota formulei; coloana ramane legacy in DB.
- **V2 Credite demo**: toate firmele demo pornesc cu 200 de credite (`seed-demo.ts`); costurile demo aliniate (cost = scor: 10/20/35). One-off pentru mediul seedat: `pnpm -F backend seed:demo-credits` (wallets → 200, `credit_cost = GREATEST(1, size_score)` pe cererile publicate; snapshot-urile claim-urilor raman istorice).
- **V3 Email cont = cale de comunicare blocata**: primul contact e emailul contului — read-only cu lacat, fara stergere, hint explicativ (`details-step.tsx`); serverul il garanteaza la publish/edit prin `withAccountEmail` (dedupe + prepend, inainte de validarea capurilor). Bugfix: schimbarea canalului Email ↔ Telefon goleste valoarea (emailul nu mai ramane in campul de telefon). Se pot adauga max 1 email extra + 2 telefoane (capurile existente).
- **V4 Geocodare robusta** (`geo.service.ts`): adresa curatata de bl./sc./ap./et., judet fara prefixul „Judetul"/„jud.", cautare in trepte adresa+oras+judet → oras+judet → oras (pauza 1,1s, rate-limit Nominatim), tara din cerere (ISO2, era hardcodata Romania) inclusiv in cheia de cache si in `addressChanged` la edit. `GEOCODING_FAILED` doar daca TOATE treptele esueaza — „Adresa nu a putut fi localizata" dispare pentru orase/judete valide.
- Fara migrari DB. Fisiere: shared `request.schemas.ts`; backend `sizing.service.ts`, `requests.service.ts`, `geo.service.ts`, `seed.ts`, `seed-demo.ts`, `update-demo-credits.ts` (nou), `package.json`; frontend `details-step.tsx`, `admin/settings/page.tsx`, `ro.json`, `en.json`.
- **V5 (fix rapid PO)**: campurile numerice din configuratorul 3D (dimensiuni corp, inaltime rand, nr. sertare/polite) nu mai sar la min/max in timpul tastarii — valoarea editata sta intr-un draft local, clamp-ul regulilor se aplica DOAR la blur/Enter (Escape anuleaza; camp golit revine la valoarea veche). Ex.: sterge 50 → scrie 25 → la iesirea din camp se aplica minimul de 15 daca e cazul.
- **Deploy prod 2026-07-13**: `railway up` (deployment `fe9502f2`, health ok) + `seed:demo-credits` pe baza live — 8 firme demo cu 200 credite, 20 cereri publicate recalculate pe formula noua (verificat read-only: scor 18 + OVER_15K → cost 13; UNDISCLOSED → cost = scor).
- **V6 (fix rapid PO)**: ciornele nu mai apar in „Cererile mele" si nici in statisticile dashboardului (erau carduri goale „Ciornă", fara actiuni) — listele de client exclud `status DRAFT`; reluarea unei cereri neterminate ramane pe dialogul de resume din configurator. „Începe o cerere nouă" ARUNCA ciorna abandonata de pe server (endpoint nou `DELETE /requests/drafts/:token`, soft delete doar pe DRAFT, tokenul e secretul de acces; dupa stergere tokenul nu mai rezolva nimic — `findByToken` respinge si cererile sterse) — ciornele nu se mai acumuleaza la reset.

### Checklist acceptare V1–V4
- [ ] Cerere cu buget minim estimat 33.000 lei costa 33 de credite la claim (si scade exact atat din wallet).
- [ ] Cerere minuscula costa minim 1 credit; Admin → Praguri fara input „cost", cu nota formulei.
- [ ] Firmele demo au 200 de credite (seed nou sau `seed:demo-credits` pe mediul existent).
- [ ] Primul contact = emailul contului (lacat, fara stergere); schimbarea canalului pe randurile libere goleste campul.
- [ ] Adresa „Str. X nr. 3, bl. C2, sc. 1, ap. 7" + „Judetul Ilfov" se publica fara „Adresa nu a putut fi localizata".

## Sprinturi U1–U4 — 2026-07-13 — feedback PO runda 4 (docs/14): insula card-bifa, layout 3D pe un ecran, fix lightbox portofoliu, preview 3D read-only pentru firme
- **U1 Insula = playing-card cu bifa**: pe ecranul formelor de bucatarie, insula nu mai e buton lat cu switch, ci acelasi card „carte de joc" ca formele (ilustratie, flip Info cu pros/cons/pret), cu indicator patrat de CHECKBOX care se bifeaza/debifeaza la click; camp nou pur prezentational `cardLabelKey` pe `BooleanStep` (shared) + eticheta „Insulă"/„Island" (i18n).
- **U2 Controalele 3D pe un singur ecran** (`configurator3d-step.tsx`, /frontend-design): pe desktop scena 3D sta STICKY in stanga (500px) si toate comenzile intr-un rail de 330px in dreapta — panoul zonei selectate apare LANGA model (primul slot), nu sub el; fara zona selectata, slotul arata indrumarea de interactiune. Compactari: intervalul dimensiunilor langa eticheta (nu sub slider), coloane+finisaj+optiuni intr-un singur card, slidere verticale in rail. Mobil ramane stivuit; modul „campuri clasice" neatins.
- **U3 Fix lightbox caiet de idei „se deschide la inceputul paginii"**: `animate-pageIn` avea `fill-mode: both` → `<main>` pastra un `transform` permanent si devenea containing block pentru `position: fixed`, ancorand lightbox-ul la inceputul documentului. Fix dublu: animatia fara fill-mode (repara toate overlay-urile fixed) + lightbox-ul prin `createPortal(document.body)` cu scroll-lock pe body — la inchidere ramai exact pe randul din galerie.
- **U4 Preview 3D read-only pentru firme** (`piece3d/piece-viewer.tsx` NOU + buton „Vezi corpul în 3D" in `room-spec-card.tsx`): pe cererile cu corp configurat 3D (marketplace firma + detaliul clientului), dialog full-screen cu modelul interactiv — rotire, click pe zone deschide usile/sertarele (inclusiv glisante), zona selectata isi arata tipul, interiorul si dimensiunile rezolvate (inaltime rand, latime coloana, adancime, din `resolvePieceLayout`); NICIO posibilitate de editare. Datele erau deja in DTO — zero backend. Fallback fara WebGL + buton de QA pe `/dev/piece-3d`.
- Fara migrari DB, fara endpoint-uri noi, fara bump de flow (schimbari prezentationale). Teste: 111/111 vitest shared + typecheck/lint verzi. Verificat in preview prin DOM: 4 carduri pe layoutScreen cu bifa functionala, grila 514+330 cu rail de 669px (incape sub 720), lightbox montat pe BODY acoperind exact viewportul la scrollY=2500 cu pozitia pastrata la inchidere, viewerul 3D cu titlu/nota/chips + inchidere pe Escape; rAF pauzat in pane-ul embedded blocheaza raycast-ul si screenshot-urile (limitare cunoscuta) — logica de click e copia celei validate in T1/R7.

### Checklist acceptare U1–U4
- [ ] Bucatarie, ecranul de forma: insula e un cartonas ca celelalte, cu bifa patrata; click bifeaza/debifeaza; „i" intoarce cardul.
- [ ] Corp individual (dulap etc.) pe desktop: modelul 3D in stanga, comenzile in dreapta; click pe o sectiune → panoul ei apare langa model, totul vizibil pe un ecran; modelul ramane pe loc la scroll.
- [ ] Caietul de idei: deruleaza jos, click pe o poza → lightbox-ul apare peste ecranul curent; la inchidere esti pe acelasi rand.
- [ ] Marketplace, cerere cu corp 3D: „Vezi corpul în 3D" deschide modelul; click deschide usi/sertare; zona selectata isi arata dimensiunile; nimic nu se poate modifica.

## Sprint R7 — 2026-07-13 — feedback PO runda 3: colectii in configurator, sumar cu inspiratie, fix sertare rotite, fix adresa Ilfov
- **Colectiile utilizatorului in pickerul de inspiratie** (`inspiration-picker.tsx`): dialogul „Alege poze de inspiratie" are acum 2 taburi — „Galerie" (filtrele existente) si „Colecțiile mele" (boards-urile din caietul de idei, API existent `/inspiration/boards`): grila de colectii cu coperti + numar de poze, intrare in colectie cu buton inapoi, aceeasi selectie toggle max 10. Tabul apare doar logat (API-ul cere auth); fara colectii → empty state cu indicatie spre caietul de idei. 5 chei i18n noi RO+EN (`tabGallery/tabBoards/boardsEmpty/boardCount/backToBoards`).
- **Selectia de inspiratie vizibila in sumar** (`review-step.tsx`): sectiunea `InspirationPicker` (thumbnails + editare) apare si pe pasul „Verifica si publica", inainte de butonul de publish — pozele alese se vad si se pot ajusta fara sa te intorci la pasul de fisiere.
- **Bugfix 3D: sertarele ramaneau rotite la ~105°** dupa usa deschisa → schimbare tip in „Sertare" (fix triplu in `piece-canvas.tsx` + `configurator3d-step.tsx`): (a) grupurile-pivot ale fronturilor sunt keyed pe rol (`DOOR_FRONT-0` etc.) — React remonteaza grupul curat la schimbarea rolului in loc sa refoloseasca `Group`-ul cu rotatia veche; (b) `applyFrontTransform` reseteaza mereu rotatia inainte de a aplica transformarea rolului curent; (c) zona iese din `openZones` la ORICE schimbare de tip (inainte doar la „Deschis") — fronturile noi pornesc inchise.
- **Bugfix adresa (raportat pe Ilfov): Next blocat desi adresa era „selectata"** — cand sugestia Google e confirmata cu Enter fara a fi evidentiata cu sageata, Places intoarce un place DOAR cu `name` (fara `address_components`), judetul/orasul ramaneau goale si validarea (min 2 caractere) bloca pasul. Fix in `address-autocomplete.tsx`: fallback de geocodare (`google.maps.Geocoder`) pe textul tastat, restrans la tara aleasa → campurile se completeaza din primul rezultat. Bonus normalizare: „Județul Ilfov" → „Ilfov", „Comuna/Oraș/Municipiul X" → „X" (consistent cu „București"; county e afisaj + geocoding, nu cheie de matching — matching-ul firmelor e pe distanta).
- Fara migrari DB, fara schimbari in shared/backend. Typecheck + eslint verzi. Verificat in preview end-to-end: colectie creata → apare in tab cu „2 poze" (plural ICU), selectie din colectie → contor + thumbnail in sumar + id in store; place fara componente → Geocoder → Județ „Ilfov"/Oraș „Otopeni" si submit-ul trece la sumar; place cu componente „Județul Ilfov"+„Comuna Chiajna" → „Ilfov"/„Chiajna". Canvas-ul WebGL nu se initializeaza in pane-ul embedded (limitare cunoscuta) — pasii manuali pentru 3D: dulap → usa pe o zona → click (usa se deschide) → schimba tipul in „Sertare" → sertarele apar drepte si inchise.

### Checklist acceptare R7
- [ ] Configurator, pasul Fisiere sau Sumar → „Rasfoieste galeria" → tabul „Colecțiile mele" arata colectiile tale; poti selecta poze direct din colectie (max 10).
- [ ] Sumarul (inainte de publish) arata pozele de inspiratie alese; le poti scoate/adauga direct de acolo.
- [ ] 3D: deschide o usa (click pe zona), schimba tipul zonei in „Sertare" → sertarele apar INCHISE si DREPTE (nu mai raman pe diagonala); valabil si usa→deschis, sertar tras→usa.
- [ ] Adresa din Ilfov (ex. „Strada Erou Iancu Nicolae 12, Voluntari"): alege sugestia cu click SAU tasteaza si apasa Enter direct → Județ/Oraș se completeaza („Ilfov", fara prefixul „Județul") si „Spre sumar" trece.

## Sprint T1 — 2026-07-13 — feedback PO configurator 3D runda 2 (docs/13)
- **Birou fara casetiere**: bugfix — `normalizePieceConfig` forta minim o coloana (`|| 1`) desi `minColumns` e 0 la birou; stepper-ul de casetiere coboara acum la 0 (blat + picioare fara corpuri).
- **Culori noi + color picker**: `PIECE3D_FINISHES` extins (crem, negru, albastru, teracota) + finisaj `CUSTOM` cu `customColor` hex ales dintr-un `<input type="color">` nativ (swatch cu gradient conic); CUSTOM fara culoare valida → STEJAR la normalize, respins la publish; culoarea aleasa se pastreaza la comutarea pe finisaje predefinite. Frontul CUSTOM = corpul deschis ~10% spre alb.
- **Comoda pe picioare** (`legs`, doar DRESSER): 4 picioare conice de 12cm (rol `LEG`, cilindru in 3D) inlocuiesc soclul; baza comuna resolver+model prin `pieceBaseHeight` — interiorul si regula sertarelor <160cm urca odata cu corpul; toggle „Baza: Cu soclu / Pe picioare"; „pe picioare" in descrierea derivata + chip.
- **Maner vs push** (`frontStyle`, toate piesele; implicit push = aspectul istoric): manere de alama VIZIBILE in 3D — orizontale pe sertare/fronturi rabatabile, verticale pe usi (muchia opusa balamalei) si pe usile glisante; alegerea intra in `systems` itemului derivat (MANER/PUSH, dedup cu `openingSystems`) si in descriere.
- **Dulap cu usi glisante** (`doorMode: SLIDING`, doar WARDROBE, DOAR 2–3 coloane): o usa pe toata inaltimea per coloana, pe 2 sine alternante in fata carcasei; directia per usa (`slideTo: L/R`, capetele doar spre interior, usa din mijloc la alegere — pills „Glisare: Spre stanga/dreapta"); zonele „Usa" devin „Deschis" in spatele usilor glisante (interiorul se pastreaza); click in 3D gliseaza usa peste vecina (`slideDx`), click pe sertare cu usa deschisa le trage; +/− coloane blocate in interval cat glisarea e activa; step-ul separat `doorType` devine ASCUNS+optional (alegerea e in 3D; GLISANTE se deriva din config, fallback pe raspunsurile vechi — drafturile/cererile vechi raman valide).
- **Reguli noi PO**: front de sertar minim 15cm (`DRAWER_H_MIN`, `drawersCountFor`) si minim 10cm intre polite (`SHELF_GAP_MIN`, `shelvesCountFor` — n polite = n+1 spatii egale). Tipar triplu ca in R5.3: normalize corecteaza (count scade, zona DRAWERS prea scunda redevine deschisa, fill-ul de polite dispare), schema Zod respinge la publish pe geometria rezolvata, UI ascunde optiunile imposibile si plafoneaza stepper-ele live (ambele moduri).
- Fara migrari DB si fara bump de flow version — campuri JSON optionale, aditive; backend fara cod nou. Teste: 111/111 vitest shared (10 noi T1) + typecheck/lint verzi. Verificat in preview pe `/ro/dev/piece-3d` (config persistat: `doorMode SLIDING` + `slideTo R`, `columns: []` la birou, `legs: true`, `finish CUSTOM #7b3fa0`, `frontStyle HANDLE`); canvas-ul WebGL nu randeaza in pane-ul embedded (limitare de mediu), geometria e acoperita de testele unit.

## Sprinturi S1–S6 — 2026-07-13 — feedback PO pe formulare (docs/12) + fixuri configurator 3D
- **S1 Bucatarie**: insula e acum TOGGLE-CARD OPTIONAL pe ACELASI ecran cu cele 3 forme (`screenGroup 'layoutScreen'`, ilustratia insulei pe card; lipsa raspuns = fara insula) — inlocuieste decizia F3 „intrebare de sine statatoare"; intrebarile sunt grupate pe partea de mobilier: material baza → deschidere baza → material suspendat → deschidere suspendat → material insula → deschidere insula (id-uri neschimbate, doar ordinea ecranelor → v2 in place).
- **S2 Material vs deschidere = intrebari SEPARATE per piesa**: `pieceConfigSteps` muta sistemele de deschidere pe ecran propriu (`piece:<X>:systems`) cu titlu per piesa („Deschidere — biblioteca") — se propaga in living, dormitor, birou, hol, spalatorie, balcon; `pantry.v2` split identic la dulapuri. 27 chei i18n noi RO+EN.
- **S3 Dressing v3 + Living v3** (versiuni NOI, v2 raman FROZEN; `CURRENT_FLOW_VERSION` bump): dressing — iluminarea LED devine modul interior langa accesorii (`interiorModules.LED_LIGHTING`), intrebarea separata dispare; living — slot de INALTIME per piesa (tvUnitHeight, bookshelfHeight, …) + intrebarea `ledPieces` (multi-choice optionala „La care corpuri vrei iluminare LED?" cu optiunile FILTRATE pe piesele selectate eligibile, prin `ChoiceOption.visibleIf`), `ledLighting` global eliminat; itemul derivat al fiecarui corp bifat primeste „Cu iluminare LED".
- **S4 Limita de fisiere per CAMERA, nu per formular**: `maxFiles 3→7` pe schita din toate flow-urile curente; cap global NOU dinamic per cerere `maxAttachmentsForRequest(rooms) = 10 + 8×camere` (schite + snapshot 3D per camera + buffer cerere) — in shared, aplicat in backend la presign (numara request_rooms + roomInstances din configuratorState pe drafturi) si oglindit in FE (`RoomSketchUpload` primeste roomCount).
- **S5 Imagini sugestive pe tabul de dimensiuni** (`dimension-figures.tsx`): figura compusa front-view generalizata (`FrontRowFigure` + registre de piese) acopera acum si LIVING (cu cota de inaltime H1/H2… per piesa), DORMITOR, BIROU (vederea de sus a biroului cu brat B/C pentru L/U + randul celorlalte piese), BAIE; VALORILE completate apar LANGA litere pe desen (text cu halou), pe langa legenda de jos — la TOATE figurile (bucatarie, dressing, hol, piese ghidate etc.); `IlluDeskU` corectat (era de fapt un T).
- **S6 Configurator 3D**: (a) casetierele biroului NU se mai pot suprapune — prag de latime `2 laterale + 2×42cm + 40cm genunchi ≈ 1.28m` (`deskMaxColumns`): + blocat sub prag, `normalizePieceConfig` scoate automat a doua casetiera la ingustare, schema Zod respinge la publish; (b) optiunile indisponibile geometric (bara de haine fara adancime/inaltime, sertare peste 160cm) se ASCUND in loc de greyed-out, in ambele moduri (toolbar 3D + campuri clasice); (c) stepper-ul de coloane DISPARE complet la piesele cu `maxColumns=1` (noptiera).
- Teste: 101/101 vitest shared (5 noi: ordine bucatarie, insula toggle, dressing v3, living v3 ×2, coliziune casetiere) + 32/32 jest backend; typecheck + lint verzi. Verificat in preview: forma+insula pe ecranul 01/11, valori langa litere pe schita bucatariei, „Inca maximum 7 pentru aceasta camera (26 ramase in total)" la 2 camere, living cu A/H1/B/H2/H pe plansa si ecrane separate Material/Deschidere, ecranul LED doar cu piesele eligibile, noptiera fara stepper de coloane, biroul pierde a doua casetiera la 110cm si + se blocheaza.

## Sprint R6 — 2026-07-12 — caietul de idei populat cu portofoliul Mobila Unicat + filtre reproiectate
- **70 de poze reale din portofoliul mobilaunicat.ro** (acord primit de PO), etichetate VIZUAL poza cu poza (planse de contact din 183 de imagini descarcate): fiecare valoare de filtru are minim 3 idei (minimul: rosu/multicolor/MDF infoliat/Aventos = 3; bucatarie/living/dormitor = 15). Pozele sunt hotlink-uri pe CDN-ul lor Webflow (acelasi pattern ca seed-ul Unsplash vechi) si sunt atribuite firmei noi „Mobila Unicat" (APPROVED, fara utilizatori — nu poate loga nimeni pe ea).
- **`source_url` pe inspiration_photos** (migrare `20260712162511_inspiration_source_url` + docs/04): linkul proiectului-sursa; afisat ca buton „Vezi proiectul" in lightbox si ca icon pe pin la hover (target _blank, rel noopener); camp nou in adminul galeriei; schema Zod + DTO-uri actualizate identic FE/BE.
- **Consola de filtre reproiectata** (/design, cerinta PO — pill-urile erau „aruncate pe mijlocul paginii" pe 4 randuri): un singur card compact cu cautarea, camerele pe un rand (scroll orizontal pe mobil) si 3 chips-uri cu panouri multi-select (Culoare cu ESANTIOANE reale de nuanta + bifa, Material, Deschidere), badge cu numarul selectiilor si preview-ul culorilor pe chip, contor „N idei" si „Sterge filtrele".
- **Seed-ul de inspiratie rescris**: idempotent PER POZA (dupa imageUrl) — ruleaza peste DB-uri deja populate; DEPUBLICA pozele demo Unsplash (galeria publica ramane doar cu mobilier real; raman in DB pentru cererile care le refera).
- Verificat local: filtrul Verde → exact cele 10 idei etichetate, badge + contor + reset; lightbox cu „Vezi proiectul" si imaginea incarcata de pe CDN; matricea completa de acoperire prin API (28/28 valori de filtru cu >=3 idei). Teste: 32/32 jest + 96/96 vitest; typecheck + lint verzi.

## Sprint R5.4 — 2026-07-12 — feedback PO pe 3D: 2-3 bare de haine suprapuse per zona
- **Bare multiple** (cerinta PO): interiorul „Bara de haine" primeste `count` optional (implicit 1, `HANGING_COUNT_MAX` = 3); zona se imparte in segmente egale cu cate o bara sub varful fiecaruia. Limita geometrica setata anterior se aplica PER BARA: 80cm de inaltime pentru fiecare (`hangingCountFor(height)` in shared) — o zona de 1.6m+ tine 2 bare, 2.4m+ tine 3; adancimea minima ramane 55cm.
- Validare Zod: count la HANGING optional (compatibil cu drafturile din R5.3), respins peste 3 sau cand `count × 80cm` depaseste zona; normalizarea REDUCE automat barele cand zona se micsoreaza (3→2 la coborarea dulapului de la 2.8m la 1.8m) si le scoate sub 80cm. Stepper-ul „Numar" din UI e plafonat live de inaltimea zonei; totals/descriere numara fiecare bara („2 bare de haine").
- Teste: 96/96 vitest verzi (3 noi: schema 2/3/4 bare pe inaltimi diferite, normalize reduce count, ROD-uri la inaltimi diferite + totaluri). Verificat in preview: bara → „Numar" apare, + blocat la 2 pe zona de 1.70m, la 280cm inaltime permite 3, inapoi la 180cm reduce singur la 2; config-ul persistat corect.

## Sprint R5.3 — 2026-07-12 — feedback PO pe 3D: dimensiuni per zona cu blocare + doar 3 tipuri (usa/sertare/deschis) + reguli geometrice
- **Dimensiuni individuale cu LACAT, pe rand si pe coloana** (cerinta PO): `Piece3dZone.heightM` si `Piece3dColumn.widthM` optionale = dimensiune BLOCATA; un resolver nou in shared (`resolvePieceLayout` + `distribute`) tine blocajele fixe si imparte egal restul spatiului intre zonele/coloanele libere. Daca blocajele nu mai incap (gabarit micsorat), sunt scalate proportional LA REZOLVARE — config-ul stocat pastreaza intentia, geometria ramane mereu valida. Normalizarea lasa mereu cel putin un rand/coloana flexibile. UI: input cm + buton lacat in bara zonei (3D) si per rand/coloana in „Campuri clasice"; editarea blocheaza implicit, lacatul elibereaza.
- **Doar 3 tipuri de zona: Usa / Sertare / Deschis** (cerinta PO): `rules.zoneTypes` redus la OPEN/DRAWERS/DOOR la toate piesele; politele si bara de haine devin `fill` (INTERIORUL zonelor Usa/Deschis: gol / polite cu numar / bara). SHELVES/HANGING/TILT_OUT raman in enum doar pentru datele vechi — `normalizePieceConfig` le migreaza automat (SHELVES→OPEN+polite, HANGING→OPEN+bara, TILT_OUT→DRAWERS, DOOR+count→DOOR+polite), iar model.ts le mai randeaza pentru snapshot-urile publicate.
- **Reguli geometrice** (cerinte PO): bara de haine cere adancime >= 55cm SI zona >= 80cm inaltime (`HANGING_MIN_DEPTH`/`HANGING_MIN_ZONE_H`); sertarele nu pot avea marginea de sus peste 160cm (`DRAWERS_MAX_TOP`) — nu ajungi la ele. Schema Zod (FE/BE identic) RESPINGE incalcarile pe geometria rezolvata; normalizarea CORECTEAZA (bara dispare sub 55cm adancime, sertarele urcate redevin zona deschisa); UI-ul dezactiveaza optiunile imposibile cu tooltip explicativ.
- Teste: 93/93 vitest verzi (16 teste noi/rescrise: resolver cu blocaje, migrare legacy, reguli bara/sertare, cel putin un rand liber) + 32/32 jest backend; typecheck + lint verzi pe toate pachetele. Verificat in preview (rAF polyfill): chips „Sertare" dezactivat pe zona inalta cu tooltip, bara aplicata pe coloana → dispare automat la adancime 45cm, inaltime rand 100cm blocata → vecinul scade la 69cm, deblocare → 84/84, config-ul cu blocaje trece validarea la „Continua".

## Sprint R5.2 — 2026-07-12 — feedback PO pe 3D: randuri per coloana in modul 3D + selectie vizibila
- **Randuri (zone) editabile per coloana IN modul 3D** (cerinta PO — „pot modifica coloanele dar nu si randurile"): bara zonei active are acum stepper-ul „Randuri in coloana" (+ adauga o zona jos, − scoate zona de jos, limitat la `maxZonesPerColumn`); pana acum adaugarea de zone exista doar in modul „Campuri clasice". La stergerea unei zone selectia ramane pe coloana (zona vecina), ca bara sa nu dispara in mijlocul editarii. Hint-ul RO/EN mentioneaza randurile; cheie i18n noua `config3d.rowsInColumn`.
- **Selectia zonei mult mai vizibila** (cerinta PO — galbenul vechi nu se observa): highlight-ul trece de la alama `#b08d57` (se pierdea pe finisajul stejar) la teracota saturata `#e2662e`, cu fill mai opac (0.35 activ / 0.15 hover) + CONTUR pe muchiile cutiei zonei (EdgesGeometry, `depthTest` off — conturul se vede si prin fronturile inchise). Bara de haine ramane pe alama (`ROD_COLOR` separat de `HIGHLIGHT_COLOR`).
- Verificat in preview (polyfill rAF + analiza pixeli pe snapshot-ul PNG generat de app): stepper 2→3→2 randuri pe coloana 2 cu toolbar persistent, conturul teracota prezent in scena (≈645px contur + ≈436px tenta); typecheck + lint verzi. Fara cod nou pe shared/backend — geometria si validarea raman neschimbate.

## Sprint R5.1 — 2026-07-12 — feedback PO pe 3D (fidelitate Tylko) + dialog "cerere neterminata"
- **Dialog la intrarea pe „Cerere noua" cu draft in lucru** (cerinta PO): daca exista progres (camere/detalii/faza avansata), wizard-ul intreaba „Continui de unde ai ramas sau incepi una noua?"; „Incepe o cerere noua" arunca progresul (fara salvare), sterge tokenul si creeaza un draft NOU; „Continua" reia exact de unde a ramas. Verificat in preview: dialogul apare doar cu progres, reset complet (rooms 0, faza cart, token schimbat).
- **Interactiunea 3D refacuta** (cerinta PO — clickul NU mai cicleaza tipul zonei): click pe zona o selecteaza, iar daca are usa/sertare/fronturi rabatabile le DESCHIDE/inchide animat (usa pivoteaza pe balama ~105°, sertarul culiseaza cu tot cu cutia lui — fund+laterale+spate vizibile, frontul rabatabil cade in fata); tipul se schimba doar din bara zonei active. Animatie cu lerp in useFrame + invalidate (ramane compatibila cu frameloop="demand").
- **Usi cu polite interioare** (cerinta PO): zonele DOOR au acum `count` OPTIONAL = politele din spatele usii (0–8), editabil din bara zonei („Polite interioare") si din modul clasic; politele se vad cand deschizi usa, intra in totalul de polite din chips/descrierea derivata. Validare shared actualizata (count obligatoriu doar la SHELVES/DRAWERS/TILT_OUT).
- **Luft vizibil intre fronturi** (cerinta PO — „nu se intelege cate usi/sertare sunt"): FRONT_GAP 2mm → 4mm (≈8mm intre fronturi vecine) + panou-fundal aproape negru in spatele fronturilor (rol nou FRONT_BACKDROP) — rosturile se citesc ca linii inchise, ca la Tylko; balamalele usilor sunt acum in model (`hinge: L/R`, usa simpla pe muchia exterioara).
- Teste: 78/78 vitest verzi (4 teste noi: DOOR count optional, polite interioare in panouri+totaluri, balamale, backdrop); typecheck + lint verzi. Verificat vizual in preview (exporturi PNG): usa dubla deschisa cu 3 polite interioare vizibile, comoda cu sertarele trase si rosturi lizibile.

## Sprint R3+R4+R5 — 2026-07-12 — configurator 3D integrat in cerere: flow-uri v3, snapshot PNG, toate piesele-carcasa (docs/10, aprobat PO)
- **Flow-uri v3** (`guided-pieces/v3.ts`) pentru TOATE cele 7 piese-carcasa: biblioteca, dulap, comoda TV, pantofar, comoda, noptiere, birou — step-ul `configurator-3d` inlocuieste `dimensions` + configuratia interioara (style/interiorModules/storage); raman intrebarile ortogonale (material cu „Altul", doorType la dulap → GLISANTE, tvSetup, cableManagement, una/pereche la noptiere) si schita. Pat/masa/bancuta nu sunt corpuri → raman pe v2. v1/v2 FROZEN in registru; cererile vechi se valideaza contra versiunii lor.
- **Derivare**: `request_items` primesc dimensiunile exacte din config (lengthM=latime×cantitate, widthM=adancime, heightM=inaltime) + descriere generata („Configurat 3D: 200 x 200 x 35 cm, 2 coloane, 4 polite, 2 sertare, 2 usi, finisaj stejar") — atelierele vad EXACT aceeasi structura de cerere ca azi; scoringul WARDROBE_TO_CEILING se pastreaza prin inaltimea config-ului (≥2.5m), prin noul hook `scoreEntries` pe step.
- **Snapshot PNG la publish (D-3D-4)**: capturat debounced din scena (gl.render direct), tinut DOAR in memorie (nu in localStorage/backup server), urcat AUTOMAT la publish prin fluxul presigned existent (3.4) si legat de camera prin step-ul upload ASCUNS `snapshot3d` (validat uuid + ownership pe server prin mecanismul existent — zero cod nou pe backend). Esecul de upload nu blocheaza publicarea.
- **Prezentare**: `RoomSpecCard` afiseaza poza corpului + chips-urile config-ului („160 × 200 × 35 cm · 2 coloane · 4 polițe…", RO/EN cu plural ICU); intrarea noua `config3d` in `summarizeAnswers` alimenteaza si Review-ul; snapshot-ul e vizibil unde atasamentele sunt deja vizibile (client + firma post-claim) — pre-claim firma vede chips-urile si descrierea derivata (invarianta 4.2 pe fisiere ramane intacta).
- **Backend**: NEATINS — `ConfiguratorService` valideaza/deriva prin shared (flowVersion 3 inregistrat), `collectUploadAttachmentIds` prinde `snapshot3d` fiind step `upload`.
- **Teste**: 9 teste noi v3 (validare publish, derive, scoring, rezumat fara snapshot3d) + testele existente actualizate la v3 → **74/74 verzi**; typecheck verde pe toate pachetele.
- Verificat e2e pe backend real: login client demo → draft → presign+PUT MinIO+confirm PNG (SAFE) → publish flow v3 → `GET /requests/:id` cu flowVersion 3, gabarit 2×0.35×2m, descriere completa cu „Material dorit: bambus", snapshot cu downloadUrl. Verificat in wizard: piesa Biblioteca intra pe „Construiește-ți biblioteca în 3D" (ecran 01/04), modul „Câmpuri clasice" editeaza acelasi config; dulapul (bara+sertare, 3 coloane la 240cm) randat corect.
- Limitare cunoscuta: pane-ul de preview headless nu ruleaza rAF → QA-ul vizual pe telefon ramane la PO (ca la ideea 8), impreuna cu decizia de a porni R6 (compuneri, pret orientativ, AR).

## Sprint R1+R2 — 2026-07-12 — configurator 3D: fundatia + compartimentarea interactiva (docs/10, aprobat PO)
- **Dependinte**: `three@0.169` + `@react-three/fiber@8` + `@react-three/drei@9` (React 18); incarcate DOAR pe step-ul 3D prin primul `next/dynamic ssr:false` din aplicatie (`piece3d/dynamic.tsx`) — restul paginilor nu ating bundle-ul three (~160KB gzip).
- **Model parametric PUR in shared** (`questionnaire/piece3d/`): `config.ts` (tipuri + `PIECE3D_RULES` per piesa + `pieceConfig3dSchema(kind)` Zod validata identic FE/BE + `normalizePieceConfig` + `describePieceConfig` RO) si `model.ts` (`buildPanels(config) → Panel3d[]`: carcasa 2 laterale/sus/jos/spate/soclu, despartitoare, separatoare de zona, polite, fronturi sertar/usa/rabatabile cu gap 2mm, bara de haine; `buildZoneBoxes` = hit-target-uri pentru click). 20 de teste vitest noi (65/65 verzi).
- **Zone**: OPEN/SHELVES/DRAWERS/DOOR din docs/10 + HANGING (dulap) si TILT_OUT (pantofar) — inlocuiesc informatia din vechiul interiorModules; usa dubla automat peste ~65cm de coloana; coloanele derivate din latime (~80cm/coloana, ±1 utilizator, latimi valide 22cm–1m).
- **`PieceCanvas`** (client-only): camera cu orbita limitata (nu vezi sub podea), lumini soft cu umbra pe podea discreta, `dpr≤1.75` + `frameloop="demand"` pentru mobil; repozitionare camera doar la schimbari de gabarit; `SnapshotBridge` expune export PNG prin `gl.render()` direct (nu depinde de preserveDrawingBuffer).
- **`Configurator3dStepUI`**: slidere gabarit (cm) legate LIVE de model, click/tap pe zona → selectie + al doilea click cicleaza tipul, toolbar zona activa (pill-uri tip + numar ±), coloane ±, paleta 5 finisaje, reset; **modul „Campuri clasice"** — acelasi `PieceConfig3d`, editat prin campuri (D-3D-2: coexistenta; fallback automat fara WebGL).
- **Pagina de dev** `/[locale]/dev/piece-3d` (404 in productie) pentru iterat vizual pe toate cele 7 piese.
- Verificat in preview: biblioteca 2 coloane randata corect (PNG exportat prin pipeline-ul de snapshot), slider latime 160→200cm actualizeaza modelul si normalizarea coloanelor, click pe zona schimba SHELVES→DRAWERS server... client-side si toolbarul apare. Nota: pane-ul de preview ruleaza ascuns (rAF pauzat) — scena a fost validata cu polyfill rAF + export PNG; QA vizual final ramane pe browser vizibil (R5).

## Sprint Q6 — 2026-07-12 — jest backend + 32 teste pe serviciile critice (D3 aprobat PO)
- **Jest instalat** (`jest@29`, `ts-jest`, `@types/jest`, `@nestjs/testing`) + `jest.config.cjs` (ts-jest pe tsconfig-ul backend, `@marketplace/shared` mapat la surse) — `pnpm -F backend test` functioneaza in sfarsit.
- **32 de teste unitare** (dependinte mock-uite, fara DB/Redis/MinIO): `credits.service.spec` (RESERVE/REFUND/CONSUME + INSUFFICIENT_CREDITS 402 — inima claim-ului pay-to-play), `claims.helpers.spec` (haversine, recalcul status cerere 3.1, republicare dupa ratare SLA in masa 4.11 cu excluderi), `uploads.service.spec` (FILE_LIMIT_REACHED, cheie sanitizata, scan mock malware→BLOCKED fara URL de download, relink cu FILE_SCAN_BLOCKED/NOT_FOUND), `notification-emails.service.spec` (HMAC dezabonare + selectia destinatarilor per eveniment), `business-calendar.service.spec` (weekend, sarbatori fixe, override admin, sfarsit de zi 23:59 Bucuresti).
- **BUG REAL prins de teste si reparat**: `BusinessCalendarService.tzOffsetMs` pierdea milisecundele (formatToParts nu le expune) → toate deadline-urile de sfarsit de zi (expirare cerere, SLA) cadeau cu ~1 secunda DUPA miezul noptii (00:00:00.997 ziua urmatoare in loc de 23:59:59.999). Referinta de timp e acum trunchiata la secunda.
- Verificat: 32/32 jest + 45/45 vitest (shared) + typecheck verzi.

## Sprint Q5 — 2026-07-12 — paginare galerie (idee 6) + limita 25MB (D2 aprobat PO)
- **API `/inspiration` cu `limit`/`offset`** (DTO cu coerce + Min/Max; fara parametri comportamentul vechi ramane — cap 200) si sortare stabila (`featured desc, createdAt desc, id desc` — id-ul sparge egalitatile ca paginile sa nu se suprapuna).
- **Galeria pe `useInfiniteQuery`**: pagini de 40, sentinel IntersectionObserver (rootMargin 600px) + buton „Mai multe idei” ca fallback (util si pe device-uri fara IO activ); cautarea text ramane client-side pe pin-urile incarcate; lightbox-ul navigheaza peste lista plata.
- **D2**: `MAX_ATTACHMENT_BYTES` 10MB → **25MB** (aliniat la invarianta 3.4) + `dropHint` RO/EN. Verificat live: presign 20MB → 201, 26MB → 400 (`max 26214400`).
- Verificat in preview: pagini de 5 fara duplicate (limit=5), butonul nu apare cand toata galeria incape intr-o pagina.

## Sprint Q4 — 2026-07-12 — emailuri de notificare cu dezabonare (idee 5 aprobata PO)
- **Prisma**: `users.email_notifications_enabled BOOLEAN DEFAULT TRUE` (migratie `user_email_notifications_pref`); docs/04 actualizat.
- **`NotificationEmailsService`** in modulul notifications, apelat din `NotificationsProcessor` (worker BullMQ — emailul nu blocheaza requestul; esecul SMTP e doar logat ca jobul sa nu se re-execute si sa dubleze notificarile in-app): trimite DOAR pentru cele 3 evenimente aprobate — `quote.created` si `claim.created` catre CLIENT (payload-urile emit acum `clientUserId`), `message.created` catre partea CEALALTA a conversatiei (clientul primeste cand scrie firma; membrii firmei cand scrie clientul — fara expeditor). Filtre: cont sters, email neverificat, opt-out.
- **Continut**: subiect + text in limba contului (`language_preference`), CTA cu deep-link (client → `/requests/{id}/offers`, firma → `/marketplace/claims`), footer cu **link de dezabonare semnat HMAC-SHA256** (fara login, `timingSafeEqual`).
- **Endpoint-uri**: `GET/PATCH /notifications/email-preference` (toggle „Primeste si pe email” in clopotel) + `POST /notifications/unsubscribe` (@Public, uid+sig) + pagina `/[locale]/unsubscribe` (apel direct idempotent — starea useMutation se pierdea la remount-ul StrictMode si pagina ramanea pe „Te dezabonam…”).
- Verificat e2e in Mailpit: mesaj de la owner.b → UN email doar catre radu cu „Salut Radu”, CTA corect si link semnat; dupa dezabonare mesajul urmator NU mai genereaza email; pagina de dezabonare confirma; toggle-ul reactiveaza (`enabled: false → true`).

## Sprint Q3 — 2026-07-12 — filtre „Cererile mele” + plansa de dimensiuni la Hol (idei 2 si 7 aprobate PO)
- **Taburi pe „Cererile mele”**: Toate / Active (DRAFT + tot ce aduna oferte) / In lucru (ACCEPTED→DELIVERED + DISPUTED) / Finalizate / Expirate, cu numaratori pe fiecare pill; filtrare client-side (lista vine oricum integral); stare goala dedicata per tab. Verificat: 7 cereri → Active 5 / In lucru 1 / Expirate 1, cardul expirat poarta badge-ul „Expirată”.
- **`HallwayFrontFigure`** in `dimension-figures.tsx` (idee 7): vedere frontala COMPUSA — doar piesele selectate, una langa alta pe linia podelei (pantofar cu fronturi rabatabile, cuier cu polita si carlige, dulap cu usi, bancuta cu lada, oglinda pe perete), fiecare cu litera latimii ei (A/B/C… alocate dinamic pe sloturile prezente) + etalonul H doar cand exista piesa inalta (dulap/cuier). Verificat in wizard: pantofar+cuier+bancuta → plansa cu A/B/C/H si chips-uri identice pe campuri.

## Sprint Q2 — 2026-07-12 — necitite in conversatii (idee 1 aprobata PO)
- **Prisma**: tabela `chat_thread_reads` (PK compus thread+user, `last_read_at` TIMESTAMPTZ, cascade, index user) — un rand per utilizator per conversatie, mai ieftin decat un rand per mesaj, acelasi rezultat; docs/04 actualizat.
- **Backend**: `ChatThreadDto.unreadCount` calculat cu O SINGURA interogare SQL pe toata lista (LEFT JOIN pe reads; necitit = mesajele ALTORA peste `last_read_at`); `POST /chat|/company/chat/threads/:id/read` (upsert); lista firmei primeste acum si userId (necititul e per utilizator, nu per firma).
- **Frontend**: punct rosu + „N mesaje noi” pe sectiunile atelierelor din „Oferte si conversatii”; badge rosu cu numarul pe tabelul „Revendicarile mele” al firmei (+ sincronizare realtime); `ChatPanel` marcheaza citit cand chatul INTRA IN VIEWPORT (IntersectionObserver — sectiunile nederulate raman necitite) sau la orice interactiune (fallback pentru tab-uri ascunse, unde IO nu emite).
- Verificat e2e: mesaj de la owner.b → radu vede unread=1 doar pe threadul respectiv + punct rosu pe sectiunea B DesignWood; interactiunea cu chatul → 1→0 server-side si punctul dispare; cererea cealalta ramane necitita.

## Sprint Q1 — 2026-07-12 — BUGFIX salvarea Pinterest + lightbox complet + „Muta in…” (bug PO + idei 3 si 4 aprobate)
- **CAUZA bugului raportat de PO** („Salvat” nu apare pana la refresh): `api()` facea NECONDITIONAT `res.json()`, iar endpoint-urile void (salvare pin, scoatere, redenumire/stergere colectie) raspund `201` cu body GOL → `res.json()` arunca, mutatia parea esuata si `onSuccess`/invalidarea nu rulau, desi salvarea REUSEA in DB. Fix in `lib/api.ts`: raspunsurile 204/goale se intorc ca `undefined`.
- **Optimistic updates** (invarianta 3.6) pe salvare/scoatere/mutare: starea „Salvat” se schimba INSTANT din `onMutate` pe cache-ul `['inspiration-boards','saved']`, cu rollback la eroare si invalidare la settle.
- **Lightbox Pinterest** (`inspiration-lightbox.tsx`, idee 3): buton Salveaza in lightbox, navigare ←/→ (butoane desktop, sageti mici pe mobil, tastatura + Escape), „Idei asemanatoare” (scor: aceeasi camera +2, materiale/culori comune +1, din lista deja filtrata) cu click-through.
- **„Muta in…”** (idee 4): endpoint atomic `POST /inspiration/boards/:id/items/:photoId/move` (tranzactie: delete sursa + upsert destinatie + bump updatedAt; ambele colectii verificate ca ale utilizatorului); click pe „Salvat” deschide meniul „Muta in colectia… / Scoate din colectie” (colectia curenta exclusa; creare inline pastrata); buton „Muta” si pe pagina colectiei. `BoardPicker` si `SaveButton` extrase ca componente partajate (pin + lightbox + colectie).
- Verificat e2e in preview: click Salveaza → „Salvat” instant + savedRefs 6→7 server-side FARA refresh; mutare din galerie si din colectie (numaratorile se muta corect); lightbox cu navigare, similare si Escape; zero erori in consola.

## Sprint P8 — 2026-07-11 — verificare integrala + TODO pentru acordul PO (feedback PO r2: item 15)
- **Verificare integrala**: typecheck + lint + teste 45/45 verzi; build de productie frontend compilat cu 51/51 pagini generate (faza standalone-symlink pica doar local pe Windows fara drepturi de symlink — in Docker trece); e2e publicare pe flow v2 nou (Balcon: ALTUL cu text liber ajunge in item description, spec-card complet pe detaliu, coverage filtering corect — firma din Bucuresti nu vede cererea din Iasi); e2e mesaje/notificari si colectii re-verificate.
- **`docs/09-po-review-todo.md`** (livrabilul item 15): 6 decizii care asteapta PO (D1 numele "Caietul de idei", D2 limita 10→25MB, D3 jest backend, D4 AV real, D5 re-seed notificari pe Railway, D6 cheia Google Places) + 8 idei de imbunatatire propuse (read-tracking conversatii, filtre cereri, lightbox cu save si navigare, mutare intre colectii, emailuri de notificare, paginare galerie, plansa hol, QA vizual pe mobil) — NIMIC din lista nu se implementeaza fara acordul explicit al PO.

## Sprint P7 — 2026-07-11 — audit complet consola admin (feedback PO r2: item 9)
- **Audit e2e ca admin@demo.ro pe toate cele 13 endpoint-uri + 9 pagini**: Tablou de bord (KPI live: 15 revendicari active, 1 disputa, breakdown firme/cereri), Firme (lista 8 + detaliu cu semnale de risc; Aproba/Respinge prezente pe firmele PENDING), Dispute (Rezolva/Respinge pe disputa deschisa), Plati (stare goala corecta), Retrageri (stare goala corecta), Audit (paginare, 61 intrari), Setari (19 setari sistem + reguli penalizare + planuri + pachete credite + praguri, 52 de inputuri), Joburi (fara joburi esuate), Caietul de idei admin (11 poze, form cu 8 ateliere).
- **Caile de scriere verificate live**: PUT /admin/settings/eur_ron_rate → 200, iar intrarea SETTING_UPDATED a aparut instant in Audit (interceptorul @Audit functioneaza); valoarea restaurata.
- **Concluzie: nimic stricat de rework-uri** — zero erori de consola pe paginile admin, toate datele se incarca. Nu au fost necesare reparatii.
- Ramase pe lista de decizii PO (P8): jest neinstalat in backend (scriptul `pnpm -F backend test` pica); limita 10MB/fisier in cod vs 25MB in invarianta 3.4.

## Sprint P6 — 2026-07-11 — "Caietul de idei" cu colectii stil Pinterest + navbar + romana implicita (feedback PO r2: itemele 8, 13, 14)
- **DB (migratie `inspiration_boards`)**: tabelele `inspiration_boards` (id, user_id FK cascade, name UNIQUE per user, created/updated_at) si `inspiration_board_items` (board_id, photo_id, PK compus, cascade) — colectiile de salvari ale utilizatorului; fara PII proprii → hard delete. docs/04 actualizat.
- **Backend `inspiration/boards`** (guard global JWT, orice rol): GET / (colectiile mele cu itemsCount + colaj coperta 3 imagini, servite cu aceleasi reguli presigned), GET /saved (toate salvarile — starea "Salvat" per pin), GET/:id (detaliu cu pozele), POST (creare; P2002 → `BOARD_NAME_TAKEN` nou in ERROR_CODES), PATCH/:id (redenumire), DELETE/:id, POST/:id/items (salvare idempotenta, doar poze publicate — assertSelectable; updatedAt reordoneaza colectia), DELETE/:id/items/:photoId.
- **Galeria devine Pinterest**: pin-urile au buton "Salveaza" rosu pe hover (dreapta-sus) cu picker de colectii + creare inline; pin salvat arata "Salvat" (negru) si un click il scoate; click pe imagine deschide lightbox cu meta; link "Colectiile mele" in antet; neautentificat → login cu redirect. Pagini noi: `/inspiration/boards` (grila colectii cu coperta-colaj 1+2, stare goala prietenoasa) si `/inspiration/boards/[id]` (masonry cu scoatere per pin, redenumire inline, stergere cu confirmare). Filtrele existente (tip/culoare/material/deschidere, F6) raman neatinse.
- **Navbar public reordonat (item 13)**: Caietul de idei → Ghid schita → Parteneri (header + landing). "Inspiratie" REDENUMITA "**Caietul de idei**" (EN "The Idea Book") — nume de marketing in limbajul ATELIER al brandului; titlul paginii si textele aliniate. NOTA PO: numele e o propunere — e o cheie i18n, se schimba in 2 minute daca vrei alta varianta.
- **Romana implicita pe Railway (item 14)**: `localeDetection: false` in next-intl routing — `/` merge MEREU pe `/ro`, indiferent de limba browserului; engleza ramane pe comutatorul RO/EN. Verificat cu Accept-Language en → 307 spre /ro.
- **Verificat e2e**: creare colectie → salvare poza → lista cu coperta (1 salvare) → saved refs → nume duplicat respins cu BOARD_NAME_TAKEN; in UI: 10 pin-uri cu "Salveaza", 1 cu "Salvat" (salvarea din API), pagina colectiilor cu "Bucataria visurilor / o salvare", nav in ordinea noua. Typecheck + lint verzi.

## Sprint P5 — 2026-07-11 — prezentare structurata a cererii + "Cererile mele" pe carduri (feedback PO r2: itemele 6, 12)
- **`RoomSpecCard` + `RoomSpecNav`** (component nou partajat): camera se prezinta pe sectiuni scanabile in loc de lista plata Q→A — antet cu iconita camerei + gabarit (L×W×H), "chips" de configuratie (intrebare: raspuns, fara intrebarile de material — acelea sunt reprezentate de corpuri), sectiunea "Corpuri si materiale" (items derivate cu badge de material + sisteme + description), dimensiunile exacte, notele libere, fisierele camerei cu thumbnails. La 3+ camere apare bara sticky de navigare cu ancore per camera. Q→A complet ramane DOAR in pasul Review al wizard-ului.
- **Aplicat pe ambele detalii**: `/marketplace/[id]` (firma) si `/requests/[id]` (client); fallback-ul legacy (cereri seed fara answers) arata curat doar corpurile.
- **"Cererile mele" rescris pe carduri** in limbajul marketplace-ului: titlu serif + StatusBadge, chips de camere cu iconite (max 4 + "+n"), oras + varsta relativa, pulsul cererii (ateliere active / oferte primite, colorate cand exista), CTA contextual ("Deschide conversatiile" cand exista activitate), stare goala prietenoasa cu CTA de creare.
- **`RequestListItemDto` imbogatit** (BE+shared): `roomTypes[]`, `activeClaims` (ACTIVE+OFFER_SENT), `quotesCount` (fara DRAFT) — un singur findMany cu include.
- **`ROOM_ICONS` extras in `lib/room-icons.ts`** (partajat de cos, carduri si spec-card).
- Verificat in preview ca radu.stanescu: lista pe carduri ("3 ateliere / nicio oferta", "Deschide conversatiile"), detaliul cu spec-card ("01 Dressing / 3 × 2.5 × 2.6 m / Corpuri si materiale: 3× Corp principal — PAL · Push"). Typecheck + lint verzi.

## Sprint P4 — 2026-07-11 — parcursul cererii per atelier + notificari reparate (feedback PO r2: itemele 4, 5)
- **CAUZA item 4 gasita**: pagina clientului de oferte itera `quotes.map(...)` si randa chat-ul DOAR sub oferte → mesajele firmelor care au dat claim FARA oferta (exact scenariul owner.b) erau complet invizibile.
- **Pagina "Oferte si conversatii" rescrisa** (`/requests/[id]/offers`): o sectiune per ATELIER care a preluat cererea — antet cu numele firmei, momentul preluarii si statusul parcursului (In discutie / Oferta primita / Oferta acceptata / Conversatie inchisa — inclusiv claim retras/anulat), preview ultimul mesaj, apoi oferta (daca exista) si chat-ul intotdeauna. Butonul de pe detaliul cererii devine "Vezi ofertele si conversatiile" si apare si post-acceptare (DELIVERED/COMPLETED/DISPUTED).
- **`ChatThreadDto` imbogatit** (BE+shared): `claimStatus` + `lastMessage {body, senderRole, createdAt}` (un singur include suplimentar, ultimul mesaj per thread).
- **CAUZA item 5 gasita**: (a) seed-ul demo crea 20 de notificari NECITITE cu payload `{demo:true}` — pareau notificari reale duplicate si "nu se puteau citi" din UI; (b) notificarile reale nu aveau titlu, context sau link (doar tipul brut); (c) dropdown-ul nu se inchidea la click in afara.
- **Notificari end-to-end**: payload-urile evenimentelor imbogatite cu context afisabil — `message.created` (+senderRole, requestId, requestTitle, companyName), `quote.created/updated/accepted` (+requestTitle, companyName), `claim.created` devine TINTIT (client + membrii firmei; inainte era doar broadcast si clientul nu afla niciodata ca un atelier i-a preluat cererea) cu acelasi context. Marketplace-ul celorlalte firme ramane sincronizat prin `request.status_changed` (broadcast, neatins).
- **Clopotel rescris**: titlu per tip + linie de context "Atelier · Cerere", timp relativ, iconita per tip, click = marcheaza citit + navigheaza la resursa (client → /requests/{id}/offers; firma → /marketplace/claims), inchidere la click-in-afara/Escape, "Marcheaza toate citite", stare goala prietenoasa. `use-socket` invalideaza acum si pe `claim.created`/`request.status_changed`.
- **Seed**: notificarile de decor se nasc CITITE (cele necitite vin doar din actiuni live); DB-ul local curatat la fel.
- **Verificat e2e**: owner.b trimite mesaj pe thread-ul lui radu → notificare "Mesaj nou / B DesignWood · DRESSING Iasi" (unread=1), click → navigare la pagina de oferte + unread=0 server-side; pagina arata toate cele 3 ateliere cu "In discutie" si mesajul lui Florin B vizibil in chat. Typecheck/lint/teste 45/45 verzi.
- Nota: `pnpm -F backend test` e configurat cu jest dar jest nu e instalat (gap pre-existent) — pe lista de decizii P8.

## Sprint P3 — 2026-07-11 — ilustratii dedicate pentru toate optiunile formularului (feedback PO r2: item 2)
- **~75 de desene SVG noi**, in acelasi limbaj "schita de atelier" ca cele existente (viewBox 120×90, currentColor, stroke rotunjit), organizate in 3 fisiere noi:
  - `illustrations/materials-systems.tsx`: MDF infoliat (folia ridicata la colt), MDF vopsit (pensula), MDF furnir (foaia de furnir), "Altul" (eticheta cu creion); sisteme: maner clasic, profil Gola (sant sub blat), Aventos (front ridicat cu brat); blat HPL.
  - `illustrations/room-pieces.tsx`: piesele si configuratiile camerelor — living (comoda TV, biblioteca, vitrina, masuta, rafturi suspendate, alta piesa, TV suspendat/pe podea/perete media, banda LED), dormitor (dulap, pat, noptiera cu veioza, comoda, masuta toaleta, marimi pat 90/140/160/180/custom vazute de sus cu perne, depozitare pat: fara/lada rabatabila/sertare, tablie tapitata, 1 vs 2 noptiere), birou (birou cu monitor, dulap, forme drept/L/U vazute de sus cu scaun), hol (pantofar cu pantof, cuier cu haina, oglinda cu polita, bancuta), spalatorie (masina, coloana, alaturate sub blat, dulap incastrare, depozitare cu detergent/cos, blat cu rufe, corp cuva), debara (rafturi cu borcane, dulapuri, mixt), balcon (inchis cu termopan, deschis cu parapet si soare), dressing (walk-in vazut de sus, bara cu umerase, suport pantofi, tava accesorii, iluminare LED).
  - `illustrations/piece-configs.tsx`: piese ghidate — usi glisante/batante (dulap cu arc de deschidere), pana in tavan, comoda TV joasa/perete media, TV pe perete/pe comoda/nehotarat (cu semnul intrebarii), biblioteca deschisa/cu dulapuri/cu usi sticla, corp sertare si etajera birou, canal cabluri, masa dining/consola, forme dreptunghiulara/rotunda/extensibila (vedere de sus cu scaune), pantofar slim/standard/cu bancuta, noptiere (sertare/raft/suspendata), bancuta (lada/pantofi/simpla), sezut tapitat.
- **Registry complet** in `illustrations/index.tsx`: toate cele 20 de camere/piese au harta de step-uri; materialele si sistemele noi intra in COMMON_BY_VALUE (valabile in orice camera). Reutilizare deliberata: layout bucatarie ↔ pereti debara/dressing, ventilatie baie ↔ spalatorie, dulap generic ↔ dormitor/hol/balcon.
- **Boolean cu concept**: cardul "Da" al intrebarilor boolean foloseste desenul conceptului cand exista (banda LED, tablie tapitata, pana in tavan, canal cabluri, iluminare dressing), nu bifa generica.
- Verificat in preview pe dormitor (6/6 piese cu desen dedicat, 5/5 marimi de pat) — zero fallback-uri lucide pe ecranele testate. Typecheck + lint verzi.

## Sprint P2 — 2026-07-11 — UX configurator: scroll mobil, planse de dimensiuni, ghid schita, fisiere in sumar (feedback PO r2: itemele 7, 11, 3, 10)
- **Scroll la intrebare (item 7)**: la schimbarea ecranului/camerei, runner-ul face scrollIntoView pe titlul intrebarii (`scroll-mt-20` pentru headerul sticky); la schimbarea fazei wizard-ului pagina revine sus. Pe mobil nu mai ramai cu scroll-ul in lista de optiuni a intrebarii anterioare.
- **BUGFIX real gasit la verificare**: `AnimatePresence mode="wait"` bloca DEFINITIV trecerea la urmatoarea intrebare cand tab-ul devenea ascuns mid-tranzitie (rAF pauzat → exit-ul nu se termina → ecranul urmator nu se monta). Scos exit-ul; ramane doar animatia de intrare directionala.
- **Planse de dimensiuni noi (item 11)** — `dimension-figures.tsx` inlocuieste diagramele izometrice generate: planse statice desenate manual (stil plansa de arhitect: pereti dubli, corpuri hasurate, cote cu sageti si linii de extensie), cu DOAR litere in insigne (A/B/C/H/L/D) in desen; valorile stau in legenda HTML de sub figura si pe campuri (chip cu litera) — nu se mai pot suprapune cu desenul si raman lizibile la orice latime. Acoperire: bucatarie (plan per layout + insula + etalon H ca sectiune), dressing/debara/spalatorie (plan laturi), balcon (plan cu parapet), birou si masa (vedere de sus), 7 piese cu vedere frontala (dulap/biblioteca/comoda/comoda TV/pantofar/noptiera/bancuta). Eliminat sufixele redundante "(H)/(L)/(D)" din etichetele i18n.
- **Ghid schita in pasul de upload (item 3)**: componenta rescrisa in limbajul plansei publice /sketch-guide — numerale romane I–IV, titluri serif, aceleasi texte editoriale (titlu/corp/sfat din SketchGuidePage, un singur continut), vignete redesenate + link "Ghidul complet".
- **Fisiere in sumar + statusuri (item 10)**: `RoomAnswerSummary` randeaza acum pasii upload (inainte ii sarea complet): thumbnail-uri reale pentru imagini (presigned URL, click-through), iconita de tip pentru PDF/ZIP, chip rosu "Respins" pentru BLOCKED; fara atasamente disponibile → "N fisiere atasate". Review-ul primeste atasamentele draftului; pagina de detaliu client foloseste aceleasi preview-uri (statusul brut "SAFE" nu se mai afiseaza nicaieri — doar eroarea).
- **ZIP acceptat (invarianta 3.4)**: `ALLOWED_ATTACHMENT_MIME` + `application/zip`/`x-zip-compressed` (partajat FE/BE), `ATTACHMENT_ACCEPT` si helperul `attachmentKind` in shared; dropzone-urile il folosesc; ZIP fara preview (untrusted).
- **Scan mock aliniat la invarianta**: confirm marcheaza BLOCKED cand numele contine "malware" (inainte totul devenea SAFE — calea de eroare nu exista); publish respinge cu `FILE_SCAN_BLOCKED` cererile care refera schite blocate (mesaj localizat in configurator).
- **Verificat e2e in preview**: bucatarie L+insula → plansa cu insigne A/B/H/L/D si legenda live "A = 2.8 m · ..."; upload PNG real prin MinIO → rand fara status; "plan-malware.pdf" → rand rosu cu "Fisier respins la verificare"; review cu thumbnail-ul pozei + chip Respins. Typecheck/lint/teste 45/45 verzi.

## Sprint P1 — 2026-07-11 — toate flow-urile aliniate la modelul bucatariei v2 (feedback PO r2: item 1)
- **Audit complet al celor 21 de flow-uri** contra modelului bucatariei v2 (config → dimensiuni → material per zona cu "Altul" text liber → sisteme de deschidere per zona → extra → upload schita): hallway v1 si camerele v2 (dressing/living/office/bedroom/bathroom) erau deja conforme; PANTRY/LAUNDRY/BALCONY foloseau setul legacy de materiale (PAL/MDF/lemn) cu material comun pe camera si fara sisteme; 7 piese ghidate ofereau "Altul" fara camp de text; tv-unit/bookcase/dresser colectau textul dar il pierdeau la derivare.
- **`pantry.v2.ts`**: material per zona (rafturi deschise vs dulapuri inchise, vizibile dupa `storageStyle`), "Altul" cu text pe acelasi ecran, sisteme de deschidere DOAR la dulapuri; items derivate cu materialul/sistemele zonei.
- **`laundry.v2.ts`**: material + "Altul" + sisteme PER PIESA (dulap incastrare / depozitare / corp cuva) prin `pieceConfigSteps`; blatul are doar material; MDF infoliat "Recomandat" cand incaperea nu e ventilata (aliniat cu baia v2; textul info corectat din "MDF vopsit").
- **`balcony.v2.ts`**: material + "Altul" + sisteme per piesa (banca cu lada / dulap); blat si rafturi doar material; lemn masiv "Recomandat" pe balcon deschis.
- **`guided-pieces/v2.ts`** + `pieceFlowV2` in builder: toate cele 10 piese ghidate la v2 — insereaza `otherMaterialStep` dupa intrebarea de material (daca lipsea) si propaga textul "Altul" in `description`-ul itemelor derivate (repara pierderea de date la tv-unit/bookcase/dresser).
- **Versionare FROZEN respectata**: v1 raman inregistrate neatinse in `FLOW_REGISTRY`; `CURRENT_FLOW_VERSION` bump la 2 pentru PANTRY/LAUNDRY/BALCONY + cele 10 PIECE_*; drafturile in curs raman pe versiunea lor, camerele noi pornesc pe v2. Helper nou `withRecommendedMaterial` in per-piece.ts.
- **i18n ro+en**: chei noi pentru titlurile de material per piesa (PANTRY 2, LAUNDRY 4, BALCONY 4).
- **Teste**: 45/45 (6 noi: pantry/laundry/balcony v2 vizibilitate+derivare+ALTUL obligatoriu, piese ghidate v2, compat FROZEN v1). Typecheck FE/BE/shared verde.
- **Infra**: `.eslintrc.json` nou in frontend (next/core-web-vitals) + eslint instalat — `pnpm lint` era interactiv/picat de la inceputul repo-ului; acum trece curat.

## Deploy MVP — 2026-07-08 — LIVE pe Railway + pregatire productie + security review
- **LIVE**: aplicatia ruleaza pe https://backend-production-2813.up.railway.app (proiect Railway `cozy-fable`: app combinat frontend+backend via `Dockerfile.app` + Postgres + Redis + MinIO cu volum + Mailpit cu basic auth). Verificat pe productie: health `{db,redis,storage: up}` prin proxy, landing 200, register→email Mailpit→verify→login cookies httpOnly→me, login admin/client demo, presigned PUT/GET pe MinIO public, seed-uri complete (22 useri / 8 firme / 15 cereri / 8 oferte / 11 poze inspiratie). Gotcha-uri Railway documentate in docs/08-deployment.md.
- **Security review (skill /security-review pe diff-ul branchului, 159 fisiere)**: 0 vulnerabilitati HIGH/MEDIUM in codul nou. Verificate: guards pe toate endpointurile noi (admin inspiration, estimate public, quote room prices), IDOR (findOwnedRequest/requireMembership/loadCompanyQuote), $queryRaw parametrizat, path traversal storage (chei server-side), XSS (zero dangerouslySetInnerHTML; PDF-urile escapeaza), questionnaire engine respinge chei necunoscute (__proto__) si re-deriva scorurile server-side.
- **Hardening deploy public**: `seed-demo.ts` citeste `DEMO_PASSWORD`/`DEMO_ADMIN_PASSWORD` din env (adminul nu mai are parola cunoscuta din repo); `MailService` suporta SMTP autentificat (`SMTP_USER/PASS/SECURE` optionale in config schema); `REDIS_PASSWORD` optional + `family: 0` (dual-stack) in toate cele 3 puncte de conexiune Redis (client, BullMQ, adapter Socket.IO); `trust proxy` in main.ts (throttling per-IP corect dupa edge proxy).
- **Same-origin in productie**: rewrites in `next.config.mjs` (`/api/v1` si `/socket.io` → `BACKEND_INTERNAL_URL`) — cookies SameSite=Lax raman valide, fara CORS cross-site; `use-socket.ts` foloseste `io()` same-origin cand baza API e relativa; `output: 'standalone'` + `outputFileTracingRoot` pentru Docker.
- **Dockerfiles noi** (`apps/backend/Dockerfile`, `apps/frontend/Dockerfile`, context = radacina monorepo): backend cu Chromium de sistem pentru PDF (PUPPETEER_EXECUTABLE_PATH) si `openssl` instalat inainte de install/generate (altfel Prisma descarca engine-uri openssl-1.1.x si crapa pe bookworm); migrate deploy la boot. Frontend standalone cu build args NEXT_PUBLIC_*. Ambele imagini validate local: backend health `{db,redis,storage: up}` contra dev-infra.
- **`dev-infra/docker-compose.prod.yml`**: stack complet production-like (postgres, redis, minio+bucket init, mailpit, backend, frontend cu proxy) pentru self-host/verificare e2e a imaginilor.
- **docs/08-deployment.md**: arhitectura de deploy, variabile per serviciu, pasii de seed, checklist post-deploy.

## Sprint F7 — 2026-07-08 — fix claim/oferte + pret per camera cu pagina de oferta prezentabila (feedback PO r1: item 22)
- **BUG 1 (cauza principala "firma nu poate revendica/raspunde")**: in `lib/api.ts`, `...init` venea DUPA `headers` in obiectul fetch → orice apel cu headere proprii (exact mutatiile cu Idempotency-Key: claim, trimitere/acceptare oferta) isi pierdea `Content-Type: application/json`; backend-ul primea body-ul neparsat, DTO gol si intorcea VALIDATION_ERROR ("Date invalide"). Fix: headerele se compun explicit dupa spread. Verificat: butonul "Revendică" functioneaza (redirect la portofel, credit rezervat).
- **BUG 2 (formularul de oferta nu se trimitea)**: in `offer-builder.tsx`, campurile numerice optionale goale (`validityDays`, `designFee`) treceau prin `z.coerce.number().positive().optional()` → `'' → 0` → pica `positive()` FARA mesaj afisat → submitul mureaa silentios. Fix: preprocess care transforma golul in `undefined`.
- **Pret per camera (item 22)**: tabela noua `quote_version_room_prices` (migratie; NUMERIC(12,2), UNIQUE per versiune+camera, snapshot scris o data cu versiunea) — `QuotesService.assertRoomPrices` cere ca defalcarea, daca exista, sa acopere TOATE camerele cererii exact o data si suma sa egaleze pretul (±0.01); persistata in toate cele 4 cai de creare de versiuni (v1, raspuns la modificare, extra, reofertare). `ClaimQuoteContextDto.rooms` expune camerele (ordinea canonica), `QuoteVersionDto.roomPrices` intoarce defalcarea cu roomType.
- **Formularul firmei** (`offer-builder.tsx`): sectiune "Preț pe camere" generata din camerele cererii — totalul se aduna singur, campul Preț se blocheaza pe suma cand toate camerele au pret; defalcare partiala → submit blocat cu explicatie; fara defalcare → pret unic ca inainte. Trimisa in payload la create/revise/extra/reoffer (`rooms` prop prin OfferCard).
- **Pagina de oferta prezentabila**: `VersionView` (acelasi component la client si firma) randeaza tabelul defalcarii (camera → pret, rand de Total accentuat) deasupra campurilor clasice; clientul o vede pe `/requests/[id]/offers` cu numele atelierului in antet, firmele pe claim. docs/04 actualizat (quote_version_room_prices).
- Verificat e2e ca firma B: claim pe cerere din marketplace (fix BUG 1), formular cu "1. Bucatarie" → 12.500 total blocat → trimis (fix BUG 2) → oferta SENT cu `roomPrices` in API si tabel "Bucatarie 12.500,00 RON / Total 12.500,00 RON" pe card. Typecheck FE/BE/shared verde; teste 39/39; chei i18n builder ro+en.
- DECIZIE ramasa deschisa: link public de oferta (fara login) — neimplementat intentionat: ofertele pre-acceptare tin de gating-ul datelor de contact (docs/03); pagina "de trimis clientului" e cea din cont, livrata prin notificarile existente.

## Sprint F6 — 2026-07-08 — platforma de inspiratie + planse hero comutabile (feedback PO r1: itemele 2, 3)
- **DB (migratie `inspiration_photos`)**: enum `inspiration_color` (11 valori) + tabelele `inspiration_photos` (company_id FK — atelierul care a facut piesa, title, room_type, colors[]/materials[]/systems[] pentru filtre, image_url SAU attachment_id, published/featured, soft delete) si `request_inspiration_photos` (max 10 poze per cerere). docs/04 §5.5 actualizat.
- **Modul backend `inspiration`**: `GET /inspiration` public cu filtre CSV whitelist-uite pe enum-uri (type/colors/materials/systems/ids) — doar poze publicate, nesterse, cu imagine; imaginile uploadate se servesc cu **presigned GET** (invarianta 3.4), cele externe direct. Admin CRUD sub `/admin/inspiration` (@Roles ADMIN, @Audit pe scrieri) + upload imagine prin fluxul standard presign→PUT→confirm (`entity_type inspiration_photo`, doar MIME imagine). Publish/edit cereri valideaza pozele alese (`assertSelectable`) si inlocuiesc join-urile in tranzactie; `RequestDto` si `MarketplaceDetailDto` expun `inspirationPhotoIds`.
- **Pagina publica `/inspiration` rescrisa pe API**: filtre tip camera (+ mapare linkuri vechi `?type=kitchen`), culoare / material / deschidere (multi-select), cautare client-side; fiecare poza arata atelierul ("de {firma}"). Datasetul static ramane doar pentru masonry-ul de pe landing.
- **Picker in formular (uploads-step)**: "Inspiratie din galerie" — dialog cu galeria filtrabila, selectie max 10, thumbnails cu stergere; selectia sta in store (`inspirationPhotoIds`, persistata in draft snapshot) si pleaca in payload la publish. Pozele alese apar pe detaliul clientului si pe detaliul marketplace al firmei (`RequestInspirationStrip`).
- **Admin `/admin/inspiration`** (+ intrare in sidebar): creare cu titlu/atelier/tip/etichete + URL extern optional, lista cu toggle Publicata/Pe landing, upload imagine per card, stergere soft.
- **Seed demo** `pnpm seed:inspiration` (idempotent): cele 11 pin-uri statice devin poze DB atribuite round-robin firmelor APPROVED — rulat pe DB-ul de dev (11 poze / 4 firme).
- **Planse hero comutabile (item 2)**: `hero-sheets.tsx` (client island) — dulapul existent + BUCATARIE si BIBLIOTECA noi (axonometrii in acelasi limbaj: oblica de cabinet, cote de alama), comutabile din placute de alama № 0001–0003 (role=tablist); schimbarea remonteaza SVG-ul → animatia plan-draw se reia. Landing-ul ramane RSC.
- Fix la boot: InspirationModule importa AuthModule (OptionalJwtAuthGuard cere JwtService).
- Verificat: `GET /inspiration?colors=GREEN` → exact poza verde cu atribuirea firmei; pagina publica 11 poze, filtrul Verde → 1; landing: 3 taburi, comutare → "Bucatarie · № 0002" cu remount; picker: 11 carduri in dialog, "2 din 10 selectate", ids in store + thumbnails; admin: 11 carduri, select cu 8 ateliere, toggles, nav "Inspirație". Typecheck FE/BE/shared verde; teste 39/39; consola curata.

## Sprint F5 — 2026-07-08 — buget estimat din scor + tara libera + contact imbunatatit (feedback PO r1: itemele 18–20)
- **Slider de buget din scor (item 18)**: endpoint nou `POST /requests/estimate` (Public, throttle 20/min, fara efecte) — ruleaza acelasi pipeline ca publish-ul (`processRooms` + `SizingService.compute`, cu bucket UNDISCLOSED si fara design platit, ca sa nu fie circular) si intoarce `{score, minRon, maxRon}` cu `minRon = scor × 1000 RON` si `maxRon = 3× minRon` (constante partajate `BUDGET_RON_PER_POINT`/`BUDGET_RANGE_FACTOR`). Coloana noua `requests.budget_estimate_ron INT` (migratie `budget_estimate_and_country`); `budget_range` ramane sursa filtrelor/scoringului si se deriva din valoarea aleasa (`budgetRangeFromRon`). Frontend: `useBudgetEstimate` (cheie = continutul camerelor), `BudgetSlider` numeric intre baza si 3× baza (pas 500, valoarea initiala = baza, clamp la re-editarea camerelor) cu fallback pe treptele vechi cand scorul e 0/API indisponibil; UNDISCLOSED ramane. Verificat: bucatarie L+insula → scor 35 → slider 35.000–105.000 lei.
- **Tara libera + Places (item 19)**: coloana noua `requests.country VARCHAR(2) DEFAULT 'RO'`; select de tara in pasul de detalii (23 de tari, etichete localizate din `Intl.DisplayNames`), inaintea adresei; `AddressAutocomplete` isi schimba dinamic restrictia de tara (`setComponentRestrictions`) — cheia GCP ramane `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` (fara ea, input simplu). Geocodarea ramane Nominatim (invarianta 3.8); matching-ul marketplace pentru cereri externe ramane subiect deschis (DECIZIE) — nimic nu se strica, cererea se comporta ca pana acum.
- **Contact (item 20)**: emailul contului e pre-completat ca prim contact (doar daca clientul nu a scris altceva); randurile de telefon au select de prefix de tara (23 prefixe, +40 implicit) cu numar national — valoarea stocata e una singura in format international; validarea partajata accepta RO national SAU E.164 (`INTL_PHONE_REGEX`); mesajul de eroare actualizat. Randurile de contact folosesc `items-start` → eroarea creste in jos fara sa mai urce inputurile vecine (fixul de "shift").
- **Fix F1 ramas**: DTO-ul backend limita camerele la `@ArrayMaxSize(20)` desi schema partajata permite 50 — acum foloseste `MAX_REQUEST_ROOMS`.
- `RequestDto` expune `budgetEstimateRon` + `country`; edit-mode le rehidrateaza; docs/04 actualizat (requests). Typecheck FE/BE/shared verde; teste shared 39/39; verificat in preview pe ecranul de detalii: slider 35.000→50.000 lei, selectul de tara RO implicit, email pre-completat (ana.popescu@demo.ro), telefon +40 cu zero initial absorbit, fara shift la eroare (Y stabil).

## Sprint F4 — 2026-07-08 — pass complet pe restul camerelor (feedback PO r1: itemele 9–17)
- **Config per piesa uniform** (`pieceConfigSteps` in per-piece.ts): fiecare piesa selectata primeste ECRANUL ei — material (carduri, set bucatarie + "Altul" text pe loc) + sistem de deschidere (pill-uri Maner/Push/Glisante, `GENERAL_SYSTEMS`, min 1) — cu iconul piesei in antet (`BaseStep.icon`, randat de StepShell). `buildPerPieceItems` v2: sisteme per piesa + textul "Altul" in description (fallback compat pe answers.openingSystems pentru drafturi vechi). Titlul sistemelor = cheie comuna `common.pieceSystems.*` (nu 25 de chei duplicate). Aplicat pe LIVING, BEDROOM, OFFICE, HALLWAY (+ BATHROOM doar materiale, item 13, cu MDF_INFOLIAT recomandat la baie neventilata).
- **Dressing (item 9)**: dimensiunile vin IMEDIAT dupa layout, cu schita parametrica noua (`wall-runs-diagram.tsx`, izometrie generica A/B/C+H reutilizata si la debara); intrebarea "pana in tavan" ELIMINATA — slotul H cere inaltimea DULAPULUI (1.8–3.2 m), nu a camerei; materiale = setul nou + Altul.
- **Living (item 10)**: optiune "Altceva" la piese cu text liber pe acelasi ecran (numele piesei derivate = textul clientului); zona TV = Comoda suspendata / pe pardoseala / Ansamblu cu dulapuri stanga-dreapta (COMPLEX_UNIT pastreaza scoringul TV_MEDIA_WALL); config per piesa.
- **Dormitor (item 11)**: "pana in tavan" eliminat de la dulap; card "Dimensiuni custom" la saltea care deschide latime+lungime pe acelasi ecran (intra in metri liniari si in descrierea patului "saltea custom WxL cm"); depozitarea sub pat, patul tapitat si noptierele (una/pereche) au fiecare intrebarea lor — tapitarea e Da/Nu de sine statator dupa depozitare (plasare aleasa de noi, PO lasase deschis); config per piesa.
- **Birou (item 12)**: forma biroului include "In U" (slot nou latura C); intrebarea de canal de cabluri ELIMINATA; config per piesa.
- **Hol (item 14)**: intrebarea de latime a holului (depthProfile) ELIMINATA — adancimea derivata ramane 0.5 m; config per piesa. **Debara (item 16)**: schita parametrica pe ecranul de dimensiuni. **Spalatorie (item 15)**: neatinsa, conform feedback.
- **Piese ghidate (item 17)**: builder-ul separa materialul (set nou + Altul pe acelasi ecran) de sistemele de deschidere (ecran propriu, set general); desenele de dimensiuni per piesa raman de adaugat cand PO livreaza imaginile (ChatGPT).
- Teste engine 39/39 (fixture-uri actualizate + test nou: config per piesa cu screenGroup `piece:X` si "Altul" vizibil conditionat). Typecheck FE/BE/shared verde; JSON i18n valid ro+en.
- Verificat in preview (reload per ecran, tab hidden fara rAF): dressing → schita + "Inaltime dressing (H)" pe ecranul 2/7; living → "Altceva"+text, zona TV cu cele 3 optiuni noi, ecran per piesa cu icon + 6 materiale + pill-uri Maner/Push/Glisante; dormitor → card "Dimensiuni custom" care deschide Latime/Lungime saltea pe loc. Consola curata.
- Ramase deschise din feedback: imagini mai bune pentru TOATE optiunile (9.2) — PO livreaza desenele; itemele 18–22 (buget scor, Places, contact, publish UX firma) in sprinturile F5–F7.

## Sprint F3 — 2026-07-08 — infra configurator + bucatarie v2 pass complet (feedback PO r1: item 8)
- **Enum-uri extinse (migratie aditiva `material_system_enum_extension`)**: material +MDF_INFOLIAT/MDF_VOPSIT/MDF_FURNIR/ALTUL, item_system +MANER/GOLA/AVENTOS (doar `ALTER TYPE ADD VALUE`; MDF si BUTON_PRESIUNE raman legacy, valabile pentru raspunsurile vechi). Seed sizing config: greutati MATERIAL (infoliat 2, vopsit 3, furnir 3, altul 2), SYSTEM (maner 1, gola 3, aventos 3), KITCHEN_COUNTERTOP +PAL/HPL (0, ajustabile din admin). docs/04 §5.5 completat cu material + item_system.
- **Kitchen v2 rework** (in place — v2 exista doar pe acest branch, nepublicat): insula = intrebare de sine statatoare cu carduri Da/Nu (nu toggle sub layout, 8.1); materiale = PAL/MDF infoliat/MDF vopsit/MDF furniruit/Lemn masiv + **"Altul" cu text liber pe acelasi ecran** (`otherMaterialStep` conditional prin visibleIf, obligatoriu doar cand e vizibil; textul intra in `request_items.description` la derive, 8.3); sisteme deschidere: jos/insula = Maner/Push/Gola, suspendate +Aventos (8.4); blat = PAL/HPL/Cuart/Granit (8.5); electrocasnice neatinse (8.6). `materialOptions`/`systemOptions` parametrizate cu seturi (`OFFERED_MATERIALS`, `KITCHEN_SYSTEMS_*`); default-urile raman cele legacy pana la migrarea celorlalte flow-uri in F4. Teste engine 39/39 (2 noi: seturi sisteme per zona; ALTUL obligatoriu + description).
- **Dimensiuni: selector m/cm/mm + validare per camp (8.2)**: comutator de unitate in dimension-group (default cm, preferinta persistata in store `dimensionUnit`); valorile raman in metri in answers — conversie doar la afisare (interval, step, min/max); **eroare inline sub camp in timp ce tastezi** (`validation.dimensionRange` cu intervalul in unitatea curenta, aria-invalid), nu doar la Continua. Etichete cu litere: "Lungime insula (L)", "Adancime insula (D)", "Inaltime tavan (H)"; in schita insula e cotata L/D scurt, cota C mutata pe fata superioara a laturii C (se suprapunea cu insula).
- **Spatele cardurilor fara scroll (8.3)**: paragraful introductiv scos de pe spatele PlayingCard (ramane in dialogul Info al pill-urilor); pros/cons dimensionate sa incapa complet (verificat scrollHeight == clientHeight).
- **Iconite ghid schita redesenate (8.7)**: cele 4 vignete (masoara/deseneaza/marcheaza/fotografiaza) redesenate fara text in desen (cota "3,40 m" se suprapunea cu peretele) — ruleta cu gradatii, foaie+creion separat, usa/fereastra/priza aerisit, camera cu blit.
- **i18n ro+en**: blocuri editoriale complete pentru cele 3 materiale MDF noi + Altul + otherMaterial, Maner/Gola/Aventos, blat PAL/HPL, `validation.dimensionRange`.
- Verificat e2e in preview (bucatarie L + insula, 12 ecrane): layout doar 3 carduri → insula Da/Nu → dimensiuni cu selector (340 cm ↔ 3400 mm, eroare inline la 5000 cm, valorile salvate in metri: runA 3.4) → materiale 6 optiuni cu "Altul"+text obligatoriu ("sticla mata" in answers) → sisteme Maner/Push/Gola (+Aventos la suspendate) → blat PAL/HPL/Cuart/Granit. Typecheck FE/BE/shared verde; teste 39/39; seed re-rulat (65 config). Nota: tab-ul de preview e hidden (fara requestAnimationFrame) → tranzitiile framer-motion ingheata DOAR acolo; verificarea s-a facut prin reload per ecran — in browser real nu se manifesta.

## Sprint F2 — 2026-07-08 — comutator de limba global + CTA "Creeaza o cerere" omniprezent (feedback PO r1: itemele 5, 6)
- **LangSwitch pe toate paginile** (item 5): comutatorul ro|en exista doar in AppHeader (paginile autentificate, inclusiv formularul de cerere). Adaugat in `public-header.tsx` (desktop: langa CTA; mobil: in footerul meniului hamburger) si in layout-ul auth (colt dreapta-sus, login/register/verify-email). Acopera acum: landing, parteneri, inspiratie, ghid schita, auth — toate rutele.
- **CTA "Creeaza o cerere" persistent** (item 6): buton walnut in headerul public, vizibil pe desktop si mobil (pe mobil login/register se muta in meniu ca sa-i faca loc); ascuns pentru rolurile COMPANY_USER/ADMIN (nu creeaza cereri). Register coboara la outline ca sa nu concureze cu CTA-ul. Pe landing, CTA-ul hero primeste marimea noua `xl` (`h-12 px-8 text-base`) adaugata in `ui/button.tsx`. Cheie noua `Nav.createRequest` ro+en.
- Verificat in preview: header logat (`ro|en · Creează o cerere · Contul meu`) si nelogat (+ Autentificare/Înregistrare); click "en" → `/en` cu tot UI-ul tradus; mobil 375px fara overflow orizontal, CTA vizibil, meniul contine linkuri + cont + LangSwitch; typecheck FE verde; consola curata. Nota: sesiunea de preview expirase intre sprinturi — headerul a reflectat corect ambele stari (nu e regresie F1).

## Sprint F1 — 2026-07-08 — bugfix rapide (feedback PO r1: itemele 1, 4, 7, 21)
- **Header landing auth-aware** (item 1 — "back ma deconecteaza"): cauza reala — headerul landing-ului era static (mereu Login/Register), userul logat parea deconectat la revenirea pe `/`; sesiunea (cookie httpOnly) nu se pierdea. Header comun nou `_components/public-header.tsx` (client island cu `useMe`, linkuri publice + "Contul meu"/Login) folosit si de landing (ramane RSC) si de `PublicShell` (simplificat, fara 'use client'). Verificat in preview: dashboard → Back → `/ro` afiseaza "Contul meu".
- **Animatie ghid schita mai lenta** (item 4): varianta `plan-draw--slow` in globals.css (trasare 1800ms/fade 900ms, fata de 1100/600), delay-uri intinse ×1.7 si bucla 8s → 13s in `/sketch-guide`; landing-ul ramane pe ritmul vechi.
- **Plafon camere 20 → 50** (item 7): constanta partajata noua `MAX_REQUEST_ROOMS = 50` in `request.schemas.ts` (folosita in cele 3 scheme cu `.max(20)` + `room-cart-step.tsx` — FE si BE valideaza identic). Plafon tehnic anti-abuz, nu limita de produs. Verificat in preview: 21 camere adaugate, butonul + ramane activ.
- **Publish: overlay de incarcare + detalii instant** (item 21): publish-ul dureaza secunde (geocoding write-time, invarianta 3.8) fara feedback — overlay fix "Publicam cererea ta..." (role=status, ramane pana la redirect; varianta "Salvam modificarile..." la edit) in `review-step.tsx`; `usePublishDraft`/`useEditRequest`/`useRepostRequest` seteaza si cache-ul `['request', id]` din raspuns → pagina de detalii randeaza instant din cache. Chei noi `Configurator.review.publishing*` ro+en.
- Verificat: typecheck FE/BE/shared verde; teste shared 37/37; consola preview curata; `next lint` esueaza pre-existent (ESLint neconfigurat).

## Configurator complet — 2026-07-07 — camere noi, piese ghidate, v2 uniform, ordine fixa a intrebarilor
- **Ordine fixa a intrebarilor dupa complexitate** (cerinta PO): `room-meta.ts` nou in shared (`ROOM_KIND`, `ROOM_ORDER`, `sortByRoomOrder` — sort stabil, instantele de acelasi tip pastreaza ordinea intre ele). Store-ul insereaza sortat (`addRoom`/`removeLastOfType`/`removeRoom` via `sortedRoomsPatch`), `normalizeInstances` sorteaza pe TOATE caile de resume (migrate localStorage, snapshot server, edit-mode) cu **remap `activeRoomIndex` prin localId** (resume mid-flow ramane pe aceeasi camera); persist bump v2→v3. Server: `toDto` + `MarketplaceService.detail` sorteaza camerele cu `sortByRoomOrder` (createdAt e identic in tranzactia de publish — nedeterminist). Ordinea: Bucatarie → Dressing → Living → Dormitor → Birou → Baie → Hol → Spalatorie → Debara → Balcon → piese (Dulap ... Bancuta) → "Alta piesa" ultima.
- **4 camere noi** (flow v1 + i18n ro/en + scoring): HALLWAY (piese + profil adancime educativ slim/standard → widthM 0.3/0.5), LAUNDRY (asezare electrocasnice + ventilatie ca la baie v2, MDF recomandat), PANTRY (pereti folositi + stil depozitare), BALCONY (calificare inchis/deschis — lemn masiv recomandat la deschis; widthM limitat de adancimea libera).
- **10 piese ghidate individuale** (RoomType `PIECE_*`, flow v1 fiecare, 4–5 ecrane: configuratie → dimensiuni → material → schita): Dulap (usi + **"pana in tavan / tip dressing"** — decizia PO pentru dressing-ca-piesa; inaltime dinamica), Comoda TV (comoda joasa/perete media + pozitie TV), Biblioteca (deschisa/corpuri jos/vitrina), Birou (drept/L + canal cabluri + corp sertare/etajera), Pat (dimensiuni saltea standard + lada rabatabila/sertare + tapitat), Comoda, Masa (dining/cafea/consola + forma cu diametru dinamic la rotunda + nr persoane), Pantofar (slim rabatabil/standard/cu bancuta → adancime derivata), Noptiere (una/pereche → quantity), Bancuta. Helper comun `guided-pieces/builder.ts`. Tab-ul "Piese individuale" din cart = grila cu +/- multi-instanta; **PIECES legacy ramane "Alta piesa"** (formular liber, cap 1, mereu ultima) — flow v1 neatins.
- **v2 pentru Dressing/Living/Dormitor/Birou** (uniformizare la standardul kitchen/bathroom v2; v1 devin FROZEN, cererile publicate raman valide pe versiunea lor): dimensiuni per piesa (sloturi dinamice), **material per piesa grupat pe UN ecran** (`screenGroup: 'materials'` + helper `per-piece.ts`), pas schita (upload max 3, sarit la hasOwnProject) in loc de notes, intrebari educative noi: dressing (pana in tavan cu scoring, LED, module cu preturi, scoring DRESSING_LAYOUT), living (stil TV comoda/perete media cu scoring TV_MEDIA_WALL, rafturi suspendate, LED), dormitor (usi+tavan dulap, dimensiune saltea/depozitare pat cu scoring BED_STORAGE/tapitat, masuta toaleta noua, noptiere una/pereche), birou (canal cabluri, rafturi perete).
- **Step-renderer**: varianta compacta pentru choice-uri inline si pentru ecranele `screenGroup: 'materials'` — rand de pill-uri cu icon + label + badge Recomandat + **buton Info per optiune** (educatia ramane peste tot); boolean inline neschimbat.
- **DB/scoring**: migratie `room_type_new_rooms_and_guided_pieces` (DOAR `ALTER TYPE ADD VALUE` ×14, verificat); seed `project_sizing_config`: ROOM_TYPE ×14 (HALLWAY 3, PIECE_WARDROBE 3, restul 1–2) + categorii noi DRESSING_LAYOUT (1/2/3/3), WARDROBE_TO_CEILING:YES 1, TV_MEDIA_WALL:YES 2, BED_STORAGE (LIFT_UP/DRAWERS 1), TABLE_EXTENDABLE:YES 1 — toate ajustabile din Admin. `ROOM_TITLE_LABELS` +14 (titluri generate corecte, ex. "Dulap + Hol — Cluj"). docs/04 §5.5 actualizat.
- **i18n**: +786 chei per limba (ro fara diacritice in namespace-ul Configurator, conform stilului existent; en complet) — toate optiunile noi au descrieri + info educativ (pros/cons/pret orientativ realist).
- Teste shared 37/37 (raspunsuri minimale pentru toate cele 21 de tipuri — Record exhaustiv la compilare; sortare stabila; FROZEN v1 regresie; PIECES neatins). Verificat e2e in preview: cos amestecat (Baie→Hol→2×Dulap→Alta piesa→Bucatarie) → intrebari in ordinea canonica cu chips corecte; flow Hol complet pana la ecranul de materiale grupate (2 randuri pill-uri); flow Dulap cu ecranul usi+tavan; consola curata dupa reload. `next lint` esueaza pre-existent (ESLint neconfigurat); backend nu are teste (script jest stub, fara spec-uri) — typecheck BE/FE/shared verde.
- **Carusel parteneri automat**: avanseaza singur un card la ~4s si revine la inceput la capat; pauza cat timp userul e cu mouse-ul/degetul/focusul pe el; respecta `prefers-reduced-motion`; sagetile raman ca override manual.
- **Schita hero 3D**: elevatia dulapului inlocuita cu **axonometrie** (oblica de cabinet: fata + blat + lateral, spalari discrete de ton pe volume) cu cotele asezate corect: 2500 sub muchia din fata, 2200 pe stanga, 600 paralel cu muchia oblica; aceeasi animatie plan-draw; `heroSheetTitle` actualizat ro/en.
- **"Cum functioneaza" animat** (`process-section.tsx`, client): porneste la scroll (IntersectionObserver) — fir de alama care se traseaza intre cele trei acte, vignete-schita per pas (plan+creion / trei ateliere cu bifa / doua oferte cu sigiliu) desenate cu plan-draw, coloane cu reveal in cascada. Utilitare noi in globals.css: `.plan-paused` (tine animatiile pe loc pana la viewport) si `[data-reveal]`/`.in-view` cu delay `--rd`.
- **Ghid schita redesenat** (`/sketch-guide`): plansa-exemplu inramata (plan de camera care se traseaza singur: cote 3400/2800, usa cu arc, fereastra, priza cu X, locul mobilierului punctat) cu legenda, pasii ca patru acte numerotate cu cifre romane + sfat per pas, banda CTA espresso; chei noi `SketchGuidePage.*` ro+en.
- **Navigatie mobila** (`mobile-nav.tsx`): hamburger + panou sub header pe toate cele trei headere (landing, PublicShell, AppHeader) — pana acum pe telefon NU exista nicio navigatie; se inchide la navigare si Escape; `aria-expanded`; login-ul intra in panou pe mobil; logout doar-icoana pe mobil; cheie noua `Nav.menu` ro+en.
- Verificat: typecheck FE OK; preview desktop+mobil (375px): meniul se deschide/navigheaza/se inchide, caruselul ruleaza si buclat (scrollLeft 754 → capat → 24), sectiunea proces primeste `.in-view` si vignetele se deseneaza; `next lint` esueaza pre-existent (ESLint neconfigurat).

## Design "ATELIER" + rebrand Cozy Home — 2026-07-06/07 — identitate vizuala, landing bogat, galerie inspiratie
- **Rebrand**: numele aplicatiei devine **Cozy Home**; logo nou SVG (casa desenata in linie + fum de alama; `src/components/brand/logo.tsx` — `CozyHomeMark`/`CozyHomeLogo`) folosit in landing, app-header, public-shell, auth, admin; metadata title actualizat.
- **Design system "ATELIER"** (dupa 2 iteratii cu PO — respins "warm generic" si respins "tehnic rece"): paleta calda originala (ivorie/espresso/nuc) + detalii de alama; display **Marcellus** (o singura greutate — fara bold pe serif), body DM Sans, mono doar pentru date; radius 8px, umbre moi; `.kicker` alama, `.label` sans spatiat; semnatura = schita SVG auto-trasata din hero (cote in alama, rama cu passe-partout).
- **Landing imbogatit**: sectiune galerie masonry (8 imagini → `/inspiration?type=...`), carusel ateliere partenere (scroll-snap + sageti; date reale din `GET /companies/partners`, rating/recenzii/specializare = mock determinist etichetat in `src/lib/mock-partner-meta.ts` pana la integrarea Google semi-manuala — Bible §1.4), procese numerotate I/II/III, footer nou. Fara sectiunea "format proiect" (informatie doar pentru firme).
- **Inspiratie** (`/inspiration`): galerie reala (20 imagini Unsplash verificate vizual, `src/lib/inspiration.ts`), cautare text toleranta la diacritice + filtre pe tip de mobilier (bucatarie/living/dormitor/dressing/birou), fiecare pin se deschide pe Pinterest (cautare pe tipul lui); filtru initial din query `?type=`.
- **Fix**: `Notifications.type` avea chei cu punct (INVALID_KEY next-intl pe fiecare pagina) — imbricate corect in ro/en.json, callsite neschimbat.
- Verificat: typecheck FE OK; consola curata; testat vizual mobil+desktop (landing, inspiratie cu filtre/search, login); `next lint` esueaza pre-existent (ESLint neconfigurat interactiv — de rezolvat separat).
- **Parteneri** (`/partners`, public): endpoint nou `GET /companies/partners` (`@Public()` + `@Roles()` gol suprascrie rolul de clasa) — DOAR firme APPROVED, doar date publice (nume, oras, judet, partener din, max 3 piese de portofoliu); rating Google = placeholder null (integrare externa ulterioara). UI: grila de carduri cu stea de rating, locatie, sloturi de portofoliu (imagine sau placeholder).
- **Inspiratie** (`/inspiration`, public): galerie masonry placeholder "in curand" (continutul real — proiecte livrate — vine ulterior).
- **Ghid schita** (`/sketch-guide`, public): reutilizeaza componenta `SketchGuide` din configurator (sursa unica) + CTA "Creeaza o cerere".
- **PublicShell** nou (logo → landing, linkuri publice, login/register sau "Contul meu" daca e logat); landing-ul primeste aceleasi linkuri in header. Impreuna cu Faza B/C: logo client → landing, logo firma → marketplace, admin → consola.
- Verificat: typecheck shared+BE+FE OK; shared 31/31; i18n JSON valid ambele; **E2E real**: `GET /companies/partners` fara auth → 4 firme APPROVED (D/E PENDING, F REJECTED, G SUSPENDED excluse corect).
- Security pass B+C+D: endpoints noi toate sub guards corecte (CLIENT/COMPANY_USER + ownership `findOwnedRequest`/`requireMembership`), presign pe id throttled 30/min, partners public expune doar date ne-sensibile; fara secrete noi; format erori unic. PASS pe toata checklista.

## Overhaul Faza C — 2026-07-05 — Pagini firma (dashboard, echipa, claims, timp relativ)
- **Dashboard firma**: endpoint nou `GET /companies/me/dashboard-stats` (orice membru) — claim-uri total/active/pe statusuri, claim-uri active per membru (nume + neatribuite), oferte trimise + valoarea ultimei versiuni per oferta (raw SQL MAX(version)), sold credite (disponibil/rezervat), credite CONSUMATE ultimele 7/30 zile, puncte de penalizare active (scope COMPANY, per memorie: fara blocaj angajat in MVP). `CompanyDashboardStatsDto` in shared; UI: carduri + sectiune credite cu link portofel + per-membru + statusuri + penalizari + CTA-uri.
- **Pagina dedicata Revendicari** (`/marketplace/claims`): tabel cu titlul cererii, cine a preluat, cui e atribuit (evidentiat cand e neatribuit), cand (timp relativ), stadiu, termen SLA (+pauza), linkuri oferta/cerere. `listMine` include acum titlul cererii + numele claimedBy/assignedTo (`ClaimSlotDto` extins aditiv). Sectiunea de claims scoasa din portofel (ramane un link); link "Revendicari" in header.
- **Pagina firmei (UX)**: formularul de locatie apare doar la "+ Adauga locatie", cu campuri ETICHETATE (Latitudine/Longitudine cu hint de format, Raza de acoperire cu explicatie, Tara "Romania"); echipa: rol afisat ca badge colorat pe rol, formular de adaugare dupa "+", si **select de schimbare rol** (promovare/retrogradare manager/trusted/managed — endpointul PATCH exista deja, acum e si in UI); matricea de permisiuni: detectie modificari nesalvate — Save dezactivat cand nu e nimic de salvat, avertizare "Modificari nesalvate" + Renunta, confirmare "Salvat.".
- **Timp relativ umanizat** (`lib/relative-time.ts` + `Common.relativeTime` cu plurale RO/EN): sub 60 min → minute, apoi ore, zile, luni — inlocuieste "35000 min ago" in marketplace (lista + detaliu) si e folosit pe pagina de claims.
- **Header firma**: logo → `/marketplace` (facut in Faza B), nav: Contul meu / Firma / Marketplace / Revendicari / Portofel.
- Verificat: typecheck FE/BE OK; **E2E API real** (owner.b@demo.ro): stats 5 claims / 3 oferte / 25.500 RON / per-membru "Florin B=3, neatribuite=2"; `claims/mine` cu titluri + nume.

## Overhaul Faza B — 2026-07-05 — Pagini client (dashboard real, editare cerere, vizibilitate)
- **Dashboard client real**: endpoint nou `GET /requests/dashboard-stats` (inaintea `@Get(':id')` — ordinea rutelor) — total cereri, distributie pe statusuri (groupBy), oferte primite (quotes SENT/ACCEPTED pe cererile mele), claim-uri active, ultimele 5 dupa updatedAt. `ClientDashboardStatsDto` in shared; UI: carduri statistici + statusuri + activitate recenta cu linkuri + CTA cerere noua (doar rol CLIENT; firmele primesc varianta lor in Faza C).
- **Vizibilitate**: marimea/scorul/costul in credite si coordonatele NU se mai servesc clientului — `toDto` (owner) intoarce `sizing/lat/lng = null`, `listForClient` `size = null`; paginile client nu le mai randeaza (colonna Size scoasa din lista; detaliu fara scor/coords, afiseaza in schimb intervalul de termen). Marketplace-ul firmelor neatins.
- **Editare cerere publicata**: buton "Editeaza cererea (N ramase)" pe detaliu (IN_MARKETPLACE 3−used / CLAIMED_* 1−used; avertizare §4.3 la post-claim — rescore, dar snapshot-urile claim-urilor raman). Backend: `POST /requests/:id/edit` autentificat (ownership; tokenul draftului e doar hash → editare de pe orice device) + rute de atasamente pe id (`POST/:id/attachments`, confirm, DELETE — throttle 30/min, aceleasi verificari de status). `edit(token)` si `editForClient(id)` converg in `editRequest()` comun. Frontend: ruta noua `requests/[id]/edit` monteaza wizard-ul in mod editare — snapshot reconstruit din `rooms[].answers+flowVersion` (configuratorState e null post-publish), publish → `saveEdit`; hooks de atasamente generalizate pe tinta (`AttachmentTarget`: draft-token sau request-id).
- **Navigatie client**: logo-ul duce pe landing pentru client (marketplace pentru firme, consola pentru admin); link separat "Contul meu" in nav pentru client si firma.
- Verificat: typecheck FE/BE OK; **E2E API real**: stats (total=5, byStatus, claims=2, offers=1, recent=5), sizing/lat/lng null pe detaliu client, edit autentificat pe id → buget+termen schimbate, editsUsed 1/3, titlu regenerat, contacte rescrise.

## Overhaul Faza A — 2026-07-05 — Configurator v2 (carduri, flow-uri versionate, redesign detalii)
- DECIZII sesiune (user, plan aprobat): slider buget pe cele 3 intervale existente + optiune "nu spun" (UNDISCLOSED); Google Places pentru adresa (UI-only, geocodarea ramane Nominatim — invarianta 3.8 neatinsa); ilustratii SVG custom in cod; ordinea fazelor A→B→C→D.
- **Versionare flow-uri** (`packages/shared/questionnaire`): `FLOW_REGISTRY` per RoomType/versiune + `CURRENT_FLOW_VERSION`; `getFlow(roomType, version?)` + `isFlowVersionRegistered`; versiunea e propagata prin `validateRoomAnswers/summarizeAnswers/collectScoreEntries`. `ConfiguratorService` accepta orice versiune INREGISTRATA (cererile v1 publicate raman valide la edit/rescore); kitchen/bathroom v1 marcate FROZEN. Store: `RoomInstance.flowVersion` + migrare persist (v veche → 1) + normalizare `loadSnapshot`.
- **Migratii schema** (4, izolate): `budget_range += UNDISCLOSED`; `room_type += PIECES`; `desired_deadline_bucket` enum (ASAP/1-3/3-6/6+/FLEXIBLE) inlocuieste `desired_deadline DATE` (backfill din diferenta de zile); `contact_channel` enum (EMAIL/PHONE) + drop `priority` (mapare date: %mail%→EMAIL, rest PHONE). Shared: `DEADLINE_BUCKETS`/`CONTACT_CHANNELS`, `contactPreferenceSchema` discriminated union (email valid / telefon RO regex), lista contacte 1..4 max 2/canal, `description` optional, `configuratorContentSchema` FARA titlu. Seeds: `BUDGET:UNDISCLOSED` 0, `ROOM_TYPE:PIECES` 1 (aplicate). docs/04 actualizat (§5.3/§5.5).
- **Titlu auto-generat pe server**: `buildRequestTitle(rooms, city)` → "Bucătărie + 2 Băi — Cluj" (RO stocat = sursa de adevar; frontendul afiseaza preview localizat din camere+oras). `resolveTitle` pastreaza titlul manual doar pe fluxul legacy.
- **Kitchen v2**: PARALLEL (galley) eliminat (seed-ul KITCHEN_LAYOUT:PARALLEL PASTRAT pt rescore v1); insula = add-on pe ACELASI ecran cu layout-ul (`screenGroup` + comutator inline, `optional` — lipsa=fara insula); `cabinetZones`/`extraPieces`/`notes` eliminate; intrebari per zona (frontMaterial/openingSystems × Base/Wall/Island) — scoring per zona curge prin items-urile derivate (fara chei noi de sizing); pas final = upload schita. **Diagrama parametrica SVG** (`kitchen-dimensions-diagram`) redeseneaza live A/B/C/H/insula din raspunsuri; inputurile de dimensiuni afiseaza intervalul permis (min–max m, toate camerele).
- **Bathroom v2**: dimensiuni per piesa selectata (lavoar/oglinda/coloana); intrebare noua VENTILATIE (geam/ventilator/deloc) inainte de materiale; MDF cu badge "Recomandat" cand NONE (`recommendedIf` — prezentational, nu restrictie); material per piesa; pas schita.
- **Carduri "playing card"** (`playing-card.tsx`): fata = ilustratie SVG + eticheta, butonul Info INTOARCE cardul (flip 3D framer-motion; reduced-motion = instant), spatele = descriere/avantaje/dezavantaje/pret mediu (pretul ascuns cand lipseste — ex. layout). Biblioteca `illustrations/` (~25 SVG line-art: layouts, zone, blaturi, electrocasnice, piese baie, ventilatie, materiale, sisteme, da/nu) cu fallback pe iconurile lucide.
- **Reordonare faze**: cart → camere → FISIERE → detalii → sumar (planurile inainte de info generale). `hasOwnProject` mutat pe cart ("am proiect pentru toata locuinta") → pasii de schita per camera se sar; altfel uploader per camera (`room-sketch-upload`, max 3/camera, cap global 10 — invarianta 3.4) + **ghid vizual de schita** (`sketch-guide`, 4 pasi, reutilizabil pe pagina publica din Faza D). Backend: attachment id-urile din answers verificate ca apartin cererii (`assertRoomAttachments`).
- **Tab "Piese individuale"** in cart (flow PIECES v1: pieces 1..10 + schita; euristica 1ml/piesa; exclus din grila de camere).
- **Detalii redesenate**: titlu doar preview auto; mesaj liber optional fara contor; buget = slider 3 trepte + "prefer sa nu spun"; termen = 5 carduri interval (fara date picker); adresa = Places Autocomplete `country:ro` (fallback inputuri simple fara cheie; `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` documentat in `.env.example`) + tara fixa Romania; contacte doar EMAIL/PHONE validate ca format, max 4 (2/canal), fara prioritate.
- **Copiere raspunsuri**: buton "Copiaza raspunsurile de la <camera> anterioara" cand exista o camera terminata de acelasi tip+versiune (niciodata automat; schitele NU se copiaza; tinta ramane ne-completa pentru revizuire).
- Verificat: shared 31/31 teste (v1 pin-uite pe versiunea 1 + suite noi kitchen v2/bathroom v2/compatibilitate); typecheck shared+backend+frontend OK; migratii + seed aplicate pe DB reala; **E2E API real**: login client demo → draft → publish kitchen v2 (insula, 3 zone materiale diferite) + bathroom v2 (ventilatie NONE) + PIECES → titlu auto "Bucătărie + Baie + Piese individuale — Cluj-Napoca", UNDISCLOSED, deadline bucket, sizing LARGE/4 credite, 6 items derivate corect. NEVERIFICAT: UI interactiv in browser (instanta secundara blocata de CORS pe portul preview — de verificat pe :3000); `next lint` neconfigurat (pre-existent).
- Presupuneri: kitchen v2 deriva MEREU baza+suspendate (decizie PO — fara selectie de zone); PIECES cap 1 instanta in cart (un cos de piese per cerere); draft-urile v1 in curs isi continua flow-ul pe v1 (registrul pastreaza v1 utilizabil).
- DECIZII sesiune (user): (1) definitiile intrebarilor = **typed TS config in `packages/shared`** (nu DB-driven); (2) **bucatarie flagship** deep + celelalte 5 camere flow-uri simple pe acelasi engine; (3) **framer-motion** adaugat; (4) vechiul formular pe o pagina **sters complet**; (5) **scoring per raspuns** — fiecare raspuns puncteaza, suma → cost claim in credite. Impact D1 (FINALA): punctele NU stau in cod, ci in `project_sizing_config` (referite din flow prin `{category, optionKey}`), editabile din admin fara deploy; suma trece prin `project_size_thresholds` existent. Marketplace/claims/billing/snapshot la claim: **neatinse**.
- Shared (`packages/shared/src/questionnaire/`): motor de chestionar nou — `types.ts` (QuestionStep discriminated union: single/multi-choice, boolean, dimension-group cu sloturi dinamice=functie, number, text, pieces; Condition serializabila; ScoringRef; InfoContentRef), `engine.ts` (evalCondition, visibleSteps/visibleOptions, `stepAnswerSchema` Zod dinamic, `validateRoomAnswers` partial/publish + respinge chei necunoscute/invizibile, `pruneAnswers`, `summarizeAnswers`, `collectScoreEntries`), `mapping.ts` (roomSizeBucket/itemQuantityBucket/linearMeters reproduc semantica SizingService), `flows/` (kitchen ~10 pasi + dressing/bedroom/living/office/bathroom pe acelasi engine). Extins `request.schemas.ts` (`configuratorRoomSchema`, `configuratorContentSchema`, `RequestRoomDto.answers/flowVersion`, `RequestDto.configuratorState`, patch `configuratorState`) + `error-codes.ts` (`CONFIGURATOR_ANSWERS_INVALID/FLOW_VERSION_UNSUPPORTED/STATE_TOO_LARGE`). **Vitest nou** (primele teste din repo): 21 teste — conditii, sloturi L/U/insula, metri liniari, validare partial/publish, respingere answers fabricate, deriveRoom valid pt toate 6 flow-urile, **collectScoreEntries regresie vs semantica SizingService**.
- Prisma: migratie `configurator_answers` **pur aditiva, nullable** — `requests.configurator_state JSONB` + `request_rooms.answers JSONB` + `request_rooms.flow_version INT`. Cererile legacy (seed/demo, answers=null) raman valide (fallback). Seed scoring nou: `KITCHEN_LAYOUT` (STRAIGHT 1/L 2/U 3/PARALLEL 2), `KITCHEN_ISLAND` YES 2, `KITCHEN_COUNTERTOP` 0 (placeholder admin).
- Backend (`requests`): `ConfiguratorService` nou (pur — flowVersion check → `validateRoomAnswers` publish → `deriveRoom` + `collectScoreEntries`; 400 cu `details:[{roomIndex,stepId,messageKey}]`). `SizingService.compute` refactorizat sa consume `scoreEntries` (rezolva `pick:'max'` pt SYSTEM din DB) + BUDGET/PAID_DESIGN — iesire score/size/creditCost identica. `publish`/`edit` deriva server-side (clientul nu trimite dims/items/scoruri) → scrie rooms cu `answers`+`flowVersion`; draftul tine doar `configuratorState` (cap 200k) — nu materializeaza camere; `configuratorState` null-at la publish. `marketplace.detail` + `toDto` expun answers/flowVersion. DTO: `ConfiguratorRoomInputDto` inlocuieste `RoomInputDto`.
- Frontend: `framer-motion` adaugat; store nou `stores/configurator-store.ts` (Zustand persist `mm_configurator_v1` — primul store din proiect, conform STATE_CONVENTIONS Ex.1); `components/configurator/*` — wizard (5 faze: cart→rooms→details→uploads→review), `room-cart-step` (adaugare camere cu cantitati, max 20), `room-flow-runner` (visibleSteps, progres, back/next, chips camere, tranzitii `AnimatePresence`), `step-renderer` (7 tipuri + editor pieces), `info-dialog` (pros/cons/pret, animat), `details-step` (RHF+Zod reutilizeaza `Requests.*`), `uploads-step` (Dropzone + status scan), `review-step` (sumar + publish), `room-answer-summary` (**partajat** cu paginile de detaliu). Sync server debounced 1.5s (`configuratorState`) + resume de pe alt device. `ChoiceCard` restructurat (div wrapper + buton Info colt dreapta-sus + prop `multi`). Pagini detaliu client + marketplace: `room.answers ? <RoomAnswerSummary/> : <lista items legacy>` (fallback compat seed/demo). Vechiul `requests/new/page.tsx` inlocuit.
- i18n: namespace nou `Configurator` in `ro.json` + `en.json` (ambele) — chrome + `common.materials/systems` (info pros/cons/pret) + toate 6 flow-urile (~470 chei/locala). Detaliile reutilizeaza `Requests.*`.
- Verificat: `pnpm -F @marketplace/shared test` 21/21 + typecheck + build; backend typecheck + `nest build` OK; frontend typecheck + **`next build` OK** (toate rutele); paritate i18n RO/EN validata (15 chei-cheie spot-check). NEVERIFICAT pe infra reala: migratia + smoke E2E (Docker oprit in mediu) — plan de test manual in plan file §10; `next lint` neconfigurat in proiect (prompt interactiv, ca la design pass).
- Presupuneri: deploy atomic monorepo (FE+BE aceeasi versiune de flows); firmele vad answers pre-claim (fara date de contact, aceeasi politica ca `description`); insula puncteaza legitim multiplu (ISLAND YES + metri liniari + item zona) — calibrare din admin; nume items derivate in DB in romana (doar fallback; UI randeaza i18n din answers); `flow.version` bump la schimbari breaking, draft vechi → reject `FLOW_VERSION_UNSUPPORTED`.
- FIX (post-integrare, pe infra reala): configuratorul ramanea blocat pe "Se pregateste" — `createDraft` folosea `mutate({}, {onSuccess})`, iar callback-urile per-apel ale mutatiei React Query sunt ignorate cand observer-ul se demonteaza la remount-ul simulat de StrictMode (dev), deci tokenul nu se seta niciodata (17 draft-uri create, zero persistate). Trecut pe `mutateAsync().then(...)` (promisiune independenta de observer). Migratie + seed aplicate pe DB reala; publish verificat cu payload valid.
- FIX (pre-existent, Sprint 5, descoperit testand feed-ul ca firma eligibila): `MarketplaceService.queryRows` (folosit de list SI detail) avea 2 bug-uri de raw SQL care dadeau 500: (1) `cs.status IN (...)` / `r.status IN (...)` comparau coloana enum cu parametri text (`Prisma.join`) → `operator does not exist: claim_slot_status = text` → adaugat `::text` pe coloane; (2) in ramura de detail `r.id = ${requestId}::uuid`, dar coloanele id sunt `text` (Prisma String fara `@db.Uuid`) → `operator does not exist: text = uuid` → scos cast-ul `::uuid`. Verificat pe infra reala: feed 200 + cererea publicata vizibila firmei Platinum; detail 200 cu answers/flowVersion. (Neatins de feature-ul configurator: singura mea modificare in fisier era expunerea answers in `detail`.)
- DECIZIE sesiune (user): "fill as guest, login to publish" — draftul se completeaza anonim (pastreaza decizia D-v6-2), dar **publicarea necesita cont**. Backend: `publish(token, dto, userId)` arunca `UNAUTHORIZED` daca nu e autentificat (verificat 401 pe infra reala) si leaga draftul de contul care publica (`clientUserId`). Frontend: `ReviewStep` verifica `useMe`; nelogat → buton "Autentifica-te pentru a publica" → `/login?redirect=/requests/new`; login/register duc `redirect` (returnUrl) mai departe, iar dupa login wizard-ul reia faza review din store-ul persistat si publica. (Nota: `IntlError INVALID_KEY` in log e pre-existent — chei cu punct in `Notifications.type.*`, Sprint 9 — neatins.)

## Design pass — 2026-06-17 — Re-skin "Plan" artizanal (design system + UI 1:1)
- DECIZIE sesiune (user): identitate **artizanala calda** (bej/nuc/salvie, titluri Instrument Serif), o singura tema (fara switcher runtime); port 1:1 al tuturor ecranelor; pastrare shell curent (header sus client/firma, sidebar admin); rutarea neschimbata.
- CONSTRANGERE respectata: pass pur de prezentare — niciun hook/TanStack Query/mutatie/API/schema Zod atins; doar markup + className + componente noi de prezentare. Stack ramane Tailwind + shadcn/ui; reutilizat `cn()` si patternul cva din `button.tsx`.
- Sursa design: prototipul din `Design/` (styles.css/theme-modern.css/components.jsx etc.). Tokenuri hex convertite in triplete HSL pentru conventia shadcn `hsl(var(--x))`.
- Tokenuri/fonturi (`globals.css`, `tailwind.config.ts`, `[locale]/layout.tsx`): paleta semantica shadcn remapata pe bej/nuc; paleta de brand noua (walnut/sage/amber/brass/crimson/info + soft) ca tokenuri Tailwind; fonturi `DM Sans`/`Instrument Serif`/`JetBrains Mono` via `next/font`; shadows/radius/easing + fundal radial cald + utilitare `.serif`/`.label`/`.kicker`/`.page-title`; `.dark` populat din tema galerie a prototipului (necomutabila inca); accent-color global nuc pe checkbox/radio.
- Primitive noi `components/ui/`: `input`, `textarea`, `select`, `label`, `field`, `switch`, `badge` (+`StatusBadge`/`TierBadge`), `avatar`, `separator`, `alert`, `table`, `tabs`, `dialog` (radix), `stepper`, `score-gauge`, `choice-card`, `slot-track`, `timeline`, `dropzone`, `toaster` (sonner); `button` extins cu varianta `walnut` + size `icon-sm`; `card` pe shadow-sm. Dependinte noi: `@radix-ui/react-dialog`, `sonner` (Toaster montat in `lib/providers.tsx`).
- Shell: `app-header` reconstruit (brand serif „Plan" + role pill + `LangSwitch` nou + clopotel + logout); `admin/layout` sidebar in stilul prototipului (sectiuni mono, nav-item activ gradient ink); `AppShell` partajat (header + container `max-w-6xl` + `animate-pageIn`) pe grupurile requests/marketplace/company; layout `(auth)` nou (card centrat brandat).
- Pagini re-skinate 1:1: landing (hero+metrici+how-it-works+sizing+CTA band), dashboard, login/register/verify-email, cereri (lista+detaliu+wizard cerere noua+oferte/compara), marketplace (feed+detaliu+claim+wallet+claims), firma (onboarding+dashboard), admin (dashboard+companies+companies/[id]+disputes+payments+withdrawals+audit+settings+jobs); componente partajate chat-panel, offer-card, offer-builder, claim-lifecycle-panel, client-clarifications, client-fulfillment.
- i18n: namespace `Landing` extins cu chei noi (hero/metrici/pasi/sizing/CTA) in `en.json` + `ro.json` (RO cu diacritice), paritate completa verificata.
- Verificat: `pnpm -F frontend typecheck` OK dupa fiecare batch + final; JSON i18n valid + paritate Landing RO/EN; sweep fara culori hardcodate ramase (neutral/blue/red/emerald) in pagini. NEVERIFICAT: `next build` (EPERM pe `.next/trace` blocat de dev-server-ul pornit — limitare de mediu, nu de cod); `next lint` neconfigurat in proiect (prompt interactiv).
- Presupuneri: tema `.dark` ramane cablata dar necomutabila (nu s-a cerut switcher); wizardul cerere noua ramane formular pe o pagina (nu s-a transformat in multi-step, ca sa nu se atinga logica); Select primitiv = `<select>` nativ stilizat (compatibil RHF).

## Sprint 9 (final) — 2026-06-16 — Admin complet + notificari + seed demo + hardening
- DECIZIE sesiune: feature complet notificari in-app; jobs viewer UI minim custom; faze cu checkpoint.
- Prisma: model nou `notifications` (userId/type/title/payload/readAt) + migratie `notifications`. Restul entitatilor admin existau deja.
- Notificari (docs/06 §7.3): `NotificationsModule` — `NotificationsProcessor` consuma coada `notifications` (alimentata de EventBus) si persista cate o notificare per destinatar; `EventBusService.publish` extins sa transmita `__targets` in job; `NotificationsController` (`GET /notifications`, `/unread-count`, `POST /:id/read`, `/read-all`).
- Admin (4.19): `AdminModule` — `GET /admin/kpi` (firme/cereri pe status, claim-uri active, dispute, plati, abonamente, credite, venit facturat, audit), `GET /admin/audit-logs` (paginat + filtre, 3.9), settings CRUD (`system_settings`, `penalty_rules`, `subscription_plans`, `credit_packages`, `project_size_thresholds` — toate auditate `@Audit`), jobs viewer (`GET /admin/jobs` esuate din toate cozile + `POST /admin/jobs/:queue/:id/retry`, 3.11).
- Seed demo (docs/06 §7): `prisma/seed-demo.ts` (idempotent prin `admin@demo.ro`, parola `Demo1234!`) — 1 admin + 5 clienti numiti + 8 firme cu stari exacte (Gold/Platinum/Silver/expirat, PENDING+risk flags, REJECTED, SUSPENDED 12pct, echipe A&B, B coverage 100km) + 15 cereri cu distributia exacta de statusuri + tipuri + 2 paid-design + 1 oferta EUR + review 5★ si 2★→DISPUTED + 2 facturi + 60 audit + 30 notificari. Verificat: KPI admin reflecta exact datele.
- Frontend: consola admin (`admin/layout.tsx` sidebar + header cu clopotel) + pagini `dashboard` (KPI), `audit` (tabel paginat + filtru), `settings` (editoare system/penalty/plans/packages/thresholds), `jobs` (esuate + retry); clopotel notificari (badge necitite + dropdown + mark-read, poll 30s + invalidare realtime) pe consola admin si dashboard; hooks `use-admin`/`use-notifications`; i18n RO+EN `Admin`/`Notifications`.
- Hardening (security-close): **FIX** audit IP salt folosea fallback hardcodat (`AUDIT_IP_SALT` inexistent) → acum `IP_HASH_SALT` validat din config; **FIX** webhook plata folosea fallback slab `'dev-webhook-secret'` → acum `PAYMENT_WEBHOOK_SECRET` din config (getOrThrow) + rate limit 30/min; **FIX** rate limits per-ruta lipsa (3.13) → claim 10/min, upload 30/min (requests/chat/quote presign), webhook 30/min; **FIX** `@Audit` adaugat pe deciziile admin ramase (company approve/reject, withdrawal review). PASS: guards/RBAC, idempotency, ownership checks, presigned+SAFE, format eroare unic, DTO whitelist.
- Verificat: typecheck shared+backend+frontend OK; JSON i18n valid; migratie aplicata; backend boot OK (toate rutele + DI, ConfigService/AuditInterceptor rezolvate); smoke Phase B 14/14 (admin KPI/settings/audit/penalty/jobs, RBAC 403, notificari processor); seed demo coerent (KPI verificat).
- Presupuneri: seed-demo pe DB proaspat (audit append-only nu permite stergere la re-rulare); blocul penalizare angajat ramane descopat (vezi Sprint 7); admin-in-chat la dispute minimal.

## Sprint 8 — 2026-06-16 — Livrare→COMPLETED + review/dispute + audit + facturare RO
- DECIZIE sesiune: flux complet plata+factura (4.16/3.7, neimplementate pana acum); creditele firmelor care **pierd** oferta se CONSUMA (pay-to-play, fara refund) — rezolva intrebarea deschisa din Sprint 6 (retrofit in `acceptQuote`).
- Prisma: modele noi `reviews`, `review_disputes`, `credit_packages`, `mock_billing_orders`, `audit_logs` + enum-uri `ReviewDisputeStatus`/`BillingOrderType`/`BillingOrderStatus` (nedefinite verbatim, confirmate sesiune); relatii pe `companies`/`requests`. Migratie `delivery_reviews_disputes_billing_audit` cu **trigger DB append-only** pe `audit_logs` (BEFORE UPDATE OR DELETE → RAISE EXCEPTION, 3.9). Seed: date furnizor (seller_*) + serie factura + 3 pachete credite.
- Livrare (4.18): `FulfillmentService` — firma castigatoare `POST /company/requests/:id/deliver` (ACCEPTED→DELIVERED_BY_COMPANY); clientul `POST /requests/:id/confirm-delivery` → COMPLETED + slot castigator COMPLETED + **consum credite castigator**.
- Review/dispute (4.18): `POST /requests/:id/review` (client, dupa COMPLETED, o singura data); sub 3★ → `review_disputes` OPEN + request DISPUTED; admin `GET /admin/disputes` + `POST /admin/disputes/:id/resolve` (RESOLVED/DISMISSED → request revine COMPLETED), decizie **auditata**.
- Audit (3.9): `AuditModule` global — `@Audit('action')` + `AuditInterceptor` (asteapta scrierea inainte de raspuns via `concatMap`, audit garantat) + `AuditService` (IP hash-uit cu salt, niciodata date sensibile). Aplicat pe rezolvare dispute + confirmare plata (extensibil in Sprint 9). Viewer-ul UI ramane Sprint 9.
- Facturare/plata (4.16/4.17/3.7): `PaymentsService` — `GET /billing/credit-packages`, `POST /billing/credits/purchase` + `POST /billing/subscription/purchase` (Idempotent) → comanda PENDING cu TVA snapshot (21%); confirmare `POST /admin/payments/:id/confirm` (calea A, auditat) sau `POST /webhooks/payment` (calea B, HMAC-SHA256 + Idempotent + replay) → CONFIRMED + numerotare factura serie-numar secventiala + seller snapshot + grant credite / activare abonament. `InvoicePdfService` (puppeteer, ESM-safe) — factura PDF conforma RO (`GET /billing/orders/:id/invoice`).
- Shared: `reviews.schemas.ts` + `billing.schemas.ts` (Zod + DTO-uri ReviewDto/AdminDisputeItemDto/CreditPackageDto/BillingOrderDto/AdminBillingOrderItemDto + Resolve/Purchase/Webhook inputs); enum-uri noi; coduri noi DELIVERY_NOT_ALLOWED/COMPLETION_NOT_ALLOWED.
- Frontend: hooks `use-billing` (pachete/comenzi/purchase/admin confirm) + `use-fulfillment` (deliver/confirm/review/admin disputes); componenta `client-fulfillment` (confirmare livrare + review pe `/requests/[id]/offers`); buton „marcheaza livrata" pe pagina firmei `/marketplace/claims/[id]`; sectiune cumparare credite + comenzi + descarcare factura pe wallet; pagini admin `/admin/payments` si `/admin/disputes`; i18n RO+EN namespace `Fulfillment`/`Billing`/`AdminPayments`/`AdminDisputes`.
- Verificat: typecheck shared+backend+frontend OK; JSON i18n valid; migratie aplicata (cu trigger) + seed; backend boot OK (toate rutele mapate, DI rezolvat); **smoke E2E pe infra reala — 19/19**: deliver→confirm→COMPLETED+consum credite → review 5★ (fara dispute) → review 2★→DISPUTED→admin resolve→**audit log scris + UPDATE blocat de trigger** → purchase→admin confirm→factura PDF+grant credite → webhook HMAC (valid + semnatura gresita 401) → accept→consum credite pierzator.
- Presupuneri: numerotare factura prin `max(number)+1` sub tranzactie (backstop unique serie+numar); abonament platit = 30 zile; review doar de client dupa COMPLETED; admin-in-chat la dispute (4.19) ramane minimal (resolve + nota) — extindere posibila in Sprint 9.

## Sprint 7 — 2026-06-15 — SLA + clarificari + penalizari (firma) + anulare/retragere claim
- DECIZIE sesiune: **blocarea angajatului (9 pct) e DESCOPATA** — pastram doar penalizarile la nivel de firma (prag 12 → SUSPENDED 6 luni). `penalty_events.scope` ramane in schema, dar MVP creeaza doar scope=COMPANY. (Abatere constienta de la 4.12/D-v6-6.)
- Prisma: modele noi `claim_withdrawals`, `clarification_requests`, `penalty_events`, `penalty_rules`, `business_calendar_holidays` + enum-uri `WithdrawalReasonType`/`WithdrawalStatus` (verbatim §5.5)/`PenaltyScope`/`ClarificationStatus` (PENDING/ANSWERED, confirmat sesiune); `ClaimSlotStatus` += `WITHDRAWN`/`SLA_EXPIRED` (confirmat sesiune); coloane noi `requests.last_edit_at` + `requests.deleted_at` (lipseau din Sprint 4 desi docs/04 le cere). Migratii: `sla_penalties_clarifications_withdrawals`, `request_last_edit_at`, `request_deleted_at`.
- SLA (4.11/D-v6-4): `ClaimsService` materializeaza `sla_deadline_at = addWorkingDays(claim, 3/3/5)` la claim + programeaza job `sla-breach` la deadline+12h grace. `SlaBreachProcessor`: daca slotul e inca ACTIVE (fara oferta) si nepauzat → `SLA_EXPIRED` + consum credite + 3 pct penalizare firma; daca cererea ramane fara claim activ → re-publicare `IN_MARKETPLACE` cu ceas nou (+5 zile lucr., D-v6-13) + excludere firme (`SLA_BREACH`), fara consum repost (Î20).
- Calendar (3.3): `BusinessCalendarService` consulta acum `business_calendar_holidays` (cache in memorie, `onModuleInit`/`refresh`) — sarbatori mobile Paste/Rusalii 2025–2028 (seed) + override admin (punti). API sync pastrat.
- Clarificari (4.11): `ClarificationsService` — firma cere clarificare (`POST /claims/:id/clarifications`) → `slaPausedAt`=now + `sla_deadline_at` +1 zi lucr. + reprogramare job; clientul raspunde (`POST /client/clarifications/:id/answer`) → ANSWERED + reluare SLA; o singura clarificare PENDING odata (`CLARIFICATION_ALREADY_PENDING`).
- Anulare/retragere (4.15): `WithdrawalsService` — motive auto-validate (CLIENT_UNRESPONSIVE_48H pe `chat_threads.last_client_message_at`, REQUEST_MODIFIED_POST_CLAIM pe `last_edit_at`; CLIENT_CONTACT_INVALID/CLIENT_REQUESTED_CANCELLATION mock-auto in MVP) → AUTO_APPROVED + refund + slot `WITHDRAWN`; VOLUNTARY_NO_REASON cu gratie 30 min (refund + 0 pct sub 30 min; altfel fara refund + 2 pct + `WITHDRAWN_VOLUNTARY`); CUSTOM → `PENDING_ADMIN_REVIEW` (slot ramas ocupat) + job reminder 48h (`withdrawal.reminder`, fara auto-decizie); admin decide (`/admin/withdrawals` + `:id/review`). Stergere cerere de client (Î17): `DELETE /requests/:id` → soft delete + claim-uri active `CANCELLED_BY_CLIENT` + refund.
- Penalizari (4.12): `PenaltiesService.applyPenalty` (fereastra rolling 180 zile, expirare individuala) + prag firma 12 → SUSPENDED N luni; `penalty_rules` seed (SLA_MISS=3, MASS_SLA_MISS=3, VOLUNTARY_WITHDRAWAL=2); `GET /penalties/me` (accesibil si firmelor SUSPENDED, fara CompanyApprovedGuard).
- Fix (preexistent Sprint 5, descoperit la smoke): query-ul raw de claim folosea `project_score` (coloana reala `size_score`) si `id = $1::uuid` (id e TEXT) → ambele aruncau 500; corectat (`size_score AS project_score`, fara cast). `POST /claims` rulat E2E pentru prima data.
- Jobs BullMQ noi: `sla-breach`, `withdrawal-reminder`.
- Shared: `claims-lifecycle.schemas.ts` (Zod withdrawal/clarification + DTO-uri ClaimWithdrawalDto/ClarificationRequestDto/PenaltyEventDto/CompanyPenaltyStatusDto/AdminWithdrawalItemDto); `CLAIM_SLOT_STATUSES`+`CLARIFICATION_STATUSES`; `ClaimSlotDto`/`ClaimQuoteContextDto` extinse cu SLA; coduri noi WITHDRAWAL_ALREADY_DECIDED/CLAIM_NOT_WITHDRAWABLE/REQUEST_NOT_DELETABLE.
- Frontend: hooks `use-claims-lifecycle` (clarificari/retrageri/admin) + `use-penalties`; componente `claim-lifecycle-panel` (SLA badge + clarificare + retragere) si `client-clarifications` (raspuns client); SLA/retragere pe pagina firmei `/marketplace/claims/[id]`, raspuns clarificari pe `/requests/[id]/offers`, banner penalizari pe wallet, pagina admin `/admin/withdrawals`, buton stergere pe detaliul cererii; i18n RO+EN namespace `Lifecycle`/`Penalties`/`AdminWithdrawals`.
- Verificat: typecheck shared+backend+frontend OK; JSON i18n valid; 3 migratii aplicate + seed (3 reguli penalizare + 12 sarbatori); backend boot OK (toate rutele mapate, DI rezolvat); **smoke E2E pe infra reala — 19/19**: claim cu SLA materializat → clarificare (pauza+reluare) → retragere voluntara dupa gratie (2 pct) → retragere CUSTOM + admin approve → stergere cerere client → SLA breach (worker real) → republicare + excludere + suspendare la 12 pct → `penalties/me`.
- Presupuneri: SLA = termen de trimitere oferta; clarificarea adauga +1 zi lucr. si pauzeaza (nu insumeaza durata reala a pauzei); ratare in masa = cand requestul ramane fara claim activ dupa o ratare; reasons fara dovada reala auto-aprobate (mock MVP).

## Sprint 6 — 2026-06-15 — Chat realtime + ofertare structurata (versiuni, block, reofertare, PDF)
- Prisma: modele `quotes`, `quote_versions`, `quote_change_requests`, `quote_validity_extensions`, `physical_consultation_invites`, `messages` + enum-uri `quote_currency`/`quote_status` (DRAFT/SENT/WITHDRAWN/EXPIRED/ACCEPTED/SUPERSEDED)/`quote_change_request_status` (PENDING/FULFILLED/REJECTED)/`physical_consultation_invite_status` (verbatim); coloana `chat_threads.last_client_message_at`; `claim_slots.quote_id` ramane denormalizat (index partial 1-claim-fara-oferta). Atasamentele oferta/mesaj trec prin tabelul generic `attachments` (entity_type QUOTE_VERSION/MESSAGE). Migratie `chat_messages_quotes_offers`. NB: `quotes` e 1—N per slot (retragere → WITHDRAWN, firma poate re-oferta).
- Ofertare (4.13): `QuotesService` — v1 (`POST /quotes`, Idempotent), cerere modificare client (`POST /client/quotes/changes`), raspuns firma cu versiune noua (`/quotes/:id/revise`, Idempotent, marcheaza change FULFILLED, max 3 inlantuite → `QUOTE_VERSION_LIMIT_REACHED`), refuz modificare (`/changes/:id/reject` → REJECTED, nu consuma slot; aceeasi modificare nu poate fi reluata → `CHANGE_REQUEST_ALREADY_REJECTED`).
- Block dupa v3 (D3): varianta suplimentara voluntara (`/quotes/:id/extra`, `extra_versions_count++`), invitatie la sediu (`/quotes/:id/consultation-invites`, expira +7 zile cal. via job → EXPIRED), inchidere negociere online (`/quotes/:id/end-negotiation` → chat read-only ambii).
- Reofertare 2 butoane (D4): A `/quotes/:id/extend-validity` (extinde valid_until, max 2 × N zile, audit in `quote_validity_extensions`, nu consuma slot); B `/quotes/:id/reoffer` (versiune noua initiata de firma la expirare, consuma slot, sub limita de 3).
- Valabilitate (D-v6-7): `valid_until = trimitere + quote_validity_default_days` (14 cal., configurabil sau setat de firma); job `quote-validity` la `valid_until` → quote EXPIRED daca nu a fost extinsa/inlocuita.
- Moneda (D-v6-12): `quotes.currency` RON/EUR; DTO expune `priceRon`/`priceEur`/`designFeeRon`/`designFeeEur` cu `eur_ron_rate` (5.2); suma contractuala in moneda firmei, conversia informativa.
- Design fee (D-v6-11): `quote_versions.design_fee` doar daca `requests.includes_paid_design`; altfel respins.
- Permisiuni campuri oferta (Î5/4.13): `assertFieldPermissions` verifica matricea `company_offer_field_permissions` per rol pentru fiecare camp atins (designFee sub PRICE) → `OFFER_FIELD_NOT_EDITABLE`; EMPLOYEE_MANAGED nu poate oferta.
- Retragere oferta (4.13): `/quotes/:id/withdraw` in 1 zi lucratoare (`BusinessCalendarService.addWorkingDays`) → quote WITHDRAWN + slot revine ACTIVE; dupa fereastra → `QUOTE_WITHDRAW_WINDOW_CLOSED`.
- Acceptare (4.14/Î19): `POST /client/quotes/:id/accept` (Idempotent) → request ACCEPTED, quote ACCEPTED, ofertele celorlalte firme → SUPERSEDED + chat-urile lor read-only imediat; chat castigator ramane activ. Tranzitii status cerere: ≥1 oferta → OFFERS_RECEIVED, modificare → NEGOTIATION, accept → ACCEPTED (helper forward-only).
- Chat (4.14): `ChatService` + controllere client (`/chat/*`) si firma (`/company/chat/*`); thread auto-creat la claim (Sprint 5), mesaje text + atasamente presigned (entity MESSAGE, max 5, 3.4), `last_client_message_at` (pentru CLIENT_UNRESPONSIVE_48H, Sprint 7), enforce read-only.
- Realtime (3.5): `EventBusService.publish` extins cu `targetUserIds` — `message.created`/`quote.*` se emit DOAR catre participanti (client + membrii firmei), nu broadcast; eveniment `quote.accepted` catre toti participantii cererii.
- PDF (Sprint-0 §9): `QuotePdfService` (puppeteer, Chromium lazy) — `GET /quotes/:id/pdf` + `/client/quotes/:id/pdf`, A4, dubla afisare RON/EUR + linie design_fee.
- Jobs BullMQ noi: `quote-validity`, `consultation-expiry` (ambele notifica participantii la expirare).
- Shared: `quote.schemas.ts` (Zod create/revise/extra/change/extend/consultation + DTO-uri QuoteDto/QuoteVersionDto/QuoteChangeRequestDto/ConsultationInviteDto/ClaimQuoteContextDto) + `chat.schemas.ts` (sendMessage + MessageDto/ChatThreadDto); enum-uri QUOTE_STATUSES/QUOTE_CHANGE_REQUEST_STATUSES; coduri noi QUOTE_ALREADY_SENT/CHANGE_REQUEST_NOT_PENDING (restul erau deja anticipate).
- Frontend: hooks `use-quotes` (firma + client, Idempotency-Key pe create/revise/extra/reoffer/accept), `use-chat` (threads/messages/send/upload), `use-socket` (`useRealtimeSync` invalideaza cache pe evenimente); componente `chat-panel`, `offer-builder` (RHF+Zod), `offer-card` (versiuni, dubla moneda, actiuni per rol, PDF); pagini `/marketplace/claims/[claimSlotId]` (firma: builder/management oferta + chat) si `/requests/[id]/offers` (client: oferte + accept/modificare/consultanta + chat); link-uri din wallet + detaliu cerere; i18n RO+EN namespace `Quotes`+`Chat` (erori mapate pe cod).
- Verificat: typecheck shared + backend + frontend OK; JSON i18n valid; migratie `chat_messages_quotes_offers` aplicata + seed rulat; backend boot OK (DI rezolvat, toate rutele mapate); smoke E2E pe infra reala (Postgres/Redis/Chromium) — flux complet oferta v1 → modificare client → revizuire v2 → acceptare → 409 re-accept → PDF (%PDF) → chat mesaj firma↔client, toate verde. Fix: puppeteer 25 e ESM-only — import dinamic via `new Function` ca TS (commonjs) sa nu-l transforme in `require()` (ERR_REQUIRE_ESM). puppeteer+Chromium + socket.io-client instalate.
- Presupuneri: o oferta deschisa per slot (status SENT/EXPIRED), iteratiile sunt versiuni; interactiunea client (chat/oferte) cere CLIENT autentificat care detine cererea (request.client_user_id) — cererile anonime pe token nu negociaza in Sprint 6 (consistent cu rutele owner-authenticate amanate din Sprint 4; seed-ul Sprint 9 foloseste conturi numite); creditele firmelor care pierd oferta raman `reserved` (consume/refund definitiv in fluxul de finalizare Sprint 8); `clarification_requests` (SLA-pausing) raman Sprint 7; `@Audit` infra AMANATA Sprint 9.

## Sprint 5 — 2026-06-14 — Marketplace + Claim (eligibilitate, gating, credite, concurrency)
- Prisma: modele `subscription_plans`, `subscriptions`, `company_credit_wallets`, `credit_transactions`, `claim_slots`, `chat_threads`, `request_company_exclusions`, `idempotency_keys` + enum-uri `subscription_plan_tier`/`subscription_status`/`credit_transaction_type`/`claim_slot_status` (6 valori verbatim docs/04)/`request_exclusion_reason`; migratie `marketplace_claims_subscriptions_credits`. Indecși partiali UNIQUE: 1-claim-activ-fara-oferta per assignee (`status='ACTIVE' AND quote_id IS NULL`) + 1-claim per firma per cerere (`status IN ACTIVE/OFFER_SENT/COMPLETED`).
- Claim concurrency (3.1): `ClaimsService.create` în `$transaction` Serializable, timeout 10s, `SELECT … FOR UPDATE` raw pe rândul `requests`; pași: lock → status ∈ {IN_MARKETPLACE, CLAIMED_PARTIAL} (`CLAIM_NOT_ALLOWED`) → sloturi ocupate < max (`CLAIM_SLOTS_FULL`) → excludere (`COMPANY_EXCLUDED_FROM_REQUEST`) → claim propriu existent (`CLAIM_ALREADY_EXISTS`) → acoperire Haversine vs `coverage_radius_km` (`OUT_OF_COVERAGE_AREA`) → reguli atribuire (1-claim/cap manager) → insert slot + `chat_thread` + rezervare credite + recalcul status cerere. `mapUniqueViolation` backstop P2002 pe indecșii partiali.
- Idempotenta (3.2): `@Idempotent()` + `IdempotencyInterceptor` pe `POST /claims` — header `Idempotency-Key` obligatoriu, hash sha256({body,userId}), dedup 24h pe `(key,endpoint)`; același key+hash → răspuns cache; key+hash diferit → `IDEMPOTENCY_CONFLICT` (409).
- Gating per plan (4.8/4.10): `SubscriptionActiveGuard` atașează `gatingDelayMinutes` din `marketplace_gating_delay_minutes` (Platinum +0 / Gold +30 / Silver +60); marketplace ascunde cererea până la `published_at + delay`.
- Credite (4.5): cost snapshot SMALL=1/MEDIUM=2/LARGE=4 înghețat pe `claim_slot`; `CreditsService` tx-aware (`balance`=liber, `reserved`=blocat) cu GRANT/RESERVE/REFUND/CONSUME + `credit_transactions`; `RESERVE` aruncă `INSUFFICIENT_CREDITS` (402) dacă sold insuficient.
- Trial (4.16): `SubscriptionsService.startTrialIfEnabled` rulat în tranzacția de aprobare admin (retrofit `companies.service`) — creează abonament trial Gold + grant 10 credite bonus, condiționat de `system_settings` (`trial_enabled`/`trial_plan`/`trial_duration_days`/`trial_bonus_credits`).
- Atribuire (4.9): self-assign la creare sau cap manager (sloturi neatribuite ≤ angajați liberi); reatribuire `POST /claims/:id/assign` (OWNER/MANAGER) cu check 1-claim (`ASSIGNED_USER_HAS_ACTIVE_CLAIM`); job BullMQ `claim-assign` la +1h → neatribuit → `CANCELLED_UNASSIGNED` + refund + recalcul status + `claim.withdrawn`.
- Marketplace (4.2): `MarketplaceService` list/detail via `$queryRaw` cu Haversine SQL + `CROSS JOIN LATERAL` pe `company_locations` (filtru `coverage_radius_km`), gating (`make_interval`), excludere `NOT EXISTS`, ocupate < max; **fără date de contact** în DTO-uri pre-claim.
- Guards: `CompanyApprovedGuard` (atașează `companyContext`; `COMPANY_NOT_APPROVED`/`COMPANY_SUSPENDED`) + `SubscriptionActiveGuard` (`SUBSCRIPTION_INACTIVE`) pe rutele `@Roles(COMPANY_USER)` marketplace/claims; `@CurrentCompany` decorator.
- Settings: `SettingsModule` global (`getString/getInt/getNumber/getBool`) peste `system_settings`; seed `subscription_plans` (SILVER 149/15cr/60min, GOLD 399/50cr/30min, PLATINUM 899/120cr/0min) + 13 settings (idempotent, fără overwrite valori ajustate de admin).
- Shared: `marketplace.schemas.ts` (Zod createClaim/assignClaim + DTO-uri MarketplaceItemDto/MarketplaceDetailDto/ClaimSlotDto/CreditWalletDto/SubscriptionDto/ClaimedRequestDto); enums noi SUBSCRIPTION_PLAN_TIERS/SUBSCRIPTION_STATUSES/CREDIT_TRANSACTION_TYPES/REQUEST_EXCLUSION_REASONS.
- Frontend: hooks TanStack `use-marketplace` (marketplace/detail/claim cu `Idempotency-Key`/assign/myClaims/wallet/subscription); pagini `/marketplace` (listă carduri cu distanță/credite/sloturi/gating-ago), `/marketplace/[id]` (detaliu + buton claim cu validare credite/sloturi), `/marketplace/wallet` (portofel + abonament + revendicări); link „Marketplace” pe dashboard firmă (status APPROVED); i18n RO+EN namespace `Marketplace` (erori mapate pe cod).
- Verificat: typecheck shared + backend + frontend OK; migratie aplicată; seed rulat (3 planuri + 13 settings); shared rebuild pentru exporturi noi.
- Presupuneri: `balance` = credite libere și `reserved` = blocate la claim (refund la anulare, consume la finalizare); marketplace listează doar cereri `IN_MARKETPLACE`/`CLAIMED_PARTIAL` cu cel puțin o locație firmă în rază; cap manager = sloturi neatribuite active ale firmei + 1 ≤ angajați liberi (interpretarea 4.9); chat thread creat la claim dar funcțional în Sprint 6.

## Sprint 4 — 2026-06-13 — Cereri client (draft anonim, scoring, geocoding, upload, expirare)
- Prisma: modele `requests`, `request_versions`, `request_rooms`, `request_items`, `request_contact_preferences`, `attachments` (generic entity_type/entity_id), `project_sizing_config`, `project_size_thresholds`, `geocoding_cache` + enum-uri `request_status`/`budget_range`/`room_type`/`material`/`item_system`/`attachment_status`/`project_size` (verbatim docs/04 + enums.ts); migratie `requests_rooms_items_sizing_geocoding`.
- Draft anonim cu token (3.x): `POST /requests/drafts` generează token (sha256 stocat în `draft_token_hash`), `OptionalJwtAuthGuard` leagă draftul de client dacă e logat; GET/PATCH draft pe token; un draft are status `DRAFT`.
- Publicare: validează conținut complet (Zod/DTO), geocodează adresa, calculează scoring + cost credite, setează `IN_MARKETPLACE` + `published_at` + `expires_at` (5 zile lucrătoare), scrie snapshot `request_versions` v1, programează job BullMQ expirare, emite `request.status_changed` prin EventBus.
- Scoring (docs/sprint-0 §7): `SizingService` citește `project_sizing_config` (greutăți ROOM_TYPE/ROOM_SIZE/MATERIAL/SYSTEM/ITEM_QUANTITY/PAID_DESIGN/BUDGET) + `project_size_thresholds` (SMALL 0–14→1cr, MEDIUM 15–29→2cr, LARGE 30+→4cr); seed idempotent `prisma/seed.ts` (tsx).
- Geocoding (3.8): `GeoService` Nominatim (user-agent custom, countrycodes=ro, timeout 8s) cu cache 90 zile în `geocoding_cache`; eșec → `GEOCODING_FAILED`.
- Calendar business (3.3): `BusinessCalendarService` Europe/Bucharest, weekend + sărbători legale RO cu dată fixă, `addWorkingDays` cu conversie corectă EET/EEST via Intl pentru sfârșitul zilei lucrătoare.
- Expirare: coadă `request-expiration` (job întârziat la `expires_at`); `RequestExpirationProcessor` trece `IN_MARKETPLACE` → `EXPIRED` doar dacă nu e preluată, emite eveniment.
- Editare (docs/03): 3 editări pre-claim (`IN_MARKETPLACE`), 1 post-claim (`CLAIMED_*`); blocaj după oferte (`EDIT_BLOCKED_OFFER_RECEIVED`); altfel `REQUEST_NOT_EDITABLE`; re-geocodare doar dacă adresa s-a schimbat; snapshot versiune la fiecare editare. Repost cerere expirată o singură dată (`REPOST_ALREADY_USED`).
- Upload presigned (3.4): `UploadsService` generic — presign PUT (15min) → confirm via `objectExists` (PENDING_UPLOAD → SAFE, fără AV real în MVP) → download presigned (5min); max 10 atașamente/cerere (`FILE_LIMIT_REACHED`), tipuri jpeg/png/webp/pdf, max 10MB; rute scope-uite pe token.
- Guards: rute draft `@Public` + `OptionalJwtAuthGuard`; liste client `@Roles(CLIENT)` (owner check în service). CompanyApprovedGuard rămâne pentru Sprint 5.
- Shared: `request.schemas.ts` (Zod content/patch/edit/contact/room/item + presign/confirm + DTO-uri RequestDto/RequestListItemDto/RequestRoomDto/RequestItemDto/AttachmentDto/RequestSizingDto/RequestDraftCreatedDto/PresignUploadResultDto); enums noi BUDGET_RANGES/ROOM_SIZE_BUCKETS/ITEM_QUANTITY_BUCKETS.
- Frontend: hooks TanStack `use-requests` (draft/publish/edit/repost/upload/list); wizard `/requests/new` (RHF+Zod, `useFieldArray` camere+iteme imbricate, multi-select sisteme, upload presigned direct în storage, token în localStorage); listă `/requests` + detaliu `/requests/[id]` (cu repost pe token); i18n RO+EN namespace `Requests` (validări + erori mapate pe cod).
- Verificat: typecheck shared + backend + frontend OK; migratie aplicată; seed rulat (22 config + 3 praguri); JSON i18n valid.
- Presupuneri: agregarea scorului (Σ camere [ROOM_TYPE+ROOM_SIZE+ITEM_QUANTITY+Σ iteme(MATERIAL+max SYSTEM)] + PAID_DESIGN + BUDGET) e interpretarea deterministică a §7 (greutățile sunt verbatim); calendar business fără sărbători mobile (Paște/Rusalii) și fără override admin — AMÂNAT la Sprint 7; atașamente fără scanare AV reală (status PENDING_SCAN/BLOCKED rezervat); editare/repost autorizate pe token (client logat reține tokenul per-cerere în localStorage) — rute owner-authenticate AMÂNATE.

## Sprint 3 — 2026-06-13 — Firme (onboarding, verificare, locații, echipe, permisiuni)
- Prisma: modele `companies`, `company_members`, `company_locations`, `company_verification_profiles`, `company_portfolio_items`, `company_offer_field_permissions` + enum-uri `company_status`/`company_member_role`/`offer_field_key` (verbatim docs/04 §5.5); migratie `companies`.
- Onboarding firmă: COMPANY_USER fără firmă creează firma și devine OWNER; tranzacție creează firma + membru OWNER + verification profile (risk flags snapshot) + seed matrice permisiuni câmpuri ofertă. Un user = o singură firmă (`@@unique userId`).
- Verificare + reaplicare: CUI normalizat (drop prefix RO); CUI deja folosit de firmă ne-REJECTED → `CUI_ALREADY_REGISTERED`; blocaj reaplicare 3 luni pe CUI **sau** email owner după REJECTED → `COMPANY_REAPPLY_BLOCKED` (4.6).
- Risk flags automate (4.6): `NO_PORTFOLIO` (0 itemi) + `INSUFFICIENT_REVIEWS` (reviews=0 < prag 3); `LOW_RATING` rezervat (reviews vin în Sprint 8). Adminul vede flag-urile dar aprobă manual oricum.
- Locații cu `coverage_radius_km` (default 50, D-v6-10): CRUD per firmă, doar OWNER/MANAGER.
- Echipă (4.7): adăugare membru existent după email (rol MANAGER/EMPLOYEE_TRUSTED/EMPLOYEE_MANAGED), schimbare rol, eliminare; ultimul OWNER nu poate fi retrogradat/eliminat (`LAST_OWNER_CANNOT_LEAVE`).
- Portofoliu: titlu/descriere/imageUrl (fără upload real — flow presigned vine în Sprint 4).
- Matrice permisiuni câmpuri ofertă (Î5/4.13): seed default la onboarding (owner/manager = orice; trusted = orice mai puțin PRICE; managed = read-only); doar OWNER ajustează (upsert pe `companyId+role+fieldKey`).
- Admin (4.6/4.19): `GET /admin/companies?status=`, `GET /admin/companies/:id` (cu risk flags + numărători), `POST /:id/approve`, `POST /:id/reject` (motiv obligatoriu); decizia + reviewer stocate pe verification profile.
- Guards: `RolesGuard` — rutele firmă `@Roles(COMPANY_USER)`, rutele admin `@Roles(ADMIN)`; autorizarea fină pe rol în firmă (OWNER/MANAGER) în service.
- Shared: `company.schemas.ts` (Zod onboarding/locație/membru/portofoliu/permisiuni + DTO-uri CompanyDto/AdminCompanyListItemDto/CompanyRiskFlag).
- Frontend: hooks TanStack (`use-company`) cu `setQueryData` din CompanyDto returnat de fiecare mutație; pagină `/company` (onboarding RHF+Zod sau dashboard cu profil/locații/echipă/portofoliu/matrice), zonă admin `/admin/companies` (listă cu filtru status + detaliu approve/reject); i18n RO+EN (`Company`, `AdminCompanies`) + erori mapate pe cod.
- Verificat: typecheck shared + backend + frontend OK.
- Presupuneri: trial Gold + 10 credite la approve (4.16) AMÂNAT la sprintul de abonamente; audit @Audit (3.9) AMÂNAT la Sprint 9 (decizia + reviewer persistate pe verification profile); risk flags fără rating/reviews până în Sprint 8; portofoliu fără upload real până în Sprint 4.

## Sprint 2 — 2026-06-13 — Auth
- Prisma: modele `users` + `refresh_tokens` (enum-uri UserRole/Language verbatim docs/04), migratie `auth_users_refresh_tokens`.
- Auth backend: register (bcrypt cost 12, rol CLIENT/COMPANY_USER), email verify mock (token in Redis 24h, link via Mailpit), login cu timing uniform + EMAIL_NOT_VERIFIED, refresh token rotation cu familie + grace 30s (reuse dupa grace → revoca familia, REFRESH_TOKEN_REUSED), sesiune unica (login nou revoca tot + `auth_expired` pe socketul vechi), logout (revoca familia), GET /auth/me.
- 2FA TOTP (otplib v12): rute setup/verify implementate, blocate de flag `TWO_FACTOR_FEATURE_ENABLED=false`; TwoFactorGuard no-op (invarianta 3.13).
- Tokens DOAR in cookies httpOnly (`mm_access` 15min, `mm_refresh` 7d pe /api/v1/auth); cookie-parser global.
- Guards globale via APP_GUARD: ThrottlerGuard (100/min global, 5/min login) → JwtAuthGuard (@Public pe rutele deschise + health) → RolesGuard (@Roles).
- Socket.IO: auth pe handshake din cookie (3.5) — token lipsa/expirat → `auth_expired` + disconnect; room `user:{id}`; EventBusService.emitAuthExpired.
- Shared: `auth.schemas.ts` (loginSchema/registerSchema Zod + AuthUser).
- Frontend: client API cu cookies + refresh-once dedupe pe 401, hooks TanStack (useMe/useLogin/useRegister/useLogout/useVerifyEmail), pagini login/register/verify-email + dashboard protejat client-side, erori mapate pe cod (3.10), i18n RO (diacritice) + EN.
- Verificat E2E: register→Mailpit→verify→login→me→refresh→logout; reuse dupa grace revoca familia (fix: revocarea scoasa din $transaction ca sa nu fie anulata de rollback la throw); sesiune unica; 2FA 403 flag off; rate limit 429 la a 6-a incercare/min.
- Presupuneri: access 15min/refresh 7d; login cere email verificat; token verify in Redis (fara coloane noi).

## Sprint 1 — 2026-06-13 — Setup monorepo + infra
- Monorepo pnpm workspaces: `apps/backend`, `apps/frontend`, `packages/shared` (+ tsconfig.base, scripts root).
- `packages/shared`: ERROR_CODES const + tipuri, enum-uri canonice din docs/04 §5.5 (verbatim), `ApiErrorBody`.
- `dev-infra/docker-compose.yml`: Postgres 16, Redis 7, MinIO (+ init bucket `uploads`), Mailpit — toate cu healthcheck.
- Backend NestJS: env Zod fail-fast (`config.schema.ts`), prefix global `api/v1`, Helmet + CORS strict, ValidationPipe global, filter erori format 3.10 cu traceId, logging pino JSON, Socket.IO cu Redis adapter (invarianta 3.5), BullMQ root + coada `notifications`, EventBusService.publish skeleton, StorageService (presigned 15min/5min), MailService (Mailpit), PrismaService.
- Prisma: prima migratie `init_system_settings` (model SystemSetting — docs/04 §5.4).
- Frontend Next.js 14 App Router: next-intl ro+en (middleware + `[locale]`), Tailwind, TanStack QueryClient provider, landing minimal bilingv.
- Verificat: typecheck ambele appuri, `next build`, infra up, `GET /api/v1/health` → db/redis/storage `up`.

## Sprint 0 — 2026-06-13 — Arhitectura si design (fara cod)
- `docs/sprint-0-architecture.md`: arhitectura monorepo, module backend + zone frontend, model de date complet (pseudo-prisma, enum-uri verbatim din docs/04), endpoint map REST, flow-uri critice (claim, accept offer, SLA breach individual/in masa, payment confirm), seed scoring + praguri (1/2/4 credite), campuri formular cerere, decizie PDF = puppeteer, riscuri tehnice.
- `docs/STATE_CONVENTIONS.md`: TanStack vs Zustand vs RHF + 5 exemple (cerere, claim, oferta, chat, dashboard).
- `docs/ERROR_CODES.md`: codurile anticipate UPPER_SNAKE_CASE per domeniu.
- In asteptarea revizuirii umane: seed scoring (§7), campuri formular (§8), puppeteer (§9), `IN_MARKETPLACE` ca nume canonic de status.
